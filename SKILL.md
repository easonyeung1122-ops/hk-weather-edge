---
name: hk-weather-edge
version: 0.15.2
description: Polymarket「香港最高气温」日度市场的 edge 计算与实况外推工具。把香港天文台(HKO)开放数据 + Open-Meteo 多模式 NWP 集合转为校准后的摄氏整数档(bucket)公允概率，并与市场价对比输出 EV 与 35% Kelly 仓位建议；`--watch` 模式基于结算站实测 + 日内气候曲线做当日峰值 nowcast。当用户提到「香港气温市场」「最高气温预测」「HKO / 天文台结算」「bucket 概率」「temperature bucket edge」「今天香港会到几度」「 polymarket weather Hong Kong」，或需要判断某个整数档是否值得买 YES / NO 时使用。
---

# 香港气温市场 Edge 工具

## 目的

把公开气象数据转换成**可与市场价对比的整数档概率**，用于 Polymarket「Highest temperature in Hong Kong」日度市场。
产出三样东西：每个整数档的公允概率、相对市场价的 EV 与动作（买 YES / 买 NO / 观望）、**35% Kelly 下注额（金额 + 份数）**。

## 核心认知（决定这个工具有没有用）

Edge **不是**来自比天文台更会预报天气。它来自三个结构性错配：

1. **结算只认一个传感器**——天文台总部站（尖沙咀）。机场、打鼓岭、手机 App 的数都不作数。
2. **市场把点预报当概率**——但结算是 0.1 °C 精度的一维分箱 `floor()`，两者系统性错配（实测偏左：`-1` 档占 43.7%，命中市场定价档仅 31.0%）。
3. **时区窗口**——香港白天是美东深夜，挂单薄，错误定价停留更久；实况每小时更新而官方 CLMMAXT 定稿滞后。

**每次出手前必须记住**：即使完美预知当日期望最高温，命中那个整数档的概率也只有约 31–40%。永远不要用单档全仓去"猜"。

## 结算口径（下单前必读）

| 项目 | 内容 |
|---|---|
| 结算源 | HKO Daily Extract → `Absolute Daily Max (deg. C)`（`https://www.weather.gov.hk/en/cis/climat.htm`） |
| 站点 | **天文台总部站（尖沙咀）**，非机场 / 非打鼓岭 / 非 App |
| 精度 | **0.1 °C**，规则明文以该精度结算 |
| 档位定义 | 档位 `N°C` = 区间 `[N.0, N+1.0)`，即 `floor(实测值)`。31.5 → 31 档；29.6 → 29 档 |
| 日界 | 香港当地日 HKT 00:00–24:00 |
| 修订 | 初始发布后的修订不予考虑，以首次公布为准 |
| 开市 | 约提前 4 天（美东 00:56 ≈ 香港 13:56） |

**档位口径的验证方法（30 秒，比读 Rules 更硬）**：拉一个已结算的历史事件，看哪一档
`outcomePrices` = 1。
```bash
python -c "import urllib.request,json; \
e=json.load(urllib.request.urlopen(urllib.request.Request('https://gamma-api.polymarket.com/events?slug=highest-temperature-in-hong-kong-on-september-10-2026',headers={'User-Agent':'Mozilla/5.0'})))[0]; \
print([(m['groupItemTitle'],m['outcomePrices']) for m in e['markets']])"
```
2026-09-12 实测：`september-10-2026` 结算到 **31°C**，而本站当日 10 分钟采样最高 **31.8**
→ 证实 `N°C = [N.0, N+1.0)`（若为四舍五入口径，31.8 会落到 32 档）。同日 `september-11-2026`（采样最高 32.5）→ 32°C，一致。

⚠️ **结算值来自 1 分钟数据，我们盯的是 10 分钟快照 —— 存在正向缺口。**
`Absolute Daily Max` 是当天**全部 1 分钟记录**的最大值，而 `latest_1min_temperature.csv`
只是每 10 分钟一次的瞬时快照，因此 **结算值 ≥ 快照最高值**。
用 2026-09-10 标定：快照最高 31.8 → 结算落在 [31.0, 32.0)，即缺口 **0 ~ +0.1°C**。
→ **当快照最高值距离档位上界 ≤0.1°C 时，档位归属进入"掷硬币"区**，
此时不要用快照值当确定事实，报价里若出现极端定价要意识到对方可能在赌这个缺口。

⚠️ **规则会变。** Polymarket 换过巴黎的结算站点、把深圳切到 NOAA。每次下单前重读该市场的 Rules。
⚠️ **与美国城市盘不同**：纽约/芝加哥用 NWS CLI / METAR（华氏整数），香港是**摄氏 0.1 位**，勿照搬中文社区那套"华氏整数不换算"的经验。

## 环境准备

脚本位于 `scripts/`，全部**仅依赖标准库**（`requests` 可选，有则网络更稳）。

Windows 下用 `py -3`（`python3` 通常不存在）。以下示例均以 skill 根为当前目录。

```bash
py -3 scripts/hk_edge.py            # 首次运行会下载 HKO 历史数据(约1.6MB)并自动校准
```

首次运行后 `scripts/data/` 会生成 `maxt_HKO.csv`（结算源历史）与 `model_calib.json`（模型偏差/残差 sd）。

### Windows：为什么会弹出一个命令框，怎么关掉（v0.13.4）

**现象**：屏幕上冒出黑色命令框。如果是**每 30 分钟自己弹一次**，那不是脚本，是计划任务。

**原因**（是 Windows 的机制，不是脚本 bug）：`python.exe`、`cmd.exe` 都是**控制台子系统**程序，
Windows 规定它们必须挂在某个控制台上。当启动它的宿主自己没有控制台时（桌面版应用、资源管理器
双击、计划任务、部分 GUI 启动器），系统会**新建一个控制台窗口**给它——那个黑框就是它。

**根治办法：换成 `pythonw.exe`。** 它是 **GUI 子系统**程序，Windows 根本不为其创建控制台，
没有窗口可弹，也不需要隐藏。凡是「无人值守、要长期跑」的调用一律走这条路。

| 场景 | 表现 | 处理 |
|---|---|---|
| **计划任务定时触发** | 任务动作写成 `watch.bat` → 每 30 分钟必弹一次，因为带 `--loop` 还会一直挂着 | 动作改为直接启动 `pythonw.exe scripts\watch_silent.py`。**不要指向 vbs**：`Run(..., 0, False)` 立刻返回，任务秒判完成，`IgnoreNew` 失效 → 每 30 分钟叠一个循环 |
| **双击 `watch.bat`** | `.bat` 只能由 cmd.exe 解析 → 必开窗口，无法规避 | **改双击 `watch_silent.vbs`**（启动 pythonw），停止用 `stop_watch.vbs` |
| 由宿主/GUI 启动 `python.exe` | 宿主无控制台 → 系统为子进程新建一个 | `hk_edge.py` 启动时自查：该控制台**只挂自己一个进程**且 stdout 不在终端上 → `ShowWindow(hwnd, 0)` 自我隐藏；在自己的终端里跑时列表长度 > 1，不动用户窗口。这是第二道防线，不是主路径 |

自查当前环境属于哪种（0 = 无控制台，不会弹）：

```bash
py -3 -c "import ctypes;print(ctypes.windll.kernel32.GetConsoleWindow())"
```

#### 两个必踩的坑（都已实测复现）

**1. `.bat` 必须写成纯 ASCII + CRLF。** cmd.exe 按系统 ANSI 代码页（中文 Windows = GBK）解析 .bat，
所以 UTF-8 中文注释会解成乱码；更糟的是 **LF-only 换行会让 `rem` 词元被粘到上一行行尾**，
cmd 于是把注释当命令执行，刷出满屏：

```
'鍛戒护妗嗐€?rem' 不是内部或外部命令，也不是可运行的程序或批处理文件。
```

同一段注释的对照实验：UTF-8+LF 版 = 4 行乱码报错；ASCII+CRLF 版 = `PARSE_OK`、零报错。
**改完用 `od -c` 或字节统计确认 `bareLF == 0`**，编辑器「另存为」默认不保证。

**2. 不要在一个 cmd.exe 正在执行的 .bat 上做就地改写。** cmd 是**分块增量读取**批处理文件的，
改写会让它重新读到新内容并**重复执行命令**——2026-09-10 排查时实测：重写 `watch.bat` 后，
同一个 cmd 又起了一个 `python.exe --loop`（16:35:07，父子关系已核实）。计划任务的动作条目不会
有这个问题，它在启动时被计划任务服务快照。

#### 日志文件的共享陷阱

不要用 shell 重定向（`>> watch.log`）写日志：**cmd.exe 的重定向句柄拒绝写共享**，只要那个循环还
活着，其它进程 append 同一个文件就一律 `PermissionError [Errno 13]`（2026-09-10 16:35 实测）。
Python 的 `open()` 允许共享，所以 `watch_silent.py` 自己开日志文件，不经 shell。

## 工作流

### A0. 取价：必须用订单簿，不要用最后成交价

**这是最容易让整套分析失效的一步。** Gamma API 的 `outcomePrices`（最后成交价）和
`bestBid/bestAsk` 都是**延迟指标**，只有 CLOB 订单簿是实时的。

实测同一时刻的差异（2026-09-07，临近结算、行情剧变）：

| 来源 | 30 档 | 31 档 |
|---|---:|---:|
| 最后成交价 `outcomePrices` | 0.82 | 0.15 |
| Gamma `bestBid/bestAsk` | 0.79 / 0.85 | – |
| **CLOB 订单簿（真实可成交）** | **0.962 / 0.988** | **0.010 / 0.040** |

差 15–17 分钱。用最后成交价算出来的 EV 完全是假的。
**越接近结算、行情变化越快，最后成交价越失真**；流动性好、变化慢的盘（如提前 1–4 天）三者通常吻合。

```bash
py -3 scripts/market_prices.py 2026-09-07            # 按香港日期取盘口
py -3 scripts/market_prices.py 2026-09-08 --depth 3  # 多看几档深度
```

输出每档的 **YES / NO 各自的 bid 与 ask**、两侧可成交深度，以及一条可直接粘贴的 `--market` 字符串
（用 YES 订单簿中间价）。

算 EV 时用**可执行价**，不用中间价：

- 买 YES → YES token 的 **ask**
- 卖 YES → YES token 的 **bid**
- 买 NO → **NO token 自己的 ask**（NO 有独立订单簿，不要想当然用 `1 − YES bid`）
- 卖 NO → NO token 的 **bid**

⚠ **脚本对 NO 边的 EV 会系统性高估，必须手工复算**（2026-09-10 实测）。
`hk_edge.py` 内部把 NO 价算成 `1 − m`，而 `m` 是你传进 `--market` 的 **YES 价**。
若你传的是 YES **ask**（推荐做法），则 `1 − YES ask = NO 的 bid`，那是**卖出** NO 才拿得到的价格。
真买 NO 要付 **NO ask**，两者差一个价差。实测：31 档脚本给 EV **+8.2%**，
按 NO ask 0.570 复算只有 **+6.3%** —— 直接从「过门槛」变成「不过门槛」。
**做法：凡脚本在 NO 边报出动作，一律用 `market_prices.py` 的 NO ask 重算一次 `P(NO)/ask − 1`。**

**算完 Kelly 必须再核对可成交量。** 临近结算时经常出现「EV +150% 但只能买 165 份（≈$6.6）」——
这种 edge 真实但不值得动手。

⚠ **薄盘报价会在几十秒内漂移，单次取价不算数——必须间隔 15–20s 连取 2–3 次**（2026-09-12 实测）。
当日 24h 成交额 **30135 完全未变**（零成交），但报价在 30 分钟内：
- 33 档 YES **0.200 → 0.340**（+70%），31 档 mid **0.310 → 0.260**，32 档 mid **0.530 → 0.460**
- 三个档位的 ask 侧深度只有 **37–128 份**（$10–40 量级）

去水分后的隐含分布出现 **33 档（28%）> 31 档（24%）** 的形状 —— 在任何合理模型下都讲不通，
纯属薄盘噪声。**结论：该市场的报价只在「多档同时、连续多次取样」下才有信息量；
直接拿一次 `market_prices.py` 的 mid 喂进 `--market` 会把噪声当信号，务必先做稳定性采样，再手工复算 EV。**

```bash
# 稳定性采样：连取 3 次，看哪些档位是稳的
for i in 1 2 3; do date "+%H:%M:%S"; py -3 scripts/market_prices.py 2026-09-12 --depth 3; sleep 20; done
```

### A. 每日扫描（找 edge）

```bash
# 1) 未来 7 天各档公允概率
py -3 scripts/hk_edge.py

# 2) 指定日期 + 喂入市场价 + 本金，输出 EV 与 35% Kelly 下注额
py -3 scripts/hk_edge.py --date 2026-09-09 --bankroll 500 \
  --market "29:0.12,30:0.30,31:0.42,32:0.11,33:0.03"

# 3) 模型与官方预报分歧时，按自己的判断手动加权点估计
py -3 scripts/hk_edge.py --date 2026-09-07 --mu 31.82 --market "31:0.37,32:0.51"

# 4) 当日：截断下限 + 覆盖点估计 + 收窄不确定性（三件套，日内必用）
py -3 scripts/hk_edge.py --date 2026-09-07 --observed-max 30.0 --mu 30.15 --sigma 0.5 \
  --market "30:0.71,31:0.235"

# 5) 同时产出 HTML 报告到 scripts/edge_report.html
py -3 scripts/hk_edge.py --html

# 6) 检查已用头寸：浮盈 + 该不该止盈（:N 表示持有 NO，默认 YES）
py -3 scripts/hk_edge.py --date 2026-09-08 --bankroll 1400 \
  --market "30:0.015,31:0.145,32:0.415,33:0.380,34:0.065,35:0.006" \
  --hold "34:0.065:200,33:0.56:70:N"
```

输出里每档给出：公允 P、市场价、动作、EV、**35% Kelly 的金额与建议份数**、**止盈警告 ⚠**、分布条。
底部固定提示一句"众数档仅 xx%——别把点预报当概率"，这是给持有仓位时的心理锚。

### A2. 当日（提前 0 天）必须做的两件事

日最高温具有**单调性**：已经实测到的最高值就是当日峰值的硬下限，不可能被抹掉。

1. **截断下限**：`--observed-max <今日已实测最高>`。省略时脚本会自动从 `--watch` 写的 `intraday_log.json` 读取，输出里会打出「已观测下限 … → 当日分布截断重命名」。
   没有这一步，脚本会把大量概率质量放在物理上不可能的低档位上（实测曾出现把 43% 放在已实测最高温以下的情况），**写进 `decision_log.csv` 会污染复盘数据集**。
2. **覆盖点估计**：`--mu`。见下方「已知局限」第 1 条——午后一旦发生对流，NWP 的日最高值会失真，`--observed-max` 只修下限、不修均值。
3. **收窄不确定性**：`--sigma`。默认的分布宽度是「提前一天」的（残差 sd + 集合距平 ≈ 0.96 °C），
   日内剩余不确定性小得多，不收窄会把结果算得过于分散（实测：同一时刻算 31 档给出 32%，物理上只有 ~3%）。
   `--sigma` 表示**剩余总不确定性**，给出后不再叠加集合距平，直接用 `N(mu, sigma)`。
   典型取值：午后可观测时段 0.3–0.5，峰值窗口刚过 0.2–0.3。

传入 `--market` 时会自动把 `日期/档位/公允P/市场价/edge` 追加写到 `scripts/data/decision_log.csv`——**结算后回来填实际档位做复盘**，这是长期唯一别人拿不走的 edge。

### A3. 实况源：必须用 0.1 °C / 10 分钟的分区气温，**不要只用 `rhrread`**

**这是 2026-09-09 用一次亏损换来的教训，优先级最高。已随 v0.10.0 接入脚本。**

`rhrread` 的 `temperature` **只有整数位**（`"value": 31`），且**每小时**才更新一次。
在结算值刚跨过整数边界时，它会同时犯两个错：

1. **漏掉整点之间的升温** —— 09-09 当天 `rhrread` 13:00 仍是 31，
   而天文台站实际在 **13:40 已升到 32.2**（40 分钟 +1.2 °C）。按 31 做外推，
   结论是「31 档还有 66%」——**31 档当时其实已经归零**。
2. **整数舍入撑开 ±0.5 °C 的未知** —— 显示 31 意味着真值 ∈ [30.5, 31.5)，
   这一步之差就决定了 30 / 31 / 32 三档的归属，而概率分布会被这 1 °C 撑得极宽。

正确的数据源（0.1 °C，**每 10 分钟**，39 个站，含结算站）：

```
https://data.weather.gov.hk/weatherAPI/hko_data/regional-weather/latest_1min_temperature.csv
```

格式：`Date time,Automatic Weather Station,Air Temperature(degree Celsius)`
→ `202609091400,HK Observatory,31.5`
注意结算站在 CSV 里的名字是 **`HK Observatory`**（不是 `Hong Kong Observatory`）。

- 脚本已接入：`fetch_hko_1min()` / `load_1min_max()`，running max 存 `data/obs_1min_log.json`，
  `watch()` 优先用它、失败才回落 `rhrread`（并打印提示）。
  **`--watch` 要每 10 分钟跑一次才能累积峰值**，见下方「跑多频繁」。
- 另有「午夜至現時的最高及最低氣溫」数据集（同目录，DATA.GOV.HK 数据集 id
  `hk-hko-rss-max-and-min-air-temp-since-midnight`）—— **TODO：CSV 确切文件名待确认**，
  未确认前仍由 `--watch` 自己按 10 分钟轮询累计当日最高。

**判断规则**：只要 `--date` 是当日、且实测值离整数边界 <0.5 °C，
就必须以上述 CSV 为准，`rhrread` 只作交叉校验。

### B. 日内盯盘（`--watch`）

```bash
py -3 scripts/hk_edge.py --watch
```

**跑多频繁？按数据的更新频率定，别跑得比数据还快**（实测 2026-09-09）：

| 输入 | 实测频率 | 证据 |
|---|---|---|
| HKO 实况 `rhrread` | **每小时**，整点后约 2 分钟 | `updateTime` 08:02 → 10:02 |
| HKO 9 天预报 `fnd` | 约每 2 小时（:50 左右） | 07:50 → 09:50 |
| NWP 多模式 `daily tmax` | 一天 4 次（00/06/12/18 UTC） | 08:43 vs 10:05，7 个模式仅 UKMO +0.3，其余一字未变 |
| CLOB 订单簿 | **实时** | 08:45→09:47，31 档 YES 0.420 → 0.505 |

→ 结论：**概率的重算节奏 = 每小时一次**（整点后 5 分跑 `--watch` + `hk_edge.py`），
因为两条路径的输入最快也就是每小时一变；跑更快只是在重复读同一个数。
**但盘口价是实时的**，要高频看的是 `market_prices.py`，不是 `--watch`。

⚠ **例外（v0.10.0 起）**：`--watch` 现在还负责用 0.1 °C 源**累积当日峰值**
（CSV 只给最新一笔，不自带当日最高）。**峰值窗口 12:00–17:00 必须每 10 分钟跑 `--watch`**，
否则会漏掉两次轮询之间出现的峰值——09-09 的 32.2 就出现在 13:00 与 14:00 之间，
整点采样完全错过。10 分钟级漏掉的峰值约 0.2–0.3 °C，整点级实测可达 1.2 °C。

推荐节奏（HKT）：
- 08:00–12:00：每整点后 5 分跑一次全套（1 次/小时）
- **12:00–17:00（峰值窗口）：`--watch` 每 10 分钟**（cron `*/10 4-9 * * *` UTC），
  其余（概率 / 盘口）每 30 分钟一次即可——全套命令慢，没必要跟着 10 分钟跑
- 17:00 后：晴天基本锁定，可停；仅当气温仍在升或过峰守卫反复触发时再跑

它不只是记温度，而是外推今日峰值并给出到达各整数门槛的概率。

**v0.13.0 起外推改为 one-touch 口径**（结算量是日内路径最大值 = 连续监控的向上触碰，
不是数字期权）：

- 建模对象是**剩余升温 `R = 日最高 − 已观测最高`**，用 `data/remaining_rise_cdf.json`
  的**历史经验分位表**（按月×时，ERA5 4266 天）——不是正态。
  R 右偏且 0 处有原子（h=13 时 35.5% 的日子 13 点就是全天最高），
  正态会在近端高估 41%、远端低估 3.4 倍。
- **水位 L = 今日已采样时段的中位偏差**（实测 − 网格），**不回归到气候均值**。
  旧式 `站点加成 k = 气候偏差 − 当下偏差` 会在干燥晴热天硬砍 1.4°C，是持续性冷偏差。
- 今天模式自己的日变化曲线只以 **δ = ρ̂ − ρ̄_h**（限幅 ±1.0°C）进入：
  气候升水已隐含在经验分布里，NWP 只贡献增量，点估计不当确定性用。
  **δ 取代了原来手调的"已过峰值守卫"**——午后剩余升水自然收敛到 0。
- 旧式 `peak = 当前读数 + 气候表 E[M−T_h] + k` 的斜率错了 15–140 倍
  （隐含 β=−1.0，实测 −0.007~−0.222），误差在 **13 时前后反号**：
  13 时前偏冷 0.28–0.4°C，14 时后偏暖 0.3–1.0°C。

改完的实测收益（Brier，奇数年建表/偶数年检验）：平均 **−25.3%**，
h=14 −51%、h=15 −85%、h=16 **−94%**。

⚠ **分位表是 ERA5 网格口径，尾档概率是下界**：9月 h=13 的 P(剩余升温≥0.5)
网格 7.0% vs 机场站实测 32.8%（差 4.7 倍）。站点口径约为其 2–5 倍，尾档尤其如此。
输出里已打印该提示，给交易建议时必须如实转达。

**把 one-touch 存活阶梯换算成档位概率**（v0.13.8，2026-09-12 新增）——
`--watch` 打印的是**存活函数** `S(x) = P(峰值 ≥ x)`，不是档位概率。档位 `N = [N.0, N+1.0)` 的概率是
**`P(N) = S(N) − S(N+1)`**。直接读存活值当档位概率会高估每个档——这是很容易犯的错。

同日实测（2026-09-12 10:00，已观测 29.6）：

| 方法 | 30 档 | 31 档 | 32 档 | 33 档 |
|---|---:|---:|---:|---:|
| 分位表 δ 平移（网格口径，尾部下界） | 9% | **50%** | **33%** | 6% |
| 正态 mu=32.2 sd=0.70 | 4% | **34%** | **49%** | 12% |

**两者分歧很大且方向相反**：经验分布偏窄（31/32 集中的多），正态偏肥尾。
分位表尾部偏薄（×2–5 修正后 33 档 6% → 12–20%）反而更接近正态。
**做法：两个口径都算，把 31/32 档的公允值报成区间（如「31 档 34–50%」），
只在两个口径同向时才当硬结论用。** 只报单一口径会在 31/32 边界附近给出过窄的置信带。

⚠ **水位 L 有「整点对齐」缺陷，小时中间取数会虚高**（2026-09-12 发现，优先级高）。
`devs.append(p['hko'] - grid_by_h[ph])` 里的 `ph` 取的是**观测点的小时数**，`grid_by_h` 是
**整点**网格值 —— 所以 10:30 的站点实测被拿去比 10:00 的网格，多算了这半小时网格的升温。
实测（2026-09-12）：

| 站点观测时刻 | 站点 | 对齐网格 | 偏差 | 正确性 |
|---|---:|---:|---:|---|
| 09:00 | 28.7 | 26.8（09时） | **+1.90** | 对齐，正确 |
| 10:00 | 29.6 | 28.3（10时） | **+1.30** | 对齐，正确 |
| 10:30 | 30.0 | 28.3（10时） | **+1.70** | ❌ 应比 28.95（10:30 插值），真实 ≈ +1.05 |

后果：`robust_bias`（4 个样本取 `devs[len//2]`）被两条 10:30 记录抬到 **+1.70**，
点估计 → **32.60**（应为 ≈32.2），δ 被顶到限幅上限 +1.00，31 档公允被压到 34%
（按整点对齐应为 **39.5%**）。**在 31/32 边界附近，0.4°C 的水位虚高足以翻转档位判断。**

**做法（在下单前手工核对）**：只信**整点或半点与网格整点重合**的样本；非整点样本的偏差
要减去 `(观测分钟/60) × 该小时网格升温`。或直接只用整点样本算 L（样本会少，但无偏）。

- 站点加成 = `日最高偏差 − 此刻偏差`，实时计算，越接近午后峰值越准。
- **观测时间戳对齐**：`rhrread` 整点更新、实际可滞后 1 小时，脚本一律以**观测时刻**为基准
  取网格值并对时尾温度表，而非"现在"（用现在会拿上一小时的实测比对下一小时的网格）。滞后时打印提示。
- **已观测最高 ≠ 自身轮询采样的最大值**：漏点会低估当日真实最高，而它既是概率下限、
  也是下述守卫的触发条件。脚本用模式逐时值 + 站点偏差（封顶在气候值）回填未采样的小时，
  有实测的一律以实测为准；回填生效时会打印 `ⓘ 已按偏差 …回填`。
- **已过峰值守卫**：无条件气候表（"还能升多少"）把"已冲到峰值又回落"的日子也算进去了。
  当实测已从今日最高回落 ≥0.3 °C、且时点 ≥14 时（典型峰值窗口），脚本判定当日峰值大概率已定格，
  把剩余升温期望与离散度**大幅收窄**，只保留少量"晚间反弹创出新高"的可能——回落越深、越晚收得越紧。
  实测（2026-09-07 16:00，29 °C vs 已观测最高 30 °C）：加守卫前 P(31) 荒谬地给 56%，
  加守卫后回到 ~2%。这是"外推低于已实测最高"那族问题的另一面：
  早上高估是网格还没到过那么高；午后高估是**已经到过又回落了**。
- 气候表由 `diurnal.py` 生成（ERA5 2020–2025，按月 × 逐时），已缓存于 `scripts/data/diurnal_climatology.json`，开箱即用。
- **阴雨/雷暴日会显著低于外推值**——把外推值视为上限，午后下雨则以上即为上限。
- 16:00 后晴天情形下日最高温基本锁定。

### C2. 性能与缓存（v0.11.0 实测重写）

**先记住这条诊断**：单次运行的耗时**几乎全在 TCP/TLS 握手，跟数据量无关**。
实测同一主机：首次请求 **1.02s**，连接热了之后 **0.20s**；
7 模型 × 10 天（1251 B）与 1 模型 × 2 天（318 B）在热连接上都是 0.20s。
→ **砍模型数、砍 `forecast_days`、砍 payload 几乎不省时间**；
省时间只有两条路：**少握手、别重新握手**。

本机实测（v0.11.0）：

| 命令 | 耗时 |
|---|---|
| `hk_edge.py --date …`（缓存命中） | **0.39s** |
| `hk_edge.py --date … --no-cache` | 1.65s |
| `hk_edge.py --watch` | 1.56s |
| `market_prices.py … --depth 6` | 1.38s |

加速只动了传输层，**任何一个数字的计算方式都没变**（已逐位比对验证）：

| 手段 | 作用 |
|---|---|
| `requests.Session` 保活 | 同主机复用 TCP/TLS，省掉每个请求的握手 |
| 互不依赖的接口并行 | 墙钟时间 = 最慢那个请求，而不是所有请求之和 |
| CLOB 批量接口 `/books` | 22 个订单簿 1 次请求拿全（0.5s vs 逐个并行 1.4s vs 串行 5.6–7.3s） |
| 模式/预报短 TTL 磁盘缓存 | 15 分钟内重复运行直接命中（模式更新周期 ≥6h） |
| 历史 CSV 按需加载 | 校准有缓存时不解析 4.9 万行 |
| **v0.11.0 `--watch --loop`** | **一个进程内反复采集，复用同一条连接**：同一进程内连续 4 次 1.85s → **0.36 / 1.24 / 0.73s**，首次之后平均 0.6~0.8s，比每 10 分钟起新进程（每次 ~1.7s）快约 **2.5~3 倍** |
| **v0.11.0 `--sigma` 时跳过集合** | 那时概率直接用 `N(mu, sigma)`，集合成员不进入计算，省掉最慢那个请求；离散度如实显示 `n/a(--sigma)` |

**严谨性边界（不可越过）**：

- **实况（`rhrread`、网格实况）与订单簿价格永不缓存**——那是结论的输入源头，缓存会让 EV 失真。
- 缓存只作用于模式/官方预报类接口，键 = 完整 URL，参数变化自动失效。
- 命中缓存时输出会明确打印**数据年龄**（`数据时效: 缓存命中，最旧数据取自 X 分钟前`），不隐藏。
- 下单前想拿最新数据：`--no-cache`；想改缓存时长：`--ttl <秒>`（默认 900）。
- 批量订单簿失败会打印 `[warn]` 并**自动回退**到逐个并行拉取，功能不依赖它。

### C. 校准与复盘

```bash
py -3 scripts/hk_edge.py --recalibrate   # 强制重算模型偏差(默认缓存<3天自动复用)
py -3 scripts/hk_edge.py --score         # forecast_log.jsonl 的 one-touch 阶梯 Brier
py -3 scripts/review_brier.py            # decision_log.csv 的逐时点「模型 vs 市场」Brier + 上午/午后分层
py -3 scripts/review_brier.py --flicker  # 站点午后 10 分钟抖动 σ_flicker → 「距边界 d 的越线概率」表
py -3 scripts/backtest.py                # 多模型预报 vs 实测 → bias/MAE/RMSE/r + model_calib.json
py -3 scripts/hitrate.py                 # 命中率与取整口径错配分析(需先跑 backtest.py)
py -3 scripts/analyze.py                 # 站点偏差、月度气候、日际波动(需额外站 CSV，见下)
```

**`--score` 与 `review_brier.py` 的分工**（别用错）：
- `--score` → `forecast_log.jsonl`，**one-touch 阶梯**口径，回答「模型自校准做得好不好」
- `review_brier.py` → `decision_log.csv`，**逐时点模型 vs 市场**，回答「今天哪个时段该听模型、哪个时段该听市场」

> ⚠ **复盘前先确认你跑的是哪份副本。** 本技能有两份安装：`.workbuddy/skills/hk-weather-edge`
> 是**活的**（每天写 `data/`），`.codebuddy/skills/hk-weather-edge` 只是 **git 源**。
> 两者的 `scripts/data/` 是**各自独立的本地状态**，`.codebuddy` 那份会停在最后一次同步时点
> （2026-09-12 实测：`.codebuddy` 的 `decision_log.csv` 停在 09-08，149 行；活副本 457 行）。
> **跑错副本复盘会得出完全错误的结论，而且不会报任何错。**
> `review_brier.py` 已在输出顶部打印数据源路径与行数，先看那两行。

`calib` 超过 3 天会自动重算；`n < 20` 时回落到保守默认 `bias=1.17, resid_sd=0.80` 并打印提示。

## 数据 / 脚本清单

| 文件 | 作用 |
|---|---|
| `scripts/hk_edge.py` | 主程序：拉数据 → 校准 → 公允概率 / edge / Kelly / `--watch` |
| `scripts/market_prices.py` | **从 CLOB 订单簿取真实可成交价**（取价必用，见 A0） |
| `scripts/diurnal.py` | 生成「某时刻 → 当日峰值」气候升温表（ERA5，跑一次数分钟） |
| `scripts/backtest.py` | 多模型回测，产出 `model_calib.json`（**需要 pandas + numpy**） |
| `scripts/analyze.py` | 站点偏差 / 月度气候 / 日际波动（需要 pandas + numpy，且需要多站 CSV） |
| `scripts/hitrate.py` | 命中率与 `floor` 取整错配分析（需要 pandas + numpy） |
| `scripts/build_remaining_rise.py` | **重建剩余升温经验分位表**（one-touch 口径），`--validate` 附新旧 Brier 对照 |
| `scripts/review_brier.py` | **收盘复盘**：`decision_log.csv` 逐时点算「模型 vs 市场」Brier，按上午/午后分层。结算档默认读 `maxt_HKO.csv`，近期日需 `--truth DATE=N`；`--flicker` 改算 σ_flicker 与越线概率表 |
| `scripts/rec_pnl.py` | **建议盈亏台账**：读 `rec_pnl_log.csv` 出「逐日胜/负 + 累计 ROI + 按方向拆分」。`--settle DATE=N --write` 回填结算档，`--markdown` 重生 `PNL.md` |
| `scripts/data/rec_pnl_log.csv` | 每日建议腿的台账（**纳入 git**）。一条腿一行，取首推价 + 建议仓位；口径见《每日建议台账》 |
| `PNL.md` | 台账的 GitHub 可见记分牌（由 `rec_pnl.py --markdown` 生成，勿手改） |
| `scripts/data/remaining_rise_cdf.json` | 剩余升温 `R = M − 已观测最高` 的经验分位表（按月×时，ERA5 4266 天） |
| `scripts/data/forecast_log.jsonl` | 每次 `--watch` 落地的概率向量，`--score` 用它打 Brier |
| `scripts/data/obs_1min_archive.json` | 0.1°C 结算站序列**按日归档**，攒够后可重建站点口径分位表 |
| `scripts/data/diurnal_climatology.json` | 日内气候缓存，已随包提供（v0.13.0 起 watch 只用它兜底） |
| `scripts/data/http_cache.json` | 模式/预报类接口的短 TTL 缓存（实况与价格不进这里） |
| `scripts/watch_silent.py` | **零窗口轮询入口**：由 `pythonw.exe` 启动，自己开 `watch.log`、带 PID 锁。计划任务 `HKWeatherEdge-Watch` 跑的就是它 |
| `watch_silent.vbs` | 手工双击用的同一个入口（转调 `pythonw.exe watch_silent.py`） |
| `watch.bat` | 保留但**必然弹窗**（.bat 的固有属性），文件头注明改用 vbs。**必须是纯 ASCII + CRLF** |
| `stop_watch.vbs` | 停掉静默轮询（没有窗口就没有 Ctrl-C 可按）：匹配 `hk_edge.py --loop` 或 `watch_silent.py`，并清理 `.watch_loop.pid` |

计划任务现状（用 `Get-ScheduledTask HKWeatherEdge-Watch` 可查）：每天 11:00 起、每 30 分钟一次、
持续 6 小时，动作 = `pythonw.exe "…\scripts\watch_silent.py"`，`MultipleInstances=IgnoreNew`
（因任务全程保持 `Running`，此设置才有效 → 退化为看门狗）。
| `assets/docs_edge_map.html` | 完整方法论报告（含实证图表），需要时可直接在浏览器打开给用户看 |

### `analyze.py` 的额外依赖（务必先看）

`analyze.py` 读 `scripts/data/maxt_{HKO,KP,TKL,SEK,CCH}.csv`，但只有 HKO 会由主程序自动下载。其余四站需手动拉取：

```bash
py -3 scripts/fetch_stations.py     # 下载 KP(京士柏)/TKL(打鼓岭)/SEK(石岗)/CCH(长洲)
```

或直接 curl `https://data.weather.gov.hk/weatherAPI/opendata/opendata.php?dataType=CLMMAXT&station=KP&lang=en`。

## 输出纪律：每次结论末尾必须给「下次刷新时间」

**这是硬要求，不是可选项。** 概率的输入源各自有更新节奏，结论只在下一批新数据落地前有效。
「算完就忘」是本工具最大的实操漏洞 —— 09-09 那天就是 13:00 到 14:00 之间漏掉 +1.2 °C 的峰值，
直接把 31 档算成 66% 而它其实已经归零。

给用户的每一条结论（包括聊天里的口头结论、不只是脚本输出）**最后一行必须是**：

```
下次刷新：MM-DD HH:MM HKT（Xh YYm 后）—— 触发原因
```

脚本已自动输出（`print_next_refresh()`，`hk_edge.py` 与 `--watch` 两条路径都接了）。
手动给结论时按下面的锚点自己算：

| 锚点 | HKT 时刻 | 说明 |
|---|---|---|
| NWP 多模式新一轮落地 | **02:00 / 08:00 / 14:00 / 20:00** | 决定点估计，**最重要**；00/06/12/18 UTC 运行 + ~2h 落地 |
| HKO 九天预报 `fnd` | 约每 2 小时（:30 前后） | 官方口径；**措辞变化优先于数字** |
| HKO 整点实况 `rhrread` | 每整点 +5 分 | 仅当日 |
| 0.1 °C 分区气温 CSV | 峰值窗口 12:00–17:00 内**每 10 分钟** | 仅当日；`--watch` 必须跟上 |
| CLOB 订单簿 | 实时 | 随时可跑 `market_prices.py`，不构成"刷新截止" |

取**最近的那个**未到时刻。当日且在峰值窗口内 → 取 10 分钟点；否则取 NWP / fnd。
已过目标日 20:00 HKT（= 12:00 UTC 收盘）→ 输出「已收盘，不再重跑」，改为提醒回填 `decision_log.csv`。

## 交易纪律（对用户输出建议时必须带上）

- **|EV| > 8% 才动手**——0.79 °C 的 RMSE 意味着概率本身有 ±5% 量级误差。脚本内部 `MIN_EDGE=0.08` 已硬编码。
- **EV 必须用订单簿可执行价算**（见 A0），用最后成交价算出的 edge 是假的。
- **挂限价单做 maker**，别吃单，taker 费用会吃掉薄盘的 edge。
- **跨 2–3 个相邻档 ladder**，收割分布形状而不是猜单点。
- **只要判定有 edge（|EV| > 8%），就必须给出 Kelly 仓位建议**——不能只说"买 YES"就收尾。
  买 YES `f* = (p−m)/(1−m)`，买 NO `f* = (m−p)/m`，**实操一律 35% Kelly**
  （脚本常量 `KELLY_FRAC = 0.35`；满 Kelly 波动过大，且概率本身有 ±5% 量级误差）。
  用户未给本金时**默认按 1000 USDC 估算，并明确标注这是假设**（脚本同样行为，`--bankroll` 覆盖）。
  **输出必须是三件套：金额 + 建议份数 + 占本金百分比**——只给金额等于没给，下单界面填的是份数。
  份数 = 金额 ÷ 该档单价（买 YES 用市场价 `m`，买 NO 用 `1−m`）；最后用**订单簿可执行价**复核一遍，
  单价差 1 分钱份数就差不少。
- **深度不足 ≠ 不报仓位**（Eason 2026-09-10 明确要求）。
  **只要判定有 edge，就必须照常给出 Kelly 三件套（金额 / 份数 / 占本金%）——不要因为 ask 侧只有几份就省略，
  更不要写"可选、很小"这种含糊话。** 用户可以挂 maker 限价单等着成交。
  深度只影响**执行方式**，不影响**该买多少**：
  - 深度足够 → 吃单（taker），按 ask 成交
  - 深度不足 → 报同样的份数，但改成「挂 XX 价限价等成交」，限价取 bid/ask 之间或略优于 bid
  - 需要另注明的只是「可能不会全额成交 / 需要等多久」，**不是把仓位数字藏起来**
  反例（勿再犯）：「YES33 @0.110–0.120，EV +22~33%，但 ask 深度只有 5 份」——这里缺的正是 151 份 / $18。
- **Kelly 是独立头寸口径，组合里已有同向腿时要打折**（经验系数）：
  与现有持仓在同一结算档重叠 → **打 7–8 折**；反向对冲腿 → **不打折**。
  独立 Kelly 会高估，因为没算相关性。例：已持 NO31 时再加 YES33（33 档重叠），151 份 → 取 120 份。
- **薄盘时仍按 Kelly 报数**，只在括号里注明「ask 深度 N 份，建议挂限价」。
- **别碰 $0.05 以下的档**——EV 看着几百个百分点，但没有挂单，是流动性幻觉。
- **报浮盈必须同时报可成交量**（2026-09-10 实测）。薄盘里「浮盈 +20%」常常只能兑现 20 份：
  32 YES 曾 bid 0.260 而两侧深度都只有 **20 份**，150 份的仓位大部分平不掉。
  **浮盈是可报价，可成交量才是可兑现** —— 这两个数必须成对给出，否则等于误导。
- **EV 很负 = 止盈信号，必须主动提醒**。某方向「现在买入」的 EV ≤ `--exit-ev`（默认 **−15%**）时，
  脚本会在该档打 `⚠持YES止盈-XX%` / `⚠持NO止盈-XX%`；含义不是"该反向开仓"，而是
  **市价已明显高于公允值，还拿着这个方向的人应该平仓，而不是持有到期**。
  用户给了 `--hold` 时，持仓检查里会直接标「⚠ 止盈（现价高出公允 XX%）」并给出浮盈。
  每次复跑发现用户的已用头寸触发这个信号，**必须在结论里点名提醒**。
- **单档概率有硬上限，用它可以做不依赖 mu 的止盈判断**（2026-09-10 实测，最强的一条）。
  1 °C 宽的档位，概率上限 = `2Φ(0.5/sd) − 1`，在 **mu 恰好落在档位中点** 时取到。
  sd 0.89（本模型典型值）→ 上限 **43%**；sd 0.76 → 49%。
  → **只要某档 YES 的 bid 超过这个上限，无论真实 mu 是多少，该价都必然高估，无条件卖出。**
  实测 9/10：32 YES bid 一度到 **0.46–0.48 > 43% 硬顶**，而模型(31.14)、官方(32.0)、市场(32.0)
  三个锚点互相打架。靠锚点吵不出结论时，硬上限直接给出答案 —— 卖。
  用前先确认当日 sd（`--date` 输出的「残差sd」）。
- **实况已逼近档位边界时，不要逆着 ≥90% 的市场价下注**（2026-09-11 实证，详见「已知局限」）。
  判别只用一个数：**当前已观测最高 距下一个整数档的距离**。
  - **< 0.3 °C 且时间 ≤ 15:30** → 市场定价优先，模型只作参考；宁可空仓，不反向下注。
  - **≥ 0.6 °C** → 模型可以领先市场，按正常 edge 流程走。
  9/11 的 NO32 @0.040 就是踩了这条：13:30 距 32.0 只剩 0.2 °C，我按分位表的 25% 反着市场做，20 分钟后被证伪。
- **边界日的赔率结构要看清**：当某档被市场定到 >95%，其余档的价格会被挤到 0.002–0.04 这种「看似百倍赔率」的位置。
  这些价位的名义 EV 动辄几千个百分点，但**深度与真实性都不可靠**（31 档 0.003、NO32 0.040 皆属此类）。
  按 Kelly 报数、注明深度，但**不要把它们当主仓** —— 这是 9/11 唯一真正付了学费的地方。
- **止盈对比必须用「总财富」口径，不能拿剩余成本比剩余期望**（2026-09-10 实测踩坑）。
  判断「卖不卖」时的正确式子：
  `卖出所得现金 + 剩余头寸的期望派息`  vs  `原头寸的期望派息`（同一成本基准下比较）。
  错误做法是把已卖出部分的成本从分母里剔掉、却又忘了把卖出所得加回来 ——
  这会得出「卖光反而更差」的反向结论。实测 9/10：按错误口径算「留 29 份 YES32」是 −1%、
  「全卖」是 −9%；按正确口径重算是 **−3.9%(冷) / +4.9%(中) / +23.7%(暖) 全卖都占优**。
  结论：**只要卖出价 > 公允价，多卖一份就多赚一份，永远不要为了"留点敞口"而少卖**。
- **记录每笔「你的概率 vs 市场价 vs 结果」**，攒够 30 笔回头校准自己的命中率。

## 已知局限（输出结论时如实说明）

- ✅ **雨日必须对站点过热做折减：用「降水量分档的站点−网格偏差」定标**（2026-09-11 新增，推荐）
  校准常数 `bias` 1.01 是**干湿混合值**；而站点相对网格的过热幅度强烈依赖当天是否下雨。
  一条命令取 92 天（past_days 上限）逐日 `temperature_2m_max` + `precipitation_sum`，
  与 `scripts/data/maxt_HKO.csv` 按日期配对，再按降水量分档统计偏差：

  ```
  api.open-meteo.com/v1/forecast?latitude=22.302&longitude=114.174&daily=temperature_2m_max,precipitation_sum&past_days=92&forecast_days=1&timezone=Asia%2FShanghai&models=best_match
  ```

  2026-06~08 实测（n=54，注意 maxt_HKO.csv 有约 10 天滞后，配对会截至上月末）：

  | 分档 | n | Δ 均值 | Δ 中位 | 站点最高均值 | P(≥32) |
  |---|---:|---:|---:|---:|---:|
  | 干日 ≤1mm | 11 | +0.82 | **+1.30** | 34.26 | 91% |
  | 雨日 >1mm | 43 | +0.93 | **+0.70** | 30.90 | 37% |
  | 大雨 >10mm | 24 | +0.73 | **+0.65** | 29.74 | 8% |

  （干日样本全落在盛夏热浪，均值 34.26 偏高，勿外推到 9 月。）
  → **做法**：预报措辞出现 showers / rainstorm / thunderstorms 时，用 **Δ ≈ +0.7** 而非 `bias` 1.01；
  连晴干燥日反过来要**上修**（见上条，实测 +1.5~+2.5）。两者可差 1.5 °C，往往就是相邻两个整数档的全部差别。
  ⚠ 这是「条件化 delta」，与无条件 `bias` 不矛盾，但**样本量小（雨日 43 天、干日仅 11 天）**，
  只用来修正中枢、不要用来改分布宽度（sd 仍用 `--date` 给出的值）。
- ⚠ **偏差校正后的模型在「连晴干燥日」系统性偏冷**（2026-09-09 实测，优先级高）。
  当日模型 mu 30.65，结算站实测 **32.2**，误差 **−1.55 °C ≈ 2 sd**（sd 0.76）。
  成因：HKO 总部站（尖沙咀市区）在干燥晴天过热，而 NWP 网格含水面/混合下垫面吃不到这部分。
  → **预报措辞出现「Dry during the day / Sunny periods / Mainly fine」时，bias 常数不够用，
  必须再叠加 regime 修正。** 实用做法：若次日措辞与今日一致，
  直接把「今日实测 − 今日模型 mu」作为次日 mu 的额外上调量。
  9/10 即按此把中枢从模型的 31.15 上调到官方的 32.0，事后看市场也是这么走的。
- ⚠ **nowcast 的「站点偏差回归长期均值」是持续性冷偏差来源**（2026-09-10 实测，优先级最高）。
  脚本默认把当前站点偏差向长期均值（约 +1.01 °C）回归，作为「站点加成」（当日实测 −1.09、−1.39）。
  但**干燥晴天时站点偏差不回归**：9/10 12:20 实测偏差仍是 **+2.40**，而脚本仍按 −1.39 折减，
  直接把峰值估计压到 30.54 而被下限顶回 31.4（等于完全不预测升温）。
  **后果**：我 9/10 的 P32 估计三小时内被市场连续上修打脸 —— 22%→25%→32%→**58%**，
  同期市场 55%→63%→**74.5%**，市场全程更接近真相。
  → **「Dry during the day / Sunny periods」日，站点加成应大幅衰减（0 到 −0.5，不要用脚本默认值）**，
  或直接用「网格峰值 + 昨日实测偏差」的类比法交叉验证。
  ⚠ **补充（2026-09-10 实测）：回测 bias 常数 1.01 在干燥晴天只覆盖了一半的缺口。**
  拿 Open-Meteo 七模式集合均值对结算站实测（尖沙咀）逐日比对：
  9/09 模式均值 30.06 → 实测 32.2（**+2.14**）；9/10 模式均值 30.13 → 实测 31.8（**+1.67**）。
  即 `bias=1.01` 仍留 0.7–1.1 °C 的冷偏差。**样本仅 2 天，勿当定论**，
  但可作为「上档情景」的交叉验证：干燥晴天把「模式集合均值 + 1.7」当 mu 的上沿，
  「+ 1.0」当下沿，两者相差的 0.7 °C 往往就是相邻两个整数档的全部差别。
- ⚠ **档位边界附近：分位表对「再升 0.2 °C」这类近端门槛同样严重低估，此时市场 ≥90% 的定价反而是对的**
  （2026-09-11 实测，优先级最高）。
  13:30 站点已 **31.8**（距 32.0 仅 0.2 °C），h=13 分位表给 `P(剩余升温 ≥ 0.2) = 25%`，
  `--watch` 综合给 24%，我据此判定「市场 97% 明显高估」，建议 **买 NO32 @0.040**。
  **13:50 站点即达 32.0 °C —— 20 分钟后被证伪，该笔直接归零。**
  市场同期把 32 档从 33.5%(10:07) → 60% → 74.5% → **97%(13:50)**，全程比我准。
  **成因**：分位表是 ERA5 网格口径，网格午后还有 +0.5 °C 的增温时，
  站点按放大系数能走 1.5–2 倍；「已观测最高」到「下一整数档」这段近端距离，
  恰是网格-站点放大效应最强、而分位表最失真的区间（此前只标注了尾部偏差，漏了近端）。
  → **新纪律：实况已逼近档位边界（距下一档 < 0.3 °C）且仍在当日升温窗口（≤ 15:30）内时，
  「市场 ≥ 90%」应视为强证据，禁止仅凭分位表反向下注。**
  与上面「无新数据落地时不许改模型」不冲突——那条针对「模型领先、市场无新信息」（9/10 情形），
  本条针对「实况已到边界、市场有实况支撑」（9/11 情形）。
  **判别钩子（两问，缺一不可）**：
  1. **离下一个整数档还有几度？** ≥ 0.6 °C 才有下注空间；< 0.3 °C 直接跟随市场。
  2. **网格的日变化曲线见顶了没有？** 见顶后当日不再有升温空间，无论距离多远都不下注。
  ⚠ 最初只写了第 1 问，9/11 14:10 就栽在这上面（见下条），故必须两问同时满足才可下注。

- ⚠⚠ **「Δ 累计值」不含时间信息，不能用来外推峰值（2026-09-11 实测，第二次同型失误）**
  14:00 站点 32.4（距 33.0 差 0.6 °C），我用「网格峰值 31.0 + Δ 类比 2.2（9/10 口径）」
  外推出 32.9–33.2，判定 33 档有 20% 概率，建议 **买 NO32 @0.140**（等价赌上 33）。
  **14:20 站点仍 32.4、走平 20 分钟，14:35 NO32 bid 只剩 0.070（浮亏 −50%）。**
  **成因**：站点相对网格的过热 Δ 逐日递减（9/9 **+2.5** → 9/10 **+2.2** → 9/11 **+1.4**），
  Δ 是**累计量**、不是速率；把它当常数外推，等于假设「网格会继续升」，而这恰恰是错的。
  **决定性事实**：三天网格日变化曲线**全部在 14 时见顶**（9/9 29.7@14、9/10 29.6@14、9/11 31.0@14），
  15 时起一律回落 0.3–1.2 °C。9/10 站点峰值 31.8 出现在 **13:20**（网格见顶前 40 分钟）。
  → **新纪律：判断当日峰值是否已过，只看「网格日变化曲线的顶点位置」，不看 Δ 累计值。
  网格 14 时见顶后，站点同日不再创新高——此时任何「再上一个整数档」的下注一律作废。**
  复现（一次请求拿逐时曲线）：
  `api.open-meteo.com/v1/forecast?latitude=22.302&longitude=114.174&hourly=temperature_2m&past_days=2&forecast_days=1&timezone=Asia%2FShanghai&models=best_match`

- ⚠ **峰值窗口内不出建议（2026-09-11；当日收盘量化后窗口已扩大）**：9/11 全天三次建议，
  两次错（13:50 的 NO32、14:10 的 NO32），一次盘中被市场反超（11:05 的 NO31，**但持有至结算为赢单**，
  +85% ROI——勿再误记为「被追平」）。同期市场把 33 档从 33.5% 推到 97% 再砸回 6.7%，
  节奏全程比模型准。**结论：模型对「1 小时内、0.2–0.6 °C 尺度」的近端变化没有预测力。**
  → **窗口已扩大为「整个下午（≥12:00）+ 提前一天」，正面出仓窗口收敛到 09:00–11:30。
  完整依据（分桶 Brier、a\* 权重、Kelly 敏感性、单日敞口上限）见「出仓时段与仓位上限」章节。**
  复现命令（一次请求拿七模式 × 逐日）：
  `api.open-meteo.com/v1/forecast?latitude=22.302&longitude=114.174&daily=temperature_2m_max&past_days=2&forecast_days=4&timezone=Asia%2FShanghai&models=ecmwf_ifs025,gfs_seamless,icon_seamless,gem_seamless,metno_seamless,jma_seamless,ukmo_seamless`
  ⚠ **模式对「绝对水平」不可信，但对「日际变化」通常可信**：9/11 七模式均值 31.01 比
  9/10 的 30.13 高 0.88 °C，而 HKO 官方预报反而报 31（较 9/10 的 32 低 1 °C）——
  两者直接打架。这类分歧无解时，**取中间值并把仓位压到保守情景的 Kelly**。
- ⚠ **`--watch` 的概率输出与它自己的点估计口径不一致（2026-09-11 发现，待修）**。
  09:10 实测 27.4 时 `--watch` 给**点估计 32.20**（= 网格日最高 30.7 + 水位 L 1.50），
  但同一屏的概率输出说「到 30.0°C 概率约 60%」（即 P(今日最高 ≥ 30.0) = 60%）——
  两者差 2.2 °C，不可能同时成立。
  **根源是 δ 的限幅**：ρ̂ = 4.80、ρ̄ = 1.92 → δ 应为 **+2.88**，被
  `max(-1.0, min(1.0, …))` 截到 **+1.00**，一次丢掉 1.88 °C；而概率**完全**由 δ 位移后的
  分位表决定（点估计 `pred` 只用于打印，不进分布，见 `watch()`）。
  **限幅本意是「NWP 日变化不可全信」，但在模式高度一致时它是纯损失** ——
  当日七模式集合离散度仅 **±0.33 °C**，模式之间已经没有分歧可按。
  → **不改代码的对策**：集合离散度 < 0.4 °C 时，把 `--watch` 的概率当**严格下界**，
  改用 `--date --observed-max <实测> --mu <模式情景> --sigma 0.9` 的口径；
  或手工按「δ 缺口」把分位表概率上移。
  **彻底修复要把限幅改成随离散度自适应，并重跑 Brier 验证——验证之前不要动代码**
  （v0.13.0 的 −25% Brier 改进依赖当前实现）。
- ✅ **尾档交叉验证：直接用结算站自己的百年序列**（2026-09-11 新增，推荐）
  怀疑某个尾档被市场低估时，先算**无条件气候基线**——它不依赖任何模式、任何校准：
  `scripts/data/maxt_HKO.csv` 就是天文台站 1884 年至今的日最高温（0.1 °C），
  取「同月 + 目标日期 ±3 天」窗口即可。实测（9 月 8–14 日窗口）：

  | 样本 | 均值 | P(≥31) | P(≥32) | P(≥33) |
  |---|---:|---:|---:|---:|
  | 1991–2025, n=245 | 30.96 | 55.5% | 31.0% | 14.3% |
  | 2016–2025, n=70 | 31.68 | 64.3% | 57.1% | 35.7% |

  **近 10 年比 30 年均值高 0.7 °C，尾档差 2.5 倍——定尾档必须用近期窗口。**
  用法：条件预报（模式 / 官方）给中心，无条件基线给**上包络**；
  市场价若不到近期基线的 1/2，通常即为错价
  （2026-09-11 市场给 33 档 3.5%，近 10 年基线 35.7%，链路公允 12%，三者呈 10 倍梯度）。
  ⚠ **但不要因此跟随市场日内动量（2026-09-10 收盘后复盘推翻）**。当日原写的是
  「若自己的估计连续 3 次被市场同向修正，视为自己有系统性偏差，向市场收敛一半」——
  **事后看这条是错的，会造成从正确位置漂移**。9/10 实证：市场 09:31 给 P32 44%、
  11:17 推到 61%、收盘前 74.5%，**实测 31.8 → P32 = 0**；模型 09:31 给的 P32 14.5%
  才接近真相。同期我按此纪律把 P32 从 22% 上调到 58%，同样错。
  → 修正后的纪律：**模型与市场分歧、且模型输入（NWP / 实况）没有新数据落地时，不许改模型。
  市场日内单边移动是叙事驱动，不是信息驱动。** 只在有新数据时更新先验。
  ✅ **v0.13.0 已修**：水位 L 改取「今日已采样时段的中位偏差」，不再回归气候均值、不再封顶。
- ⚠ **剩余升温分位表是 ERA5 网格口径，尾档概率是下界**（v0.13.0，优先级高）。
  9月 h=13 的 P(剩余升温≥0.5)：网格 **7.0%** vs 机场站实测 **32.8%**，差 **4.7 倍**。
  成因：25km 网格的日变化比单点站平滑，日内尖峰被抹掉。
  → **给尾档（需再升 ≥0.5°C）的建议时，把模型概率当**下界**，站点口径约为其 2–5 倍。**
  彻底解决要等 `obs_1min_archive.json` 攒够结算站自己的 10 分钟级序列后重建分位表。
- **注意 P(N 档) 不随 mu 单调**：它是 `mu` 的"帐篷函数"，在档位中点附近见顶后随 mu 继续上升而**下降**。
  9/10 实测 P(31 档)：mu 31.15→39.4%、31.5→**42.6%（顶）**、31.8→40.8%、32.0→37.4%。
  → **持有 NO 31 是「越暖越安全」的腿**（公允 57.4%→62.6%），直觉上很容易搞反。
  判断 NO 头寸时要先看 mu 在档位的哪一侧，不能只说"天气变热了所以要平 NO"。
- **当日午后 NWP 的日最高值会失真**——这是实战中最容易踩的坑。模式给的 `temperature_2m_max`
  是整天最大值，一旦午后发生雷暴/强对流，它不会实时下修（实测：站点 14:00 已降到 29 °C 且在下雨，
  模式仍给当日 31.7 °C）。因此**提前 0 天时必须以 `--watch` 的实况外推为准，并用 `--mu` 覆盖点估计**；
  `--observed-max` 只修正分布下限，修正不了失真的均值。
- 回测窗口只有 92 天（Open-Meteo previous-runs 上限）且偏夏季，冬季表现需自行积累数据。
- 站点偏差是季节平均，单日可能偏离。
- 日内外推的「站点加成」基于单时点实测对比，有噪声。
- ⚠️ **日内估计对「站点偏差」这个输入高度敏感**（2026-09-08 同一快照实测）：偏差取观测时刻单点
  +1.70 °C → P(≥33)=22%；取全天稳健均值 +0.73 °C → 68%。同一份数据、同一个模型，
  结论差三倍。当前实现的处理是：站点加成沿用单点，回填封顶在气候值（保守）。
  **因此 `--watch` 的午后概率只能当方向，不能当报价**——把它当定量结论下单前，先看市场价差多少倍；
  差 10 倍以上通常说明你手里那个数不够硬（需要逐时站点历史才能给偏差定标）。
- 台风 / 暴雨等非线性事件下官方预报会跳变，模型与外推同时失效。
- 事前概率随提前期自动变宽：提前 6 天很散，当天下午很尖。别用同一套仓位标准对待不同提前期。

## 出仓时段与仓位上限（2026-09-11 量化，优先级最高）

用 `scripts/data/decision_log.csv`（440 行快照，覆盖 9/07–9/12）中已结算的 9/08–9/11 四天
（实测峰值 32.0 / 32.2 / 31.8 / 32.5），按快照相对目标日的时点分桶，对比模型与市场的 Brier：

| 时段 | 快照数 | 模型 Brier | 市场 Brier | 差 | 模型胜率 | a\* |
|---|---:|---:|---:|---:|---:|---:|
| 提前一天 D-1 | 14 | 0.6112 | **0.5914** | +0.0198 | 29% | **−0.189** |
| 当天上午 <12 | 22 | **0.6430** | 0.6768 | −0.0338 | 68% | **0.695** |
| 当天下午 ≥12 | 9 | 0.2496 | **0.1412** | +0.1084 | 22% | **−0.053** |

a\* 是「模型概率的最优权重」，用最小二乘最小化 Brier 估出，`p* = a*·模型 + (1−a*)·市场`，
闭式解 `a* = Σd(y − p_mkt) / Σd²`，`d = p模型 − p市场`。a\*=1 全信模型，=0 全信市场，**<0 表示模型是负信息**。

**结论：edge 只在当天上午存在。提前一天与下午两侧的 a\* 都 ≤ 0。**

### 1. 出仓窗口收敛到 09:00–11:30，重心 10:30–11:30

按小时细分（先按「天 × 小时」聚合再跨天平均，避免单日多快照压过其他日子；
比较用「模型 − 市场」的 Brier **差**，绝对 Brier 随临近峰值自然变小、不可直接比）：

| D0 小时 | 天数 | 模型−市场 Brier | 模型胜天率 | a\* |
|---|---:|---:|---:|---:|
| 08 | 1 | +0.0504 | 0% | −1.20 |
| 09 | 3 | −0.0065 | 67% | 1.11 |
| 10 | 4 | −0.0006 | 50% | 0.82 |
| **11** | **4** | −0.0011 | **75%** | **1.37** |
| 13 | 2 | +0.0079 | 50% | 1.90 |
| **14** | **3** | **+0.0520** | **0%** | −0.13 |

- **11 时最优**：4 天里 3 天是全上午表现最好的小时；剔除 9/09（模型大败日）后 a\* = **2.11**。
  逐日（模型−市场）：9/08 −0.0144｜9/09 +0.1581｜9/10 −0.1053｜9/11 −0.0427。
- **08 时最差**（实况样本不足 2 小时、网格相位误差最大），**14 时次差**（网格已见顶，胜天率 0%）。
- 机制（可复用）：11 时同时满足两个条件——**实况已积累 2–3 小时升温速率**（信息足够）
  且**网格日变化曲线尚未见顶**（14 时才见顶，外推前提仍成立）。13 时后两个条件同时失效。
- 汇总：09–11 时合计胜天率 **7/11 = 64%**；13–14 时合计 **1/5 = 20%**。

→ **执行：只在该窗口内出仓位建议；13:00 之后与「提前一天」一律只做实况播报，不给仓位。**
（13 时那一行的 a\* 看似最高，但只有 2 天样本且 Brier 差为正——**2 天样本下的 a\* 不可采信**。）

### 2. 概率必须按 a\* 打折后再算 EV / Kelly

**不能拿原始模型概率直接算 EV / Kelly**——上午改用 `p* = 0.70×模型 + 0.30×市场`。
名义 EV 与实际期望增长的裂口极大：上午名义 EV 平均 **+131%**，按 a\*=0.5 算的期望对数增长只有 **+1.5%/腿**；
下午名义 EV **+446%**，实际为**负**。**名义 EV 不可作为加仓依据。**

### 3. 不要提高 Kelly 系数——它事实上已经很高

期望对数增长 g(k)（真值取模型/市场 50-50 混合，下注额由模型概率定，扣 1 分价差摩擦）：

| 时段 | k=0.35（现状） | k=0.5 | k=0.75 | k=1.0 |
|---|---:|---:|---:|---:|
| 上午 | +1.50% | **+1.56%** | +1.31% | +0.73% |
| 下午 | **+3.78%** | +0.95% | −7.57% | **−25.4%** |
| 提前一天 | +0.14% | +0.14% | +0.08% | −0.03% |

上午最优 k ≈ 0.35–0.5，两者只差 0.006 个百分点；**下午 k ≥ 0.5 即转负**，k=1.0 时每腿亏 25%。
若假设市场全对（a=0），上午 k 从 0.35 提到 1.0，每腿从 −0.93% 恶化到 −3.99%（亏损扩大 4.3 倍）。
**→ 保持 35% Kelly 不动。**

### 4. 单日总敞口硬上限 25–30% 本金

35% Kelly 是**单笔独立头寸**口径；逐腿相加后实际敞口远超直觉：

| 日期 | 累计建议敞口 | /本金 | 实际结果 |
|---|---:|---:|---|
| 9/09 | $644 | **55%** | 浮亏一度 −28.5%，全天仅 +2%（当日 a\* = −0.67，模型负信息） |
| 9/10 | 单批 10:10 即 $472 | **40%** | 日志另记「主题已 22% 敞口」 |

→ **单日所有腿的累计建议金额 ≤ 本金 25–30%（硬顶）**。超限时按 a\* 排序砍掉低权重时段的腿，
不做等比例缩水。**9/09 若系数翻倍，浮亏会从 −28.5% 放大到约 −50%。**

### 5. 样本限制（如实说明）

日内多个快照高度自相关（一天 8 个快照 = 1 个独立事件），**有效样本只有 4 天**。
逐日估的上午 a\* = **1.40 / −0.67 / 1.11 / 1.32** —— 四天里一天翻转符号，
9/09 一天的反向幅度几乎抵消其余三天优势总和。二项单尾检验 **p ≈ 0.31，统计上不显著**。
方向可信，但**不要当「已验证规律」用**；攒够 15–20 天再重估。

**两条独立路径交叉验证**：自上而下（NWP 集合）与自下而上（`--watch` 实况外推）同时给峰值估计。两者一致时信心最高；分歧说明模型没吃进最新实况——此时应下调仓位而不是挑一边下注。

## 复盘的硬证据：模型的 Brier 随一天推进而衰减（2026-09-10 ~ 09-12 实测）

**工具：`python scripts/review_brier.py --truth 2026-09-11=32`**
（`--truth` 可省略，脚本自动从 `maxt_HKO.csv` 读结算档；该源滞后约 10 天，近期日需手填。
`--json` 供程序消费。**不要用 `--score` 做这件事**——它打的是 `forecast_log.jsonl` 的 one-touch 阶梯口径，
回答不了「模型和市场谁更准、在一天的哪个时段更准」。）

`data/decision_log.csv` 逐时点成对记录「模型公允 P vs 市场 P」，与结算档比对即得逐时点 Brier。
**口径提醒**：分母只取 decision_log 实际记录的 5–8 个档位（不是全部 11–12 档），所以绝对值偏高；
同口径下模型 vs 市场的**差值**才有意义。

| 日期 | 结算 | 上午（<12时） 模型 / 市场 | 午后（≥12时） 模型 / 市场 |
|---|---|---|---|
| 9/10 | 31°C | **0.1038** / 0.1411 → 模型领先 **26.4%** | （无当日午后记录） |
| 9/11 | 32°C | **0.0917** / 0.1215 → 模型领先 **24.5%** | 0.0543 / **0.0016** → 市场领先 **97.1%** |
| 9/12 | **33°C** | **0.1927** / 0.2357 → 模型领先 **18.3%** | （无当日午后记录） |

9/11 午后三点，市场已把 32 档定到 0.997 → Brier **0.0004**，接近完美；同期模型还给 31 档 37%。
**这是「出仓窗口收敛到 09:00–11:30」的第一条直接量化证据**（此前只有逐日 a\* 的间接证据，且 p≈0.31 不显著）。

### ⚠ 结算档错了，整段归因会翻转（9/12 实测，v0.15.2 更正）

9/12 首次复盘时按 **32°C** 结算（14:57 盘中快照 32.6），得出的结论是
「上午 **市场领先 29.9%**，原因是官方隔夜把预报 30→32°C、市场 07:50 就重定价而模型 09:18 还停在 0.40」。
结算档更正为 **33°C** 后，同一份 `decision_log.csv` 重跑：

| 结算档 | 模型 Brier | 市场 Brier | 结论 |
|---:|---:|---:|---|
| 32°C（错） | 0.0792 | **0.0556** | 市场领先 29.9% → 「模型有纳入时滞」 |
| **33°C（正）** | **0.1927** | 0.2357 | **模型领先 18.3%** |

**一个档位之差，把领先方整个对调。** 那条「官方修订纳入时滞」的推论因此**作废**
（观察本身还成立：市场确实先重定价到 0.59，只是它和模型一样把众数押在 32，两家都错了）。
→ **纪律：结算源（HKO Daily Extract）滞后 2–3 天，任何依赖结算档的统计结论在官方数据落地前
只能标 provisional，不得写进本文件当永久结论。**（本文件历史版本就是踩了这条。）

**→ 合并结论（跨 3 日，n=3，弱证据）：**
- **上午：模型连续 3/3 天相对领先**（+26.4% / +24.5% / +18.3%）。但 9/12 是「两家都错、模型错得少」
  （Brier 双双 0.19–0.24，众数都押 32）——**不要把「相对领先」读成「按模型定价就对」**。
- **午后：市场占优，且优势量级远大于上午**（9/11 达 97.1%）。
- **适用条件收紧为「距下一整数档 ≥1.0°C」**（原为 0.6，被 9/12 证伪，见下节 σ_flicker）。
  条件不满足即停止出建议 —— 近端该听抖动模型，不该听分位表也不该听模型 center。

### 日志缺口：9/12 午后一条成对记录都没有

decision_log.csv 的 9/12 只有 3 个当日时点（09:18 / 09:19 / 10:11），**午后 0 条** ——
而当天最贵的几笔（13:50、14:10、15:30）全在午后。`decision_log` 只在 `hk_edge.py` 带 `--market`
运行时落盘；当天午后是单独跑 `market_prices.py` 看盘，**没留下 fair_p/market_p 的配对**，于是无法打分。
→ **纪律：每次出建议前都必须用 `--market "33:0.998"` 跑一次 `hk_edge.py`**（不是只跑 market_prices），
否则当天最关键的决策时点在复盘里就是黑的。

### 9/11 的幸存者偏差：别把「mu 猜对」当成能力

9/11 全天中心估计 **mu = 32.0**，结算 32°C，看起来命中。但实际快照最高只有 **32.5**，
32 档区间是 [32.0, 33.0) —— 落在档内**靠的是 +0.5 的余量**。
若当日快照最高为 31.9（结算 31°C），则同一天里 **NO31 全系列赢单变输单、YES32 赢单变输单**，
即「mu 误差 0.1°C」会翻转当天全部 5 笔结论。
→ **在 31/32 这样的整数边界日，不要用「点估计命中」自我评价；要点估计的误差带是否小于到边界的距离。**

### 9/11 的两次反向操作（同一天内 45 分钟内互相矛盾）

| 时刻 | 建议 | 押注方向 | 结果 |
|---|---|---|---|
| 13:50 | 买 NO32 @0.040（$28） | 「不是 32 档」（≈押 31） | **−100%** |
| 14:10 / 14:15 | 买 NO32 @0.180 / @0.140 | 「高于 32 档」（≥33） | **−100%** |

两笔方向**完全相反**，间隔 20–45 分钟，**全输**。这不是方向判断问题，
是**峰值窗口内被迫表态**：信息没有增加，仓位却在两个相反方向各开一次。
→ 呼应《峰值窗口内不要钉档位》：**判「已定格」需连续 2–3 个同向点；未达此门槛时，默认空仓。**

## 漏算的那一项：站点的 10 分钟级抖动 σ_flicker ≈ 0.33 °C（2026-09-12 定量）

**起因**：9/12 13:44，已观测最高 31.9（距 32.0 只差 **0.1**），市场把 31 档打到 `0.0015`（P≈99.85%），
而我们按分位表给 **9%**。谁对？

**答案：市场对。漏算的不是物理，是「分辨率」——分位表基于 ERA5 *网格*，
网格是 9–31 km 的空间平均，单点的局地抖动（阵风、云影、建筑热羽）在平均中被抹平。**

### 实测：午后 10 分钟相邻差分（`review_brier.py --flicker`）
| 日期 | σ_flicker | n（差分） | 最大单步 |
|---|---:|---:|---:|
| 9/12 | **0.407** | 25 | **0.9**（13:40→13:50 的 31.7→32.6；15:20→15:30 的 32.2→33.1） |
| 9/10 | 0.303 | 5 | 0.4 |
| **合并** | **0.355** | **30** | — |

→ 按 N(0, 0.355)，**「剩余窗口内至少越线一次」** 的概率：

| 距下一档 d | 单步 | 剩 6 步 | 剩 10 步 | 判读 |
|---:|---:|---:|---:|---|
| **0.1** | 38.9% | 94.8% | **99.3%** | ★ 抖动主导 |
| **0.2** | 28.7% | 86.8% | **96.6%** | ★ 抖动主导 |
| **0.3** | 19.9% | 73.6% | **89.2%** | ★ 抖动主导 |
| **0.4** | 13.0% | 56.7% | **75.2%** | ★ 抖动主导 ← v0.15.2 新划入 |
| 0.5 | 8.0% | 39.3% | 56.4% | 过渡区：取 max |
| 0.6 | 4.6% | 24.5% | 37.3% | 过渡区：取 max |
| 0.8 | 1.2% | 7.1% | 11.5% | 分位表可用 |
| 1.0 | 0.2% | 1.5% | 2.4% | 分位表可用 |

### 四个口径同场对照

| 场景 | 距 d | 分位表（网格） | 抖动模型 | 市场 | 实际 |
|---|---:|---:|---:|---:|---|
| 9/11 13:50 | 0.2 | **25%** | 96.6% | **97%** | 越线 → 结算 32 ✅ |
| 9/12 13:44 | 0.1 | **9%** | 99.3% | **99.85%** | 越线 → 结算 33 ✅ |
| **9/12 14:00** | **0.4** | **0–1%**（我们给 6.5%） | **75.2%** | **5–8%** | **越线 → 结算 33 ✅** |

**市场前两次都准，误差 < 2pp，我们的口径低估 1–2 个数量级。**
且当 d ≤ 0.4 时 **d 与 σ_flicker 同量级** —— 越线不是尾部事件，是**基准情形**。

**第三行是 v0.15.2 的关键新增**：d=0.4 时**两家都错**（我们 6.5% / 市场 5–8%），
只有抖动模型对。当时 33 档 YES 报价 **0.05–0.08 → 12–20 倍**，
**这是 9/12 全天最大的一笔机会，被「d ≥ 0.6 才需要看抖动」这条规则自己屏蔽掉了。**

### 修正规则（v0.15.2：阈值由概率反推，不再写死）

按「剩 10 步」（午后典型视野）的抖动越线概率分三档：

| 抖动项 P | 对应 d | 处理 |
|---|---|---|
| **≥70%** | **d ≤ 0.4** | **抖动主导**：该档**不输出概率**、**禁止反向下注** |
| 30–70% | d ≈ 0.5–0.6 | **过渡区：取 `max(分位表, 抖动)`** |
| <30% | d ≥ 0.8 | 分位表可用 |

**为什么是 `max` 而不是加权平均**：站点 = 网格 + 单点额外方差，分布**单调加宽** →
越线概率的真值**同时 ≥ 两个口径各自单算的值**，加权平均必然系统性低估。
v0.14.0 写的「中间按 0.5 权重对半折」就是这么错的 —— 9/12 d=0.4 落在该区间，
但模型实际输出仍是 **6.5%**（≈纯网格值），说明「折」要么没被应用、要么被折没了，
两条路都指向同一个结论：**不能用加权平均，要用 max。**

### 边界与反例（不要把市场当成全知，也不要把抖动模型当成真理）

1. **抖动模型给的是上界。** N(0, σ) 随机游走假设**零漂移 + 步间独立**；
   一旦午后有明确降温趋势（9/12 16:00 起转雨，30 分钟内从 33.1 掉到 30.4），
   真实越线概率会显著低于表值。**只在「距边界 d ≤ 0.4 且仍处于升温/平台期」时采信它。**
2. **失败情形真实存在。** 9/10 13:20 达 31.8（d=0.2），剩 6 步 → 抖动模型给 86.8%，
   **实际未突破**（当日最高 31.8，结算 31 档）。市场当时报 97–99.85%，仍系统性偏高。
   但在 d ≤ 0.4 时反向下注 = 赌这个 ≤30%（多数时候 ≤15%）的失败，
   **期望上仍是错的**（且薄盘吃单滑点会再吃掉几个点）。

> **样本限制（务必如实转达）**：σ 只有 **2 天、30 个差分**；越线案例 **2 次**、未越线反例 **1 次**。
> σ 会随天气型变化（晴热静风日最大、阴雨日最小）。
> **方向可信，数值不可信**；要 10+ 天样本才能收紧。**当前用法：只在 d ≤ 0.4 时用它做
> 「不出概率、禁止反向」的硬开关，不做精细概率输出。**

**现状**：`review_brier.py --flicker` 已能算出 σ 与越线概率表（v0.14.0），
判读阈值自 **v0.15.2** 起改为**由概率反推**（原来写死 d≤0.3 / d≥0.6，与表内数字自相矛盾：
d=0.6 那一行标注「抖动项 <30%」，实际剩 10 步是 37.3%）。
**待办**：① 把它并进 `remaining_rise_cdf.json` 的使用路径（现在分位表只含趋势项、不含抖动）；
② 在 `--watch` 里对 d ≤ 0.4 直接打「抖动主导」标记并**不输出概率**（避免再出现「给 9%」这种数）；
③ 补齐 `obs_1min_archive.json` 的每日归档 —— σ 目前只有 2 天样本（9/11 漏跑 `--watch`，序列已永久丢失）。

## 每日建议台账：胜 / 负与累计 ROI（v0.15.0）

**问题**：每天给出去的建议，整体是赚还是亏？此前这个问题没有答案 —— 逐日复盘散落在会话记录里，
既不能累计，也无法回答「这套方法长期有效吗」。

**台账文件** `scripts/data/rec_pnl_log.csv`（**已纳入 git**，与 `.gitignore` 掉的 `decision_log.csv` 不同）。
一条腿一行；以 `#` 开头的行是口径说明，脚本会跳过。

| 字段 | 含义 |
|---|---|
| `settle_date` | 结算日（= 目标日） |
| `leg` / `side` / `bucket` | 腿名 / `YES`·`NO` / 档位 N（`N°C = [N.0, N+1.0)`） |
| `entry_price` | **首推可执行价** |
| `cost_usd` | 建议投入（USDC）；份数由 `cost ÷ price` 反推 |
| `settle_bucket` / `settle_status` | 实际结算档 / `confirmed`·`provisional` |
| `outcome` | `win` / `loss` / `open` |
| `note` | 一句话事后归因 |

**计入口径（重要，防止自我美化）**

1. **每条腿只记一次**：取「**首推价 + 建议仓位数**」。同一条腿后续的加仓、改价、换价重新推荐
   一律不重复计入 —— 否则同一论点被反复计数，且加仓价通常优于首推价，会造出虚假盈利。
2. 论点翻转后重新建仓（如 9/11 的 NO32 先押「不是 32」、后押「高于 32」）**仍算同一条腿**。
3. **记录的是建议，不是成交**。实际成交量受订单簿深度限制（薄盘常只能成交建议额的 20–40%），
   所以台账是**收益上限口径**：真金白银的波动小于表内数字，但**符号（胜 / 负）不受影响**。
4. **未结构化留存的日期不硬凑**：9/07–9/08（迭代期只有单腿试探）、9/10（结算日全天多次换腿，
   流水无法可靠还原）在 CSV 里以 `# 未纳入台账` 注明，宁可空着。
5. **口径的副作用：不记加仓 = 不记加仓的亏损（v0.15.2 补充）。**
   9/12 的 YES31 首推在 10:15（$15），同一腿 13:50 又以 0.010 加仓 **$54** 并全额归零 ——
   $54 不入账，于是当日显示 **+$24.22**；若计入则翻为 **−$29.78**。
   → **当日盈亏的符号对「加仓有没有被记录」高度敏感。** 读表时以**逐腿胜负**为主信号、
   金额只作量级参考；要精确对账必须保留**实际成交流水**（这一栏我们至今没有）。

**工具**

```bash
py -3 scripts/rec_pnl.py                                   # 逐日汇总 + 总计 + 累计曲线 + 按方向拆分
py -3 scripts/rec_pnl.py --open                            # 只列未结算的腿
py -3 scripts/rec_pnl.py --settle 2026-09-12=33 --write    # 回填结算档（近期日唯一可靠途径）
py -3 scripts/rec_pnl.py --auto --write                    # 用 maxt_HKO.csv（结算源，滞后约 10 天）回填
py -3 scripts/rec_pnl.py --markdown                        # 重新生成仓库根目录 PNL.md
```

`--settle` 默认**只试算不落盘**，加 `--write` 才写回 CSV。口径细节见 `PNL.md`。

**当前战绩（含 13 条腿 / 3 个结算日）**

| 结算日 | 结算档 | 腿 | 胜-负 | 投入 | 净盈亏 | ROI | 当日 |
|---|---|---:|---:|---:|---:|---:|:--:|
| 09-09 | 32°C | 5 | 1-4 | $577.30 | +$12.70 | +2.2% | 盈 |
| 09-11 | 32°C * | 5 | 3-2 | $156.00 | +$34.71 | +22.3% | 盈 |
| 09-12 | 33°C * | 3 | 1-2 | $198.00 | +$24.22 | +12.2% | 盈 |
| **合计** | | **13** | **5-8** | **$931.30** | **+$71.64** | **+7.7%** | 3 盈 / 0 亏 |

`*` 结算档暂定（09-11 / 09-12 的官方 Daily Extract 尚未出，按结算站全天快照最高 + 市场价判定；
2026-09-12 首次回填时误用 14:57 的时点快照 32.6 记成 32，当晚按全天快照 33.1 更正为 33）。
**按方向拆开才看得清**：`NO` 7 腿 4-3、ROI **+35.9%**；`YES` 6 腿 **1-5**、ROI **−76.7%**。
两类亏损泾渭分明，都是**逆着当日已经确立的方向下注**：

- `YES` 的 5 笔亏损全是「**上望更高一档**」的右尾注（YES30 / YES29 / YES33 ×2 / YES31）；
- `NO` 的 3 笔亏损全是「**押注封顶**」（9/09 NO32、9/11 NO32、9/12 NO33 —— 都赌「到不了这个整数」，
  结果当日快照最高分别摸到 32.2 / 32.6 / 33.1）。

**盈利侧**则集中在「顺已确立的上升方向做 NO」：9/09 NO31（结算 32）、9/11 NO30 + NO31、
9/12 NO32（结算 33）。与《峰值窗口内不要钉档位》《σ_flicker》两节同源：**方向顺势、但不要在
峰值窗口内用单点快照把某一档钉死**。

**每日收盘必做（漏一天就永久断档，补不回来）**

1. 当日所有给出建议的腿写入 `rec_pnl_log.csv`，`outcome` 先留 `open`；
2. 结算档出来后 `--settle YYYY-MM-DD=N --write`；
3. `--markdown` 重新生成 `PNL.md`；
4. 按《发版流程》同步双副本并推送。

> **样本限制（如实说明）**：3 个结算日 / 13 条腿，**无统计显著性**，且 9/09 与 9/11 结算档相同、
> 市场状态高度相关。台账的价值是**逼出可复盘的归因**，不是给方法打分。攒到 20+ 个结算日再谈胜率。

## 发版流程（每次推送 GitHub 前必做）

用户的要求：**每次更新到 GitHub 都要更新版本号，并简述最新版本的改动**。固定四步：

1. **定版本号**（语义化 `MAJOR.MINOR.PATCH`，规则见 `CHANGELOG.md` 顶部）
   - 只改文档 / 性能 / 修 bug 且**任何输出数字都不变** → PATCH
   - 新增功能或参数（向后兼容）→ MINOR
   - 结算口径、档位定义、核心算法改变 → MAJOR
2. **同步四处版本号**（漏一处就会自相矛盾）
   - `scripts/hk_edge.py` 的 `VERSION` 常量（`--version` 读它）
   - `SKILL.md` frontmatter 的 `version:`
   - `README.md` 顶部的「当前版本」行
   - `CHANGELOG.md` 自己的「当前版本：」行（最容易漏的一处）
3. **在 `CHANGELOG.md` 顶部加一条**：日期 + 一句话主题 + 要点（改了什么、为什么、
   **有没有改变输出数字**）。若数字会变，必须写明验证方式（例："优化前后逐位比对，最大差 0.00e+00"）。
4. **双副本同步：`cp` 的目标路径必须逐文件写全，不同目录的文件不许塞进同一条命令的同一个目标**
   （2026-09-11 实际踩坑）。
   两份副本：`.workbuddy/skills/hk-weather-edge`（活的工作副本）与
   `.codebuddy/skills/hk-weather-edge`（git 源）。同步一律 `.workbuddy` → `.codebuddy`，方向别反。
   ❌ 反例（会在根目录造出孤儿文件）：
   ```bash
   cp "$D/SKILL.md" "$D/README.md" "$D/CHANGELOG.md" "$D/scripts/hk_edge.py" "$S/"
   ```
   最后那个参数是 `$S/`（根目录），于是 `scripts/hk_edge.py` 被散到仓库根目录，
   留下一个 58937 B 的未跟踪 `hk_edge.py`，此后每次 `git status` 都多一行 `??`。
   ✅ 正例（按目标目录分两组）：
   ```bash
   cp "$D/SKILL.md" "$D/README.md" "$D/CHANGELOG.md" "$S/"
   cp "$D/scripts/hk_edge.py" "$S/scripts/hk_edge.py"
   ```
   同步后用 `md5sum` 逐文件比对两份副本（四个文件全部 OK 才算同步成功）。
5. **提交并推送**：只 `git add` 明确列出的目标文件（**不要 `git add -A` / `git add .`**），
   提交信息带版本号（`Release v0.5.0: ...`）；推完核对本地 `git rev-parse --short HEAD`
   与远端 `git ls-remote origin main` 一致（`gh` CLI 未认证，用 `git ls-remote` 代替）。
6. **收尾自检**：`git status --short` 应只剩预期的 `M ...`，**不应出现任何 `??`**。
   出现 `??` 说明有文件被散落到了错误位置，先查清来源再提交，必要时清理（可先确认该文件内容
   在 git 历史中有无对应版本，有则可安全删除）。

## 盘中锚点：用站点自身残差，别用网格剩余升温

**问题**：结算档位是站点口径，但 `remaining_rise_cdf.json` 是 ERA5 **网格**口径。
临近峰值时直接用「网格剩余升温」会给站点峰值带来 0.5–1.0°C 的系统偏差，
在 31/32 这种边界上足以翻转判断。

**做法（2026-09-12 建立，数据仅 1 天，样本待扩）**：
1. 读 `scripts/data/obs_1min_archive.json` —— 它保存历史日的**日内站点逐 10 分钟实测**。
2. 取同月、同一时刻（如 11:50）的站点值，算到当日实测最高的**残差**。
   已归档样本：**2026-09-10 11:50 = 30.7 → 当日最高 31.8，残差 +1.1**。
3. 今日峰值 ≈ 今日同刻站点值 + 该残差（再做当日条件修正）。
4. 与网格口径互校：`station_peak = grid_daily_max + Δ_daily`，Δ_daily 用近 3 日**站点−七模式日均**实现值。
   两条路给出一致 mu 才当硬结论，不一致就报区间。

**为什么不能只看 `--watch` 的点估计**：① 它把「时点偏差」与「全天气候偏置」**相加**，重复计数
（2026-09-12 11:10 实测点偏差 +0.60，打印 L = +1.70，峰值被抬高 0.5–0.7°C）；
② 它的水位 L 有**整点对齐缺陷**（非整点观测拿去比整点网格）。
→ 手工核对只用整点样本，或扣掉 `(观测分钟/60) × 该小时网格升温`。

**距离峰值 2 小时内，超薄盘的 top-of-book 报价没有信息量**：2026-09-12 12:00–12:03，
31 档 YES 报价在 3 分钟内从 0.200/0.320 → 0.110/0.170（mid 0.26 → 0.14），
同档位 level-1 深度只有 14–360 份，单笔 $100 即可推动。**必须用 `--depth 8` 的加权中间价，
并间隔 20s 连取 3 次**；两次采样不一致时，宁可不做。

### ⚠ 选哪条腿：不要用 EV，也不要用满 Kelly 分数 f\*，要用期望对数增长 ELG

**2026-09-12 12:00 实测踩坑。** 两条候选腿：

| 腿 | 可执行价 | 公允 P | EV | 满 Kelly f\* |
|---|---:|---:|---:|---:|
| 买 YES 31（赔率 10:1） | 0.090 | 39.6% | **+340%** | 33.6% |
| 买 NO 32（赔率 2:1） | 0.330 | 54.2% | **+64%** | 31.7% |

两个直觉都是错的：

1. **EV 不能用来排序。** EV = p/q − 1，分母 q 越小被放得越大。同一份概率优势放在高赔率腿上会膨胀成 3–5 倍 EV。EV 差 5 倍，而 f\* 只差 2pp —— 那 5 倍全是杠杆，不是 edge。
2. **f\* 也不能用来排序。** f\* 是"这条腿自己的最优仓位"，不是"这条腿的价值"。举反例：p=0.5/q=0.4 的 f\*=16.7%、ELG=0.0204；p=0.1/q=0.05 的 f\*=5.3%、ELG=0.0207 —— f\* 小 3 倍但 ELG 反而更高。**f\* 最大的赌注未必是增长率最高的。**

**正确做法：算每条腿的期望对数增长（相当于各自的 log-growth 价值），选 ELG 高的一条。**
因为 mu 本身有不确定性，要用 mu 的先验分布加权，而不是在点估计上算：

```python
# 同样投入本金 12%，ELG = E_mu[ p(mu)·ln(1+f·(1−q)/q) + (1−p(mu))·ln(1−f) ]
# mu 先验取"当日三情景"的离散度：如 N(32.2, 0.35) 离散为 {31.5,31.9,32.2,32.6,32.9}
```

同日 12:00 在 mu 先验 {31.5:12%, 31.9:24%, 32.2:28%, 32.6:24%, 32.9:12%} 下：

| 腿 | 最优 f\* | **ELG** |
|---|---:|---:|
| 买 YES 31 @0.150 | 30.5% | **0.196** |
| 买 NO 32 @0.330 | 38.7% | **0.141** |

→ **YES31 的期望增长率高 39%，选它。**（同 f 比较也一致：同为 f=12%，mu 32.2 时 YES31 ELG 0.238 vs NO32 0.060。）

**两条仍然有效的、非 EV 层面的理由，值得在 ELG 之上做定性加权：**

- **右尾保险**：低档 NO 腿在「峰值高于本档上界」时也赢，而尾档概率恰是分位表最被低估的区间（网格尾部比站点薄 4.7 倍，见上文）。把 P(≥33) 从 4.7% 修到 19%，NO32 公允 54%→62%，ELG 反超。**这个修正只抬高 NO 腿，不改变低档 YES 腿。**
- **跨 mu 稳定性**：NO32 的 ELG 在 mu 31.9–32.8 全区间 ≥ +0.05（下凸、有地板）；YES31 在 mu 32.9 附近穿过零轴转负。若 mu 的向上意外是主要风险，NO 腿的"地板"值钱。

**「低价腿是高价腿的子集」不能用来选腿。** 落 N 档 ⇒ 必然不落 N+1 档，所以赢 YES\_{N} ⇒ 赢 NO\_{N+1}，
两腿同向且一腿严格包含另一腿 —— 这只支持「**不要两条都打**」（重复暴露），
**不支持「选 NO 而不是 YES」**。这是 2026-09-12 12:05 犯的逻辑跳跃。

### 覆盖度现实：先算 depth，再决定值不值得讨论

同一天 12:11 的实际可成交量：YES31 ask 侧 **48 份（$4）**、NO32 ask 侧 **47 份（$16）**。
两条腿的 Kelly 建议分别是 $165 / $155，**合计能吃到的不到 $21**。
→ 深度差两个数量级时，**"选哪条腿"这个问题的经济价值本身就很小**；
应先把结论落到「今天值不值得盯盘」上，不要为一个 $20 的仓反复出建议。

### 4. 午间回落 ≠ 见顶（2026-09-12 实证）

**12:00–12:40 之间站点回落 0.3–0.5°C 是常态，不是见顶信号。** 2026-09-10 日内档：
`12:00 31.1 → 12:10 31.3 → 12:20 31.4 → 12:30 31.2 → 12:50 31.2 → 13:00 31.4 → 13:20 31.8(日最高)`
—— 12:30 回落 0.2°C 之后，13:20 再创新高 31.8。**把午间下探读成封顶，会系统性放弃 13:00 后那一跳。**

正确读法：看**网格当日云量与升温曲线**，不看站点最后 1–2 个 10 分钟样本。
网格 13:00 前后若云量降到当日最低、且温度仍在上行，站点 13:00–14:00 大概率还有一次冲高。

**类比日平移法**（比 `--watch` 的 L 混合法更稳，推荐峰值窗口内当主口径）：
找日内形态最接近的归档日 → 用「今日同刻值 − 类比日同刻值」作平移量 → 叠加类比日峰值。
2026-09-12 实测：今日比 09-10 同刻高 0.2°C，09-10 峰值 31.8 → 今日峰值预估 **32.0**，
而 `--watch` 给 31.80、「日最高偏差法」给 32.6。**三路径分歧 0.8°C 时，报区间并把众数定在边界档，不要给点估计。**

### 5. 观测下限会双向移动概率，但只能截断一次（2026-09-12）

盘中「已观测最高 31.6°C」这类事实，**同时**做两件事：
① 封住下界（31 档必中，30 档及以下归零）；② 把峰值分布**从下方截断**，把质量上推到 32 档。
只读 ①（「31 档已经安全了」）会系统性高估 31 档。

**但截断不能做两次。** mu 的每个估计量（类比日平移、L 混合法、日最高偏差法、官方预报）
都已经吃过盘中观测，它们给出的 mu 已是**条件分布**的中心。若再对正态做一次 `x ≥ obs` 截断，
就是把同一份信息用两遍 —— 实测会把 P(31) 从 52% 抬到 74%（mu 31.9/sd 0.55 口径），方向恰好错。
**正确做法：把观测当作 mu 估计量的输入，而不是当成对 mu 分布的额外约束。**

### 6. 每天必须跑一次 `--watch`，否则当天序列永久丢失（2026-09-12 发现）

`_archive_1min()` 只在**跨天首次运行 `--watch`** 时触发归档（`obs_1min_log.json` 的 `date` 与今天不符时）。
后果：**某天没跑 `--watch`，那天的 10 分钟级站点序列就再也拿不回来了**。
已发生：2026-09-11 未跑 → `obs_1min_archive.json` 只剩 09-10 一天，日内类比法至今 **n=1**。
而归档正是把剩余升温分位表从 ERA5 网格口径换成站点口径的**唯一途径**（网格尾部比站点薄 4.7 倍）。

**纪律：每个交易日至少跑一次 `--watch`（任意时刻均可），峰值窗口内每 10 分钟一次。**
若当天确实无法跑，收盘前手工调用一次归档函数保底。

### 7. HKO 分区 CSV 会滞后（2026-09-12 实测）

`latest_1min_temperature.csv` 名义 10 分钟更新，但 13:06 时时间戳仍停在 **12:50（滞后 ≥16 分钟）**。
该文件不带更新延迟标识，`--watch` 只会照读。
→ 盘中判读时先看 `CSV 时间` 字段，滞后 >15 分钟则**本轮实况读数的时效性打七折**，
不要用它去判断「刚过去的 10 分钟涨没涨」。此时只有 `rhrread`（整点，整数位）可作时效性交叉核对。

### 8. 极端定价出现时，读完整档阶梯而不是 top-of-book（2026-09-12）

**场景**：13:44 时 31°C 档 YES 报价 `0.001 / 0.002`，而快照最高值 31.9 距上界仅 0.1°C。
表面读法：「市场 99.8% 确定冲上 32，31 档已死」→ 不敢碰。

**正确读法**：拉完整 ask 阶梯再看。

| 价位 | 份数 | 累计成本 | 累计均价 |
|---:|---:|---:|---:|
| 0.002 | 1653 | $3.31 | 0.0020 |
| 0.003–0.009 | 778 | $8.11 | 0.0033 |
| **0.010** | **4586** | **$53.98** | **0.0077** |

→ **$54 能买下 7018 份**，若 31 档结算则赔付 $7018。**top-of-book 的 0.002 只是冰山一角，
0.010 处挂着 4586 份** —— 只读 level-1 会完全错失这笔不对称性。

**纪律**：当某档价格 ≤ 公允值的 1/10 时，必须跑一次完整档阶梯（`--depth 40` 或直连 CLOB
`/book?token_id=`），算出「按公允概率的 EV / 最大可买成本」，再判断值不值得动手。
**不要因为 top-of-book 报价极端就跳过；也不要只看 level-1 就下判断。**

## 瞬时值 vs 已观测最高：同一个分位表，定义差 11 倍（2026-09-12 实测）

盘中算「再升 X°C 还有多少概率」时，**base 只能用「截至当前已观测最高」，绝不能用「当前瞬时值」**。
两者在同一个 ERA5 九月样本（2015–2025，n=330）上的差距：

| 统计量 | 定义 | P(≥0.4) | P(≥0.9) | P(≥1.4) |
|---|---|---:|---:|---:|
| `D` | 日最高 − **14 时瞬时值** | **30.9%** | 14.5% | 6.4% |
| `R14` | 日最高 − **截至14时最高** | **2.7%** | 0.3% | 0.0% |
| `R13` | 日最高 − 截至13时最高 | 9.4% | 2.1% | 0.0% |
| `R15` | 日最高 − 截至15时最高 | 0.9% | 0.0% | 0.0% |

`D` 比 `R` 大 11 倍的原因：`D>0` 里混进了大量「日最高出现在 14 时之前、14 时已回落」的日子
（九月占 ~33%），这些日子在 `D` 口径下被误算成「还有升温空间」。**用 `D` 会系统性高估尾档。**

✅ **分位表本身是准的**：`R14` 实算 2.7% vs `--watch` 表 3.0%，`R13` 实算 9.4% vs 表 10.0%。
可以直接采信，放心用。

**复现脚本口径**（需要时重跑）：
```python
# ERA5 archive，逐时 temperature_2m，按日取
up14 = max(v for hh, v in vs.items() if hh <= '14')   # 截至14时的最高
R14  = max(vs.values()) - up14                        # ← 这才是分位表定义
```

**当日实操**：`--watch` 输出的「已观测最高」正是 `R` 口径的 base，直接可用。
手工核验时，**先确认这个最高值是真的出现过**（见下条），再拿它去查表。

## 峰值窗口内不要钉档位：瞬时尖峰与回落（2026-09-12 实测）

**10 分钟瞬时读数会出现尖峰。单点不能作为「已定格」的依据。**

9/12 当日实测（结算站，10 分钟快照）：

| 时刻 | 13:20 | 13:30 | 13:40 | **13:50** | **14:00** |
|---|---:|---:|---:|---:|---:|
| HKO | 31.9 | 31.7 | 31.7 | **32.6** | **32.3** |

13:40→13:50 单步 **+0.9°C**（全港其他站同期几乎不动，是本站局地现象），14:00 即回落 0.3。

**代价**：13:50 我基于「快照 31.9 距 32.0 边界仅 0.1、1分钟/10分钟缺口 ∈[0,+0.1]」
给出「买 31YES @0.010」的建议 —— 15 分钟后站点跳到 32.6，**该仓位直接归零（−100%）**。

### 一天之内出了两个同量级尖峰，**第二个才决定结算档**（2026-09-12 全天）

| 时刻 | 13:30 | 13:40 | **13:50** | 14:00 | 15:00 | 15:20 | **15:30** | 15:50 | 16:10 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| HKO | 31.7 | 31.7 | **32.6** | 32.3 | 31.8 | 32.2 | **33.1** | 32.1 | 30.9 |
| 单步 | −0.2 | 0.0 | **+0.9** | −0.3 | — | +0.4 | **+0.9** | −1.0 | −0.7 |

- **13:40→13:50 的 +0.9 是本站局地现象**（全港其他站同期几乎不动）→ 当日最高抬到 32.6，维持 1.5 小时；
- **15:20→15:30 的 +0.9 是全港同步**（赤鱲角 33.5 / 大陇 33.4 / 打鼓岭 33.2 / 元朗公园 33.2 / 上水 33.0；
  `watch.log` 15:30 步）→ 当日最高抬到 **33.1**，**直接决定结算档 33°C**。

**同一天、同一现象，让我在三个档位上分别判对、判错、判错**：

| 时刻 | 动作 | 依据 | 结果 |
|---|---|---|---|
| 13:50 | 买 31YES @0.010 | 「31.9 距 32.0 仅 0.1，来不及越线」 | **−100%**（14:00 前越线） |
| 13:50 | 判 NO32 死刑（押 32 已定） | 用 32.6 这个**局地**尖峰当既成事实 | **反败为胜**：结算 33 → NO32 派息 $222（+$142） |
| 全天 | 持有 NO33 @0.70（$103） | 「封顶到 32–33 之间」 | **全额损失**（15:30 全港同步越线） |

**→ 用峰值窗口内的单点快照去「钉死」任何一档，方向对错都只是一枚硬币。**
关键区别在**共振范围**：13:50 是**单站**尖峰（可信度低、可被后续回落证伪），
15:30 是**全港同步**（可信度高、不是传感器噪声）。**判「已定格」时，先问这个尖峰有多少站共振。**

**四条纪律**：
1. **13:00–15:30 峰值窗口内，不下「钉住档位」的结论。** 一个 10 分钟步长就能翻转，
   而极低价档位（≤0.02）一旦判错是全额损失，风险回报极差。
2. **判「已定格」需要连续 2–3 个 10 分钟点同向**（同档位或不创新高），单点不足。
3. **极低价 ≠ 便宜。** 当某档报价 ≤0.02 时，先问「市场为什么敢这么定」，
   再问「我凭什么认为市场错了」。**薄盘+极低价+峰值窗口三者同时出现时，默认不动手。**
4. **等最后一个尖峰（v0.15.2 新增）。** 结算量取**全天**最高，而本站一天能出 2 个以上
   同量级尖峰（9/12：13:50 与 15:30 各 +0.9）。**在最后一个尖峰出现前（9 月晴天约 15:30 之后），
   任何「该档已定格」的结论都不成立** —— 这一条推翻了此前「13:00–15:00 内可判定格」的写法。

### 9/12 结算 33°C：全天成绩与错误归因（v0.15.2）

| 项 | 值 |
|---|---|
| 结算档 | **33°C**（15:30 全港同步 33.0–33.5，本站 33.1；结算量取 1 分钟口径 → ≥ 33.1） |
| 台账 | 3 腿 1-2，投入 $198，净 **+$24.22（+12.2%）**（首推口径；若计入未入账的 13:50 加仓 −$54，则为 **−$29.78**） |
| 模型 vs 市场（上午） | 模型 Brier 0.1927 vs 市场 0.2357 —— 两家都把众数押在 32，**都错**；模型只是错得少 |
| 全天最大机会 | 14:00 的 33 档 YES @0.05–0.08（**12–20 倍**）—— 被「d ≥ 0.6 才需看抖动」这条规则自己屏蔽 |
| 全天最大亏损 | NO33 @0.70（$103）押「封顶不过 33」，15:30 越线后全额损失 |

**三个错误，按严重程度排序**（第 1 条污染的是复盘本身，另两条污染的是当天决策）：

1. **用盘中快照代替「全天最高」来记账/判断** —— 14:57 记成 32，当晚按全天 33.1 更正为 33（v0.15.1）。
   它同时污染了**归因**：同一份 `decision_log.csv`，结算填 32 得「市场领先 29.9%」，
   填 33 得「模型领先 18.3%」。**记账时点选错，等于把复盘结论也一起写错。**
2. **抖动项只在 d ≤ 0.3 生效，d=0.4 被交给了分位表** → 33 档被低估 10 倍以上；
   当时报价 0.05–0.08 的 33YES 是当天唯一的大 edge，却因为规则边界划错而没被识别。
3. **午后没跑带 `--market` 的 `hk_edge.py`** → 13:50 / 14:10 这两个最贵的决策时点
   在 `decision_log.csv` 里完全不存在，复盘只能打到上午（见上文《日志缺口》）。

> **复盘本身的不确定性（如实标注）**：σ_flicker 只有 2 天样本、越线 2 例 / 未越线 1 例；
> 台账金额是建议口径不是成交口径；9/11 与 9/12 的结算档仍是 provisional（官方 Daily Extract
> 截至 2026-09-12 只出到 **09-10**，已实查 `dailyExtract_202609.xml` 只含 1–10 日 + 月均值/常值两行）。
> **结论方向可信，数值不可信。**

## 合规与免责

方法论研究，**不构成投资建议**。香港《赌博条例》(Cap. 148) 下预测市场处于灰色地带：未被明确许可，也未见针对此类平台的执法，SFC 未表态。向用户输出结论时应保留此风险提示，必要时建议其咨询香港律师。**不要建议用 VPN 绕过地区限制**（违反 Polymarket ToS，可能冻结资金）。

数据来源：Weather data © Hong Kong Observatory；Model data via Open-Meteo（ECMWF / NOAA / DWD / Météo-France / JMA / UKMO / CMC），全部免费、无需 API key。

## 参考资源

- 实证数据（站点偏差表、模型回测表、偏移分布、dressed ensemble 公式推导）见 `references/methodology.md`。
- 向用户讲解方法论或需要图表时，直接提供 `assets/docs_edge_map.html`。
