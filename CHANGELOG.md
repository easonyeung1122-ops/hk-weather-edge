# Changelog

语义化版本 `MAJOR.MINOR.PATCH`：

- **MAJOR** — 结算口径 / 档位定义 / 核心算法改变，旧结论不再可比
- **MINOR** — 新增功能或参数（向后兼容）
- **PATCH** — 修 bug、文档、性能（**不改变任何数字**）

每次推送 GitHub 前必做（见 `SKILL.md`「发版流程」）：
递增版本号 → 在下面加一条 → 同步三处：`README.md` 顶部版本行、
`SKILL.md` frontmatter 的 `version:`、`scripts/hk_edge.py` 的 `VERSION` 常量。

当前版本：**v0.10.0**（`py -3 scripts/hk_edge.py --version` 可查）

---

## v0.10.0 — 2026-09-09 · 接入 0.1 °C / 10 分钟实况源（结算精度的关键修复）

`--watch` 与 `load_observed_max()` 的实况从 `rhrread`（**整数位 + 每小时**）换成
**分区气温 CSV**（**0.1 °C + 每 10 分钟**，39 站，含结算站）。这是 09-09 用一次实亏换来的。

**改了什么**
- 新增常量 `HKO_OBS_1MIN`（`.../regional-weather/latest_1min_temperature.csv`）
  与 `OBS_STATION_NAMES`（结算站在该 CSV 里叫 **`HK Observatory`**）。
- 新增 `_http_raw()`（非 JSON 接口取原文）、`fetch_hko_1min()`、`load_1min_max()`、
  `_load_1min_log()`，running max 落在 `data/obs_1min_log.json`，跨天自动作废。
- `watch()`：三路并行（0.1 °C 源 / rhrread / 网格），**优先 0.1 °C 源**，
  取不到才回落到 rhrread 并打印醒目提示；输出新增「实况源」一行。
- `load_observed_max()`：0.1 °C running max 与整点日志**取大者**，`hk_edge.py --date 今日`
  无需再手动传 `--observed-max`。
- 回填未采样小时用的 `sampled_h` 现在并入 10 分钟序列覆盖到的小时。

**为什么值这一版**
`rhrread` 在结算值刚跨过整数边界时双重失真：① 漏掉整点之间的升温
（09-09 13:00 报 31，实际 13:40 已 **32.2**，差 1.2 °C）；② 整数舍入撑开 ±0.5 °C 未知。
后果是 13:49 算出「31 档 66%」，而**31 档当时已归零——错 66 个概率点**。
接入后同一时点自动得到「已观测最高 32.2 → 众数档 32」，并正确触发已过峰值守卫
（回落 0.9 °C）。

**数字会变**：只影响当日（`--date == today`）的实况输入与由此而来的下限截断、
站点偏差、过峰守卫；**历史回测、提前 N≥1 天的概率、Kelly 公式一律未触碰**。
验证方式：`--watch` 前后逐行比对，唯一变化是实况源与 `今日已观测最高`
（31 → 32.2），其余结构不变；`--date 2026-09-10` 输出与改动前逐位一致。

**必须配套**：CSV 只给「最新一笔」，不自带当日最高 —— **要每 10 分钟跑一次 `--watch`**
才能累积 running max（cron `*/10 0-12 * * *` UTC）。轮询越密，漏掉的峰值越小。

---

## v0.9.3 — 2026-09-09 · 新增 A3：实况源必须用 0.1 °C / 10 分钟分区气温

**这是一次真实亏损换来的（09-09 当天）**，优先级高于其它条目。

`rhrread` 的 `temperature` 只有**整数位**且**每小时**更新，在结算值刚跨过整数边界时会双重失真：
① 漏掉整点之间的升温（13:00 仍报 31，实际 13:40 已达 **32.2**）；
② 整数舍入带来 ±0.5 °C 未知，直接决定 30/31/32 三档归属。
后果：按 31 外推得出「31 档还有 66%」，而**31 档当时已归零**。

- 新增 A3 小节，给出正确数据源
  `https://data.weather.gov.hk/weatherAPI/hko_data/regional-weather/latest_1min_temperature.csv`
  （0.1 °C / 10 分钟 / 39 站；结算站名是 **`HK Observatory`**）。
- 给出判断规则：当日且实测离整数边界 <0.5 °C 时，必须以该 CSV 为准。
- **TODO**：同目录「午夜至現時最高最低氣溫」的 CSV 文件名未猜到（`latest_max_min_temperature.csv`
  等 6 个候选均 404），未确认前仍由 `--watch` 按 10 分钟轮询自建 running max。
- **数字不变**：本版只改文档，脚本尚未接入该源（接入属 MINOR，留待下一版）。

---

## v0.9.2 — 2026-09-09 · 按实测更新频率重定 `--watch` 的盯盘节奏

原来建议「08:00–20:00 每 10 分钟跑一次 `--watch`」，但**跑得比数据还快**：实测（2026-09-09）
`rhrread` 是**每小时**更新（整点后约 2 分钟，08:02 → 10:02）、HKO 9 天预报约每 2 小时
（07:50 → 09:50）、NWP 多模式 daily tmax 一天 4 次（08:43 vs 10:05，7 个模式仅 UKMO +0.3）。
10 分钟一次只是在重复读同一个数。

- 改为：**概率重算 = 每小时一次**（整点后 5 分）；**13:00–17:00 峰值窗口加密到每 30 分钟**。
- 明确区分：需要高频盯的是**盘口价**（实时，用 `market_prices.py`），不是 `--watch`。
  实测 08:45→09:47 一小时内 31 档 YES 从 0.420 走到 0.505，EV 从 +23% 推到 +43%。
- 附实测更新频率表，后续再调节奏有据可依。
- **数字不变**：只改文档，脚本计算路径未触碰。

---

## v0.9.1 — 2026-09-08 · 修「已观测下限」提示对非当日目标的误报

查未来日期（如 `--date 2026-09-09`）时，`bucket_probs` 并不会用当日已实测最高温截断
（`floor` 只在 `t == today` 时传入），但提示行无条件打印
「已观测下限 … → 当日分布截断重命名」，读者会误以为目标日分布已被截断。

- 现在区分两种情况：目标日包含当日时照原样打印；否则打印
  「注: …已实测最高 X°C 已知，但目标日不是当日 → 不做截断」。
- **数字不变**：只改打印文案，概率 / EV / Kelly 的计算路径未触碰。
  验证方式：同一条命令 `--date 2026-09-09 --market "…"` 修改前后各档公允 P 逐位一致
  （29:0.1968 / 30:0.4254 / 31:0.2851 / 32:0.0607 / 33:0.0040）。

---

## v0.9.0 — 2026-09-08 · 修正日内外推的三处系统性偏差

`--watch` 的日内外推以前会**凭空高估午后还能升多少**，三个来源叠加，实测在 2026-09-08 把
P(≥33) 推到 25%（当时市场只给 3%）。三处都修了：

1. **观测不对时**：rhrread 整点更新、实际滞后可达 1 小时，却拿 `now.hour` 去取网格值，
   等于用 15 时的实测比 16 时的网格（当日多算出 +0.4 °C 偏差）。
   现在所有小时内推算一律以**观测时间戳**为基准，并在滞后 ≥1 小时时打印提示。
2. **「今日已观测最高」只是自身轮询采样的最大值**：漏点即系统性低估当日真实最高
   （09-08 漏掉 12、13 时整点）。它是概率下限、也是「已过峰值守卫」的触发条件——
   低估等于让模型以为"还能再升"。现在用模式逐时值 + 站点偏差回填**没有实测样本的小时**
   （`past_hours` 拉到当天 0 时），有实测的一律以实测为准。
   回填偏差**封顶在气候值**（`min(当下偏差, calib.bias)`）：当下偏差今天能在 1 小时内
   摆 ±1.3 °C（09-08：15 时 +0.3 → 16 时 +1.7），直接拿它外推未采样小时会凭空宣称
   "中午已到过 33.1 °C"。宁可低估，不可高估。
3. **过峰守卫与概率起算点不一致（双重惩罚）**：守卫里的 `net = gross − g` 本来就是
   "在已观测最高之上还能再升多少"，但概率循环却从**已回落的当前读数**起算，
   把同一段回落罚了两次。现在起算点统一为 `max(当前读数, 已观测最高)`。

同快照对照（09-08 16:00，站点 32 °C）：v0.8.0 峰值 32.34 °C / P(≥33)=22%；
v0.9.0 峰值 32.40 °C / P(≥33)=24% —— 概率几乎没变，但**下限与守卫站到了完整的数据上**：
已观测最高按未采样的中午时段修到 32.4 °C，过峰守卫随之正确触发（回落 0.4 °C）。

⚠️ 已知未解：整套估计对"站点偏差"这个输入**高度敏感**——同一快照，偏差取单点 +1.70 得
P(≥33)=22%，取全天稳健均值 +0.73 得 68%。本案不对此下结论（那等于给 P(≥33) 凭空调档），
只做保守封顶；要定标它需要逐时站点历史，见 `SKILL.md`「已知弱点」。

- 批量订单簿 `POST /books` 已验证：与逐个并行拉取的输出**逐字一致**（19 行全等），
  耗时 0.79s vs 回退路径 1.72s；批量失败仍自动回退，功能不依赖该接口。

## v0.8.0 — 2026-09-08 · Kelly 比例 1/4(25%) → 35%

- 仓位缩放从 **1/4 Kelly（25%）改为 35% Kelly**：建议在原来的基础上放大 1.4 倍
- 抽成常量 `KELLY_FRAC = 0.35`（`scripts/hk_edge.py`），金额、份数、占本金%、表头、
  参数说明全部由它派生——以后再调只改一处，不会再散落
- 表头由 `1/4Kelly(金额/份数)` 变为 `35%Kelly(金额/份数)`
- SKILL.md 交易纪律与 README 同步改为 35% Kelly
- ⚠️ 提醒：这会让**单笔下注额放大 40%**。容量约束没变（近月档深度常常只有几十到几百份），
   Kelly 算出来的金额超过可成交量时，**仍以可成交量为准**（见 SKILL.md A0）

## v0.7.0 — 2026-09-08 · 反向 edge = 止盈提醒 + 持仓检查

- 每档新增**止盈警告**：某方向「现在买入」的 EV ≤ `--exit-ev`（默认 **−15%**）时打
  `⚠持YES止盈-XX%` / `⚠持NO止盈-XX%`。含义不是反向开仓，而是
  **市价已明显高于公允值，持有人应平仓而不是持有到期**
- 新增 `--hold "档位:成本价:份数[:Y|N]"`：把已用头寸喂进去，直接输出
  **份数 / 成本 / 现价 / 浮盈% / 公允值 / 现在买入EV / 建议（止盈·持有观察·仍低估可加仓）**
- 新增 `--exit-ev` 调整止盈阈值；SKILL.md 纪律要求每次复跑必须在结论里点名提醒这类信号
- 只新增展示与提醒，**概率、EV、Kelly 的数值计算均未改动**

## v0.6.0 — 2026-09-08 · 建议输出增加「份数」

- 1/4 Kelly 列从「只给金额」改为 **金额 + 建议份数 + 占本金百分比**：
  下单界面填的是份数，只给金额等于没给
- 份数 = 金额 ÷ 该档单价（买 YES 用市场价 `m`，买 NO 用 `1−m`）；
  输出末尾固定提示「按订单簿可执行价复核」——单价差 1 分钱份数就差不少
- SKILL.md 交易纪律改为**三件套**（金额 / 份数 / 占本金%），并写进 A 节输出说明
- 只改展示，**不改变任何概率、EV 或 Kelly 数值**

## v0.5.0 — 2026-09-07 · 性能：全流程 ~9s → ~1.5–3s

只动传输层，**任何一个数字的计算方式都没变**（已逐位比对：优化前后概率表最大差 `0.00e+00`）。

- `requests.Session` 保活 + 互不依赖的接口并行 → 主脚本 3.2s → 冷 1.3s / **热 0.2s**
- `market_prices.py` 改用 CLOB **批量接口 `/books`**（22 个盘口 1 次请求）：5.6–7.3s → **1.2–1.6s**；
  批量失败自动回退逐个并行拉取
- 模式 / 官方预报类接口加 **15 分钟磁盘缓存**；**实况观测与订单簿价格永不缓存**
- 命中缓存时输出必打印数据年龄；新增 `--no-cache`、`--ttl <秒>`（默认 900）
- 延迟导入 `requests`；历史 CSV 只在真需要重校准时加载
- `--watch` 的观测与网格实况并行：1.7s → 1.2s
- 文档：SKILL.md / README 新增「性能与缓存」章节

## v0.4.0 — 2026-09-07 · 已过峰值守卫（修午后系统性高估）

- `--watch` 日内外推新增**已过峰值守卫**：实测已从今日最高回落 ≥0.3 °C 且时点 ≥14 时 →
  剩余升温期望与离散度大幅收窄，只留少量"晚间反弹创出新高"的可能
- 修掉一个荒谬输出：09-07 16:00（29 °C vs 已观测最高 30 °C）P(31) 此前给 **56%**，修复后 **~2%**
- 与「外推低于已实测最高」是同一族问题的两面：早上高估是网格还没到过那么高，
  午后高估是**已经到过又回落了**

## v0.3.0 — 2026-09-07 · 订单簿取价（A0）

- 新增 `scripts/market_prices.py`：从 **CLOB 订单簿**取可成交价（YES/NO 各自 bid/ask + 深度）
- 明确「最后成交价 vs 订单簿」的差异（实测同一时刻差 15–17 分钱），
  算 EV 必须用**可执行价**；买卖方向对应 ask/bid 的规则写进 SKILL.md

## v0.2.0 — 2026-09-07 · 日内分布以实况为条件 + Kelly 常驻

- 新增 `--observed-max`：以「今日已实测最高」截断并重命名分布（省略时自动读 `--watch` 日志）
- 新增 `--mu`：手动覆盖点估计（午后雷暴时模式的日最高值不会实时下修）
- 新增 `--sigma`：给出**剩余总不确定性**，不再叠加集合距平
- 1/4 Kelly 列改为常驻，同时输出金额与占本金百分比；未给 `--bankroll` 时按 1000 USDC 并标注假设
- `decision_log.csv` 记录「公允 P vs 市场价 vs edge」，供结算后复盘

## v0.1.0 — 2026-09-07 · 首个可用版本

- 多模型（7 家）确定性融合 + ECMWF 集合，dressed ensemble 输出整数档公允概率
- 92 天回测自动校准 `bias` / `resid_sd`
- `--watch` 日内实况追踪，写 `intraday_log.json`
- `backtest.py` / `hitrate.py` / `analyze.py` / `diurnal.py` 校准与分析脚本

---

**v1.0.0 的门槛**：累积 30 笔带实际结算结果的复盘（`decision_log.csv` 填回实际档位），
并用真实命中率校准过自己的概率之后再发。
