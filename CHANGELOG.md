# Changelog

语义化版本 `MAJOR.MINOR.PATCH`：

- **MAJOR** — 结算口径 / 档位定义 / 核心算法改变，旧结论不再可比
- **MINOR** — 新增功能或参数（向后兼容）
- **PATCH** — 修 bug、文档、性能（**不改变任何数字**）

每次推送 GitHub 前必做（见 `SKILL.md`「发版流程」）：
递增版本号 → 在下面加一条 → 同步**四处**：`README.md` 顶部版本行、
`SKILL.md` frontmatter 的 `version:`、`scripts/hk_edge.py` 的 `VERSION` 常量、
以及**本文件下面的「当前版本」行**（最容易漏的一处）。

当前版本：**v0.23.21**（`py -3 scripts/hk_edge.py --version` 可查）

## v0.23.21 — 2026-09-25 11:54｜网格口径必须固定 `forecast_days` + σ 必须与网格源配对（§2.9 纪律 11–12，PATCH）

**本轮改正了 v0.23.20 的两个错误结论**（v0.23.20 作废的是其数字，不是其规则精神）。

### 1. 纪律 11：`forecast_days` 会切换 seamless 链的分量

同一坐标、同一时刻、同一份 7 模式列表，仅改 `past_days`/`forecast_days`：

| 请求参数 | 9/25 七模式均值 |
|---|---:|
| `past_days=16&forecast_days=2` | **30.586** |
| 技能内 `forecast_days=10` / `past_days=92` | **30.314** |

均值差 **0.27 °C**，单模式最大差 **0.8 °C**（gfs 29.9 vs 29.1）。手写脚本自拟参数串 = 台账污染。
**校验法**：`OM_FC` 与 `OM_PREV` 对同一目标日必须给同一个数（今日两者均 30.314）。

另：HKO 日高真值必须走 `l_cond_check.hko_month("202609")`（`YYYYMM` 格式）；
`2026-09` 会静默返回空表，易被误读为「该月无数据」。

### 2. 纪律 12：σ 必须与网格源配对

同源网格下 n=13 clean 的三个回归：A `final~grid` resid 0.525 / **LOOCV 0.613** → 32.08；
C `final~rm11+grid` resid 0.512 / LOOCV 0.646 → 32.23；B `final~rm11` resid 0.772 / LOOCV 0.853 → 32.59。

- v0.23.20 报的 **σ 0.50–0.52 是在另一份网格源上算的，跨源引用作废**；本轮同源口径为 **0.61–0.65**。
- ⚠ in-sample resid 排序与 LOOCV 排序可以相反（本例 A 的 in-sample 残差**大于** C，LOOCV 却**小于** C）→ **选口径用 LOOCV，报口径用 in-sample，两者都写。**

### 3. 纪律 10 追加第 4 条：收敛判据必须有稳定性要求

v0.23.20 的「C 32.28 ≡ 市场二阶矩 32.28 → 收敛」在 **13 分钟后失效**（市场中心 32.86）。
**市场中心盘中来回摆动（11:34→11:53 在 32.6–32.9 间往返两次），用它印证我方口径等于用噪声确认信号。
收敛判据必须要求该独立口径在 ≥15 分钟窗口内稳定，否则只能记为「瞬时巧合」。**

### 4. 本轮中心口径重分裂

条件化三口径 **32.08 / 32.23 / 32.29**（A / C / `R@11|gap`）；非条件化两口径 **32.59 / 32.81**；
市场 **32.72**。条件化口径收敛，非条件化与市场同一侧。
关键量：今日 `gap = 网格日高 − 网格@11:00 = **0.47**`（历史 mean 1.14 / sd 0.51），
`corr(gap, R) = +0.495`、**`corr(rm11, R) = −0.246`**（早盘越暖，剩余升幅越小）。

**33 档三条独立计数口径都把上界封在 20% 附近**（Δ≥2.69 为 0/13；网格≤30.4 的 7 天最终值全 ≤32.2；
从 11:30 实测 30.8 还需再升 2.2 °C），而市场付 32.5%。
**31 档 0/13（95% 上界 20.6%）**，市场付 4.0% —— 模型口径的 27–33% **不得直接引用**。

⚠ **流动性是唯一真实约束**：两腿可成交深度 20 分钟内摆幅 273→29 份（YES31）、161→581→53 份（NO33）。

## v0.23.20 — 2026-09-25 11:35｜中心须同时条件化「早盘水位」与「网格日高」+ 收敛/发散读数规则（§2.9 纪律 10，PATCH）

9/25 11:32：今日网格日高 **30.19** vs 9/24 **30.20**（差 −0.01，网格被同批 run 重算收平），
而 `rm@11` **30.5** 比 9/24 的 29.6 高 **+0.9**。三个回归（n=14，同日同源）：

- A `final ~ grid`：resid_sd 0.739 → 今日 **31.89**（丢掉早盘信息，偏低 0.39）
- B `final ~ rm@11`：resid_sd 0.926 → 今日 **32.76**（偏高 0.48）
- C `final ~ rm@11 + grid`：resid_sd **0.688** → 今日 **32.28**

**纪律：**
1. 至少并列 A 与 C；两者差 > 0.3 °C 时必须写明差异来源（今日早盘相对昨日同刻的偏移），不得只报其一。
2. ⚠ C 相对 A 的残差改善**不显著**（F ≈ 1.70, df 1/11, p ≈ 0.22）→ **C 是并列口径，不是替代**。
3. **多口径收敛才是强信号，发散时必须报双峰，不得取中位。** 判据为是否存在**独立数据源给同一数值**：
   本轮 C（32.28）与市场全报价二阶矩中心（32.28）同值 → 收敛，可出单峰；
   上一轮条件化 32.13–32.35 vs 非条件化 32.65–33.10 差 0.5 °C 且无第三口径 → 必须双峰。

## v0.23.19 — 2026-09-25 11:20｜R@anchor 必须与「网格剩余升幅 gap」同时条件化（§2.9 纪律 9，PATCH）

9/25 11:00 锚下，无条件 `R@11` = +2.15 与条件化 `R@11` = +1.64 相差 0.5 °C（跨整数档），
而**两者残差 sd 几乎相同**（0.930 vs 0.915）。根因：当日 `gap = 网格日高 − 网格@11 = 0.51`，
仅为历史均值 1.49 的三分之一 —— **网格 12:00 见顶（9/24 是 14:00），相位提前 2 小时**。

六个条件化估计（Δ 台账 / `R@11|gap` / `R@10|grid` / `R@10:20|grid` / 市场二阶矩中心 / 逆方差合并）
全部收敛于 32.13–32.35；三个非条件化估计（`R@11` 无条件 / `rm@11` 分箱 / 市场 LSQ）全部高出 0.3–0.8。

**纪律要点**：① 报 `rmax + R` 前先算并打印 `gap`，偏离均值 >0.5 时无条件值与条件化值并列；
② ⚠ **不得因「条件化更精细」就只报条件化值** —— 本例条件化只降残差 sd 1.6%，却挪动点估计 0.5 °C，
「高杠杆 + 低信息」时正确输出是**双峰混合分布**，不是取中位；③ `rm@anchor` 分箱必须与网格日高同时条件化；
④ 与硬规则 23-二 配合（23-二 管网格绝对水平，本条管相位/gap）；
⑤ **`fnd` 整数值不是当日硬顶** —— 同日 11:00 官方发出 WHOT 且 `flw` 改「Very hot during the day」，
而 `fnd` 仍写 32；nowcast 类信号与 `fnd` 矛盾时以 nowcast 为准。

## v0.23.18 — 2026-09-25 10:45｜网格跨日比较纪律（§2.9 纪律 8）（PATCH）

**新增 §2.9 纪律 8：跨日比较网格「谁更热」，必须看整条逐时曲线，不能只看早盘领先幅度。**

2026-09-25 10:40 实测（同一次 `past_days=3` 拉取，7 模式均值逐时）：
9/25 在 10:00–12:00 领先 9/24 达 **+0.52~+0.73**，但 **14:00 起转负（−0.20）**；
两日逐时均值峰值几乎相同（**30.11 vs 30.13**），差别只是今日**峰值提前 2h**（12:00 vs 14:00）。
`daily=temperature_2m_max` 口径：9/25 = 30.27、9/24 = 30.20，**只差 +0.07**。

→ 只看早盘领先幅度会把「相位提前」误读成「全天更热」，直接错 0.5 °C 以上（跨整数档）。
→ 纪律同时明确：**日高一律取 `daily=temperature_2m_max`**（相对时口径差 0.1~0.2）；
逐时曲线只用于判**相位与形态**，不用于取日高。

同批实测另有两条口径确认（写入 `MEMORY.md`，不改代码）：
1. 市场「中心」有三个口径 —— `LSQ 拟合 μ`（9/25 10:40 = 32.70）、`逐档报价二阶矩中心`（= 32.16）、
   买方期望中心。**逐档中心才可与「我方 μ」直接比**；LSQ 与逐档中心之差（0.54）= 右尾被正态拟合折进中心。
2. **同向腿的平衡 μ 若重合，则它们是同一个观点的两种表达，不是两笔独立仓**：
   9/25 10:40 NO33 平衡 μ 32.63、YES31 平衡 μ 32.56（相差 0.07）→ 只按组合口径控总额与最大损失。

## v0.23.17 — 2026-09-25 09:50｜σ 标定纪律（§2.9 纪律 7）+ NO 腿配对口径修正（PATCH）

**新增 §2.9 纪律 7：σ 必须由「网格→实测」回归的 LOOCV RMSE 标定，不得沿用先验 0.70。**

2026-09-25 09:34 实测（n=15 干净日，剔 9/14 对流，网格与 Δ 同一次拉取）：

```
actual = 4.78 + 0.903 · grid        R² = 0.652      resid_sd = 0.51
LOOCV RMSE：bw 0.4 → 0.500 | 0.7 → 0.515 | 1.2 → 0.547 | 2.5 → 0.617
```

- 经验标定 **σ = 0.50 ~ 0.52**，与市场自拟合 0.51 吻合；长期沿用的 0.70 偏宽约 0.2。
- 后果：同一 μ 下众数桶概率 49.8% → 56.2%。`NO 32`（买价 0.38，平衡 σ ≈ 0.42–0.53）
  由 **+32% 降到 +15%**，且 σ 的 ±0.10 不确定度跨过平衡点 → 判为**不稳健**；
  `NO 33` 对 σ 不敏感（+39% → +27%），是 **μ 赌**，平衡 μ ≈ 32.72。
- 配套修正：`NO_k` 赢的条件是**终值 ∉ [k, k+1)**（双尾都赢），
  故 `NO 32` 与 `NO 33` **不是嵌套**，而是**有界对冲**（各 1 份成本 1.08、最低赔付 1.00）。

**纯文档改动，不改变任何工具输出数字。**

## v0.23.16 — 2026-09-24 21:35｜9/24 结算回填（32 档）+ 9/23 转 confirmed（结算回填，PATCH）

- **2026-09-24 结算 32°C（provisional）**。三源互证：① 结算站官方 **Max Since Midnight = 32.2°C**
  （14:00 设定，14:00–16:40 全程稳定 32.2；`obs_1min_log.json` 10 分钟快照亦 32.2 @14:10/@15:20）
  → 落 `[32.0, 33.0)`；② 收盘后 `market_prices.py 2026-09-24 --depth 3`：**32 档 YES bid 0.999**、
  其余 11 档 NO 0.999（24h 量 100.8k）；③ 当日 14:30 复盘判定 32 档（96%）。
  官方 Daily Extract 尚未出 09-24 → 标 `provisional`。
- **2026-09-23 转 confirmed**：官方 `dailyExtract_202609.xml`（09-24 21:32 复核，**必须带 UA**）已出到
  **09-23 = 31.9 → 31 档**，与 provisional 推定逐位一致 → 该日 1 腿由 provisional 转 confirmed，
  **金额一位未变**（−$71.38）。
- **台账 09-24 唯一腿 = 09-23 的 D-1 实做腿**（NO32 @0.550 / $87.45）→ 结算 32 → **输 −$87.45（−100%）**。
  当日窗口内（10:30–12:00）**零新腿**：10:10 / 10:30 / 11:00 / 11:40 / 12:39 五轮均为「持有 NO32、不加不平」，
  12:39 起入 13:20 硬禁区、零执行措辞。
- **累计**：40 腿 / 12 个结算日 → **41 腿 / 13 个结算日 / 15-26 / 投入 $4,040.19 / 回收 $2,773.97 /
  净 −$1,266.22 / ROI −31.3% / 6 盈 7 亏**；NO 21 腿 12-9（−8.4%）、YES 20 腿 3-17（−79.9%）。
- **口径 10 第八个校准点（方向 = 排除即减亏）**：被排除的第二条 D-1 腿 YES31 @0.280 / $96 若入账，
  结算 32 → 亦输 −$96.00 → 本口径等于**少亏 $96.00**。前七个校准点方向不定，本次首次出现「排除 = 减亏」。
- **代码零改动**，只回填结算数字与文档（CSV / PNL.md / 本文件）→ PATCH。

## v0.23.15 — 2026-09-24 16:40｜历史网格重算导致 Δ 台账口径漂移（§2.9 纪律 6）

- **新增 §2.9 纪律 6**：Open-Meteo 的**历史格点会被后续 run 重算上修**，
  与目标日的上修同幅 → Δ（实测 − 网格）在两侧同步收窄，**中心估计基本不动**。
- 2026-09-24 实测：15:16 与 16:30 两次拉取，9/25 网格 29.97 → 30.20（+0.23），
  同期 9/24 网格 29.86 → 30.07（+0.21），近 5 晴热日 Δ̄ 2.20 → **2.05**，推出 μ 32.17 → 32.25。
- **纪律**：一次 `past_days=16` + `forecast_days=3` 在同一响应内配对算 Δ；
  引用 Δ 必须标注拉取时刻；**不同时刻的 Δ̄ 不得相减当天气变化**。

## v0.23.14 — 2026-09-24 12:45｜R@12 台账重建 + 市场小支撑集 σ 作废 + NO 腿双尾性质（全文档）

- **硬规则 23 补**：新增 **R@12 台账**（n=13，`forecast_log.jsonl` 逐日精确重建）
  `0.0/0.0/0.7/0.7/0.9/1.5/1.6/1.8/1.8/1.8/1.9/2.3/2.4` → 全样本 mean 1.338 / median 1.60;
  **剔对流日 n=11 → mean 1.582 / median 1.80 / sd 0.561**。自洽校验 R@13 0.631、R@14 0.200。
  **修正**此前记忆中的近似值（mean 1.200 / median 1.500）。
- **硬规则 24 补**：**支撑集 ≤3 桶时 LSQ 拟合 σ 一律作废**（9/24 12:39：3 桶 → σ0.42，
  而全报价二阶矩 sd 0.569，把 `maxP` 从 62.0% 虚抬到 76.6%，直接污染硬规则 32 的 σ 开关）。
  强制报 σ 格式：`拟合 σ（支撑集 n 桶）｜全报价二阶矩 sd`。
- **硬规则 32 补**：**NO_k 在双尾都赢**（终值 <k 或 ≥k+1）⇒ 它是**方差下注**而非方向下注，
  只有 μ 落进 k 档才归零；「σ 塌缩必杀 NO」表述错误。**临界点必须做金额换算**
  `P(k) = 1 − 回收/结算`，禁止用固定百分比止损（9/24 NO32：临界 72.0%，七口径 46–70% 全低于临界，
  −32.7% 与 −49.1% 两次触及机械止损线而两次都是持有更优）。

## v0.23.13 — 2026-09-24 11:45｜修复跨天轮转把昨日轨迹静默覆盖（代码）

- **根因**：`_load_1min_log()` 在 `date != today` 时返回 `{}`，使调用处的轮转判据
  `log.get('date') != iso[:10]` **恒为 False** → 紧随其后的 `_archive_1min(log)`
  是**死代码**，昨日 `points` 被 `json.dump` 直接覆盖。
- **后果（已发生）**：9/23 的 43 点 1min 结算站轨迹**永久丢失**；
  `obs_1min_archive.json` 长期只有 09-10 一天 → **P2-3 站点口径尾档重建被卡死**
  （剩余升温表尾部比站点薄 4.7 倍，攒站点序列是唯一解法）。
- **修复**：① `_load_1min_log(raw=True)` 绕开跨天作废，轮转路径改用它；
  ② `intraday_log.json` 同源缺陷一并修 —— 新增 `_archive_intraday()`，按日归档
  到 `data/intraday_archive.json`（含**全部站点**读数，是硬规则 19 跨站两步换算
  与「昨日同刻对照法」的逐时档案）。
- **验证**：隔离测试（`DATA` 重定向到临时目录）—— 造昨日 log → 触发轮转 →
  archive 正确出现昨日键、新 log 正常开当日，`raw=False` 仍按预期作废。
- **部分代偿**：`forecast_log.jsonl` 保留 9/23 整十点 `rm` 序列（39 条），
  整十点粒度的「昨日同刻对照」仍可用；1min 瞬时序列不可恢复。

## v0.23.12 — 2026-09-24 10:45｜SKILL.md 新增硬规则 32（单腿双开关，文档，代码零改动）

- **任何单腿必须同时报「μ 盈亏平衡点」与「σ 临界」**：买 NO_k @q 的 μ_break 解
  `P(k) = 1 − q`；σ_crit 解 `maxP(σ) = 2Φ(0.5/σ) − 1 = 1 − q`。缺一无法判存活。
- **判据补全**：若 `maxP(σ_当前) ≤ 1 − q`，则该腿**对 μ 免疫**（任何中心偏移都杀不死），
  报告须明写；只有 σ 能杀 → 必须同报「当前 σ 距 σ_crit 多远」。
- **9/24 NO32 @0.55 实测**：μ_break = **32.3**（当前 31.9，P(μ≥32.3)≈13%）；
  σ_crit = **0.662**（LSQ 0.63 已破、二阶矩 0.75 与我方 0.80 守住）。
- **新纪律：边际压缩速度也是信号** —— 9/24 上午 3 小时内 NO32 的 EV +38% → +18%，
  公允 P 仅动 5pp，差额全由价格追赶贡献。**边际砍半即划掉「加仓」选项，即使 EV 仍为正。**
- 引 σ_crit 时必须并列该拟合的 RMS 与单桶残差占比（与硬规则 24 衔接）。

## v0.23.11 — 2026-09-24 09:10｜SKILL.md 补早盘 L 台账（文档，代码零改动）

- **§2.2 L（水位）新增「9 月实测 L 台账」**：L ≡ 实测日峰 − 7 模式均值网格日高，
  9/14–9/21 n=8 mean **+1.78** / median +1.84 / sd **0.64**；含 9/23（+2.67）n=9 mean **+1.88**。
- **明确早盘口径**：08:00–11:00 是 R 锚点（11:00 起）与引擎 bias（9 月系统性偏低
  0.4–1.5°C）之间的空白段，**唯一可用口径是 L 水位**；用法
  `l_integrate.py --hour <h> --l-hat <近期L>`，**必须显式覆盖 `--l-hat`**
  （缺省吃引擎 bias +0.99；9/24 实测默认给中心 30.88，`--l-hat 1.88` 给 31.38）。
- 两条边界写入：**sd 0.64 → 只够判「μ 落在整数分水岭哪一侧」，不能定档**；
  **L 无单调趋势**（最低值 +0.37 出现在对流暴雨日），不得外推为「L 在上升」。
- 注：曾拟新增 `scripts/l_level.py` 做该台账外推，**因 `l_integrate.py` 已覆盖同一口径而废弃**
  （不引入重复工具，责任边界保持单一）。

## v0.23.10 — 2026-09-24 08:55｜新增 `--no-log`（修台账污染）

**`hk_edge.py` 新增一个参数，不改任何概率数字。**

- **问题（2026-09-24 实测）**：`decision_log.csv` 写盘**无条件执行**——每跑一次带 `--market` 的
  运行就追加一轮 6 档。做 μ/σ 情景扫描（同一时刻跑 3 组参数）会写入**同键多值**
  （`logged_at` + `target_date` + `bucket` 相同、`fair_p` 不同），
  这正是 09-23「口径 10」第六个校准点的同键顶替风险的批量版本 ——
  事后 Brier 复盘按行序取首推，会把情景腿当成真实留档腿。
  当日实测：三次情景运行写入 18 行，手工删 12 行才复原。
- **修法**：`--no-log` 跳过写盘并打印 `[--no-log] 本轮为情景/敏感性扫描，未写 decision_log.csv`。
  **纪律：任何情景 / 敏感性 / 口径对照扫描必须带 `--no-log`；只有「主力口径的首次留档」才写台账。**
- 验证：`--no-log` 运行后 `wc -l decision_log.csv` 保持不变（1290 行）。

## v0.23.9 — 2026-09-23 21:30｜台账 09-23 首度入账 + 09-22 转 confirmed

**台账与文档改动，`hk_edge.py` 代码零改动。**

- **09-23 结算档 = 31 档（provisional）**：结算站官方 Max Since Midnight **31.9**（14:10 定格，
  `watch.log` 1 分钟口径 14:10–16:40 全程稳定记 31.9）→ 落 [31.0, 32.0)；收盘后市场
  **31 档 YES bid 0.999**、其余 11 档 NO 0.999（`market_prices.py 2026-09-23 --depth 3`，24h 量 130.0k）。
  官方 `dailyExtract_202609.xml`（09-23 21:35 复核）**仅出到 09-22** → 不转正。
- **09-23 入账 1 腿：买 NO31 @0.430 / $71.38 / 166 份（11:08 窗口内，当日唯一执行腿）→ 输 −$71.38（−100%）**。
  峰值 31.9 落 31 档，NO31 归零；收盘 bid 0.020（名义回收 $3.32）。
- **09-22 转 confirmed**：官方 XML 已出到 **09-22 = 30.2 → 30 档**，与 provisional 推定逐位一致，
  该日 1 腿金额一位未变（+$38.10）；PNL.md 记分牌去掉该行 `*` 星号。
- **口径 10 第六个校准点（新增，逐条留档于 CSV 文件头）**：09-23 首次出现**「窗口外腿与窗口内腿同键」**——
  09:18 留档 NO31 @0.680 $56 与 11:08 入账 NO31 @0.430 $71.38 **同为 NO31 / NO / 31**。
  照录时按行序取首推，会让入账腿被 09:18 那条顶替：**亏额由 −$71.38 收窄到 −$24.00（3 腿 1-2，投入 $240）**，
  摆动 **$47.38**，方向与 9/22 的「排除 = 少赚」相反。被排除的另 3 条（09:18 YES30 @0.380 $22、
  11:14 NO31 @0.440 $35 加仓、14:31 YES31 @0.750 名义 $162 而真实可成交仅 29 份）已逐条写入 CSV 头部。**口径 10 仍待拍板。**
- **09-23 三条归因已写入 SKILL / MEMORY**：① 硬规则 30（对流日冻结 Δ）建议废弃 —— 当日 Δ=+3.00
  （7M 网格日高 28.90）破 9 月纪录（前高 +2.46），冻结代价 = 中心低估 1.41°C；② 报告内部口径未调和 ——
  R4 连续正态 P(≠31) 53–63% vs 同报告离散计数 P(31)=53.4%（反推 46.6%），差 16–21pp，而盈亏平衡 0.570 下真实边际仅 3.6pp；
  ③ σ 内生塌缩 —— 11:35 报 P(31)=38%，实况 14:10 定格 31.9 → 实际 ≈96%。
- 累计：**40 腿 / 12 个结算日 / 15-25 / 投入 $3,952.74 / 回收 $2,773.97 / 净 −$1,178.77 / ROI −29.8% / 6 盈 6 亏**；
  NO 20 腿 12-8（−5.4%）、YES 20 腿 3-17（−79.9%）。

## v0.23.8 — 2026-09-22 21:30｜台账 09-22 结算状态改回 provisional（无数字变更）

**纯状态与文档改动，不改任何算法、口径或输出金额。**

- **09-22 结算档 30 档**（结算站官方 Max Since Midnight 30.2 于 11:50 设定，之后一路回落至 28.2
  雷暴冷池；收盘后市场 30 档 YES bid 0.999、其余各档 NO 0.999，24h 量 183.7k）。
- **`settle_status` 由 `confirmed` 改回 `provisional`**：官方 `dailyExtract_202609.xml`
  （09-22 21:35 复核）**仍只出到 09-21 的 33.0**，按「官方未出即 provisional」口径不得转正。
  该行金额（+$38.10）与档位（30）**一位未变**，仅 PNL.md 的记分牌加回 `*` 星号。
- 顺带核对：**09-21 = 33.0 已由官方源确认**（与 provisional 推定值一致），该日 1 腿转正、金额不变。
- 累计口径不变：**39 腿 / 11 个结算日 / 15-24 / 投入 $3,881.36 / 回收 $2,773.97 / 净 −$1,107.39 /
  ROI −28.5% / 6 盈 5 亏**；NO 19 腿 12-7（−2.8%）、YES 20 腿 3-17（−79.9%）。

## v0.23.7 — 2026-09-22 16:20｜新增 §5.0 全天复盘标准流程（六步）

**纯文档改动，不改任何算法、口径或输出数字。**

把 2026-09-22 复盘实际执行的流程固化成可重复的六步清单，写进 `SKILL.md` §5.0：

1. **定结算档** —— 官方 XML / `watch.log` 轨迹 / 市场自评三证互证。
2. **拉全天轨迹** —— `rmax` 序列 → 峰值时刻、回撤、`d_rmax`；先查采样起点。
3. **建「各口径 vs 实测」对照表** —— 这是复盘的骨架，9/22 靠它发现唯一命中的口径被自己否决。
4. **分「口径族」打 Brier** —— 全天汇总会把「引擎族领先 0.0858」与「合成族落后 0.0342」
   平均掉；⚠ 先剔除 floor 饱和时段行与 `lead ≥ 2 天`行。
5. **本腿诚实重算** —— 只用当时可得信息重算，差 >5pp 即写进复盘并在台账留痕。
6. **台账回填 + 本金归因** —— 记账口径摆动 >10% 本金即升级为「必须拍板」。

并附第 4 步的三条必备自检（结算是否官方确认 / 剔除后 n<5 / 当日是否 floor 饱和或冷池）。

## v0.23.6 — 2026-09-22 16:05｜落地硬规则 31（三）到代码：floor 饱和拒写 `decision_log.csv`；修空分布崩溃

**修改脚本逻辑（不改任何概率输出）。** 承接 v0.23.5 的文档规则，把当时标注的
「代码层待修」补上，并在回归测试中顺手挖出两个台账污染源。

**改动 1 — 饱和目标日拒写台账。** `scripts/hk_edge.py` 写台账前按
`results[t]['mu_saturated']` 分流：饱和目标日**整日拒收**并打印
`🚫 [硬规则26/31] 拒写 decision_log.csv：...`；`[已记录]` 行改为回显实际写入的目标日，
避免「拒了一条却显示已记录」的误导。

**为什么不拦会污染 Brier**：2026-09-22 14:40 轮台账写的是 `P(30档)=0.565`，
而同一轮报告给用户的是 **96%** —— 若不拦，当日午后 Brier 会显示
**模型 0.1097 vs 市场 0.0003**，台账将系统性记录一个从未对用户生效过的分布。

**改动 2 — 修 `floor` 高于分布支撑集时的崩溃。** 原 `max(r['probs'].items())`
在 probs 被清空时抛 `ValueError: max() iterable argument is empty`（已复现）。
现改为打印 `→ ⚠ 分布为空：已观测下限高于模型分布的全部支撑区间` 并继续，
不输出众数档 —— 现实中该情形意味着 `--observed-max` / `intraday_log.json` 的上限异常。

**回归测试（两个用例，台账已回滚）**：

| 用例 | 结果 |
|---|---|
| `--observed-max 40`（支撑集外） | 无崩溃 ✅、打印空分布警告 ✅、拒写 09-22 ✅ |
| `--observed-max 31.0`（饱和） | 拒写 09-22 ✅ |

**测试中发现的待拍板项（未改，需 Eason 定口径）**：
不带 `--date` 时 `targets` 取未来 7 天，而 `--market` 只描述一天 → 同一天的盘口价
被写进 09-23…09-28 六行 × 各档。现有台账里 **lead ≥ 2 天共 25 行**（lead=1 的 211 行
属正常 D-1 记录）。这些行的 `market_p` 与 `target_date` 不匹配，
`review_brier.py` 复盘前**必须先剔除 lead ≥ 2 的行**。

## v0.23.5 — 2026-09-22 15:55｜新增硬规则 31：剔离群点样本的频次报告 + 网格极值日回退 73 天 bias + floor 饱和禁写日志

**纯文档改动，不改任何算法、口径或输出数字。**

**触发事件**：2026-09-22 是「引擎口径唯一命中、却被自己两次否决」的一天。

| 口径 | 给出 μ | 误差 vs 实测 30.2 |
|---|---:|---:|
| **引擎（73 天 bias +1.00）** | **30.23** | **−0.03** |
| 近期 Δ 口径（+1.88） | 31.09 | +0.89 |
| 逆方差合成（当时实际采用） | **31.40** | **+1.20** |
| 市场隐含（D-1） | 32.15 | +1.95 |

**（一）剔除动作本身制造选择偏差。** 硬规则 6 的「近期 Δ」样本剔掉了 9/14 冷池日：
剔后 mean 1.88 / sd **0.38**；**放回 9/14** → mean 1.68 / sd **0.78（翻倍）**。
被剔 regime 在 12 天窗口内出现 **2 次（9/14、9/22）= 17%**。
→ 新规：被剔日、频次、折算频率必须同报；**频率 ≥ 2/窗口 时该样本不得作点中心**，
且硬规则 6 的「Δ 胜过 73 天 bias」在该频率下**不适用**。

**（二）硬规则 30 的门控全是反应型，拦不住要拦的错误。** 2026-09-22 10:25（唯一建仓时点）时
无 WTS、0 站有雨、回撤 0、峰值时刻未知 —— 四条门控无一可触发。
唯一事前可见的信号是**网格处于样本极值**（29.53 = 12 日最低）。
→ 新规：网格日最高处于样本极值时，`Δ`/`R` 家族（含硬规则 29-2 的条件回归）降级为上限，
主判回落 73 天 bias。本日回归给 `R̂ = 2.31`、实测 R = **0.7**，仍高 1.6。

**（三）floor 饱和的运行被自动写进 `decision_log.csv`。** 14:40 轮记录 `P(30档)=0.565`，
而同一轮报告给用户的是 **96%**（差 39.5pp）。该分布已被硬规则 26 判「整段作废」。
→ 新规：检测到 `mu_saturated` 时不得写日志（**已于 v0.23.6 落地到代码**，见上）。

**未改动任何脚本逻辑；`hk_edge.py` / `market_shape.py` 逐位不变。**

## v0.23.4 — 2026-09-22 14:45｜新增硬规则 30：对流/雷暴日必须冻结 `Δ` 口径

**纯文档改动，不改任何算法、口径或输出数字。**

**触发事件**：2026-09-22 首次出现 `Δ = 站点日最高 − 7Mmd日最高` **跌破样本最小值**。

| 口径 | 值 | 与实测（30.2）之差 |
|---|---:|---:|
| 引擎 `7Mmax + bias(1.00)` | **30.56** | **−0.36** |
| 近 10 日 Δ 口径 `+1.88` | **31.44** | **−1.24**（跨一整档） |

10 日干净样本 mean 1.88 / sd 0.38 / **min 1.27**；今日实际 **Δ = 0.64**（−3.3σ）。
当日 WTS 生效、4 站雨量 > 0、早峰（11:50）、最大回撤 2.0 °C ——
与 09-14（Δ = −0.39，冷池+雷暴）同型。

**新规**：满足「WTS/暴雨警告生效或雨量站 ≥1 且 >2mm」「最大回撤 ≥1.0 °C」「峰值 ≤12:00」
任一 → **冻结 Δ 口径**，改用「引擎 73 天 bias + 实况截断」。
这是硬规则 6 的**第一次符号反转**（那里说引擎近 10 日偏冷 0.86），
说明两版谁赢取决于**天气型**而非窗口长短。

**未改动任何脚本逻辑；`hk_edge.py` / `market_shape.py` 逐位不变。**

## v0.23.3 — 2026-09-22 10:45｜新增硬规则 29：核心桶子集拟合禁止外推支撑集尾部 + `R` 与网格水平的相关性量化

**纯文档改动，不改任何算法、口径或输出数字。**

**（一）核心桶子集拟合的适用边界 —— 一条被误用了一天的方法**

v0.23.2 把「只喂核心桶做子集拟合、用其 RMS 判市场主体形状」写成了正例。2026-09-22 两批盘口显示：
**它只能读支撑集以内的桶，不能外推尾部**。10:25 那批被读成「市场自相矛盾 ≈15pp」并据此建仓，
但同批的全档拟合给 12.4%、盘口自己给 17.5% —— 真实缺口只有 5pp，15pp 全部来自薄子集外推。
10:36 两法收敛（9.2% vs 9.7%）证明「形」是准的、只有支撑集外那一截尾不可用。
新规：子集拟合的输出只在桶集内引用；报 `P(<最低桶)` / `P(>最高桶)` / 「距边界 g 度」一律用全档拟合。

**（二）`corr(网格日最高, R) = 0.842`（n=9）**

`R = 峰值 − rmax@10:20` 的 2.90 中位是被高网格日撑起来的；低网格日回归只给 2.10。
这是硬规则 23「天花板效应」的反向一半：**低基准日同样不许用无条件 R**。
新增条件化 R̂ 与无条件中位并列报的要求。

**未改动任何脚本逻辑；`hk_edge.py` / `market_shape.py` 逐位不变。**

## v0.23.2 — 2026-09-22 10:35｜修正 market_shape.py 的 RMS>3% 分支崩溃 + 「市场自称核心形状」内部一致性检验

**修 bug（不改任何数字）**：`scripts/market_shape.py` 在 `RMS > 3%` 时抛
`TypeError: not enough arguments for format string` —— 告警文案里有裸 `74%`，被 `%` 格式化当成转换符。
该分支自 v0.22.1 加入以来**从未跑通过**，等于「RMS 超标时脚本直接崩」而看不出原因。
修法：转义为 `74%%`。**正常路径（RMS ≤ 3%）输出逐位不变。**

**本轮（2026-09-22 09-22 盘中）新确立的一条判读方法**：
`market_shape.py --market` 支持**只喂核心桶**（剔除异常低档）做**子集拟合**，用它的 RMS 判断
「市场主体形状是否可用」。9/22 实测：全 7 桶拟合 μ 31.13 / σ 1.09 / **RMS 3.10%（不可用）**；
剔除 29 档后（30–35 六桶）μ **31.40** / σ **0.77** / **RMS 1.05%（可用）**，唯一主导残差是 33 档（占 76%）。
→ 读法：**市场主体（30–35）自洽，低档报价（29 档 mid 17.5–18.5% vs 其自身形状隐含 3%）才是错位处**。
这条比「全 7 桶 RMS>3% ⇒ 形状不可用」更有信息量，且**不依赖「我方模型更准」**（§3.4 内部一致性族）。

**本轮不涉及算法 / 口径 / 任何输出数字的改动。**

---

## v0.23.1 — 2026-09-21 21:30｜台账结算 09-21（+$108.92）+ 09-18/19/20 转正 + 口径 10 第三个校准点

**改动类型**：只回填结算数字与文档（CSV / PNL.md / SKILL.md），`hk_edge.py` 与 `market_shape.py` 代码零改动 → PATCH。

**一、09-21 结算档 = 33°C（provisional）**
- 判据 ①：结算站官方 Max Since Midnight 于 **15:43 触及 33.0**（`watch.log` 1 分钟口径 15:50–16:40 稳定记 33°C；
  `obs_1min_log.json` 10 分钟快照最高 32.9）→ 同落 `[33.0, 34.0)`。
- 判据 ②：收盘后市场 **33 档 YES bid 0.999**、其余各档 NO 0.999（`market_prices.py 2026-09-21 --depth 3`，
  事件 12:00 UTC / 20:00 HKT 收盘，24h 量 93.6k）→ 已定档。
- 官方 `dailyExtract_202609.xml` 尚未出 09-21（21:30 复核只到 09-20）→ 维持 `provisional`。

**二、09-18 / 09-19 / 09-20 由官方 XML 确认并转正（**输出数字不变**）**
- 官方值：**09-18 = 31.7 → 31、09-19 = 33.2 → 33、09-20 = 33.1 → 33**，
  与三日 provisional 推定值**逐位一致** → `--settle 2026-09-18=31,2026-09-19=33,2026-09-20=33 --write`
  回填 **13 条**（档位/胜负变更 **0 条**，仅 `settle_status` 写入）。09-18 / 09-19 为无腿日，无腿可翻转。
- 至此**全表已无 provisional** 除 09-21 一处。

**三、09-21 台账：唯一腿 NO32 @0.520 / $118 / 227 份 → 赢 +$108.92（+92.3%）**
- 10:40 窗口内（10:30–12:00）本日唯一执行腿（会话日志明写「本日唯一」）。结算 33 档 → `NO32` 赢。
- **「伪安全 NO」族首次取胜**（此前 4 战 0 胜）。但复盘定调为**运气兑现、不是判据兑现**：
  建仓时 rmax 仅 30.4、当日最终走 33.0，**赢面落在「≥33」那一支而非「封顶」那一支**
  （10:40 的 μ32.8 本身低 0.2°C）。→ 该族「仍需在『上侧没有大涨』的日子里再验一次」。
- 11:00 / 11:45 / 12:05 / 12:55 / 13:15 / 14:30 六轮均为「持有（不加不平）」或「零可执行机会」→ 无新腿。

**四、口径 10 出现第三个校准点，且方向与 9/17 / 9/20 相反（**需 Eason 拍板**）**
- 9/21 会话日志自己写明排除：00:20 隔夜轮「留档腿标研究观察 非执行指令」+「**10:30–12:00 重跑后才是台账口径**」；
  08:55 轮「标研究观察、**不入台账**」。
- 本次据此**只入账窗口内那 1 条**，被排除的 4 条留档（00:20 的 YES31 $146 / YES30 $12 / NO32 $90、
  08:55 的留档）**未建腿**，清单已逐条写进 CSV 文件头，**补 4 行即可复算**。
- **三天三种做法**（9/17 照录、9/20 照录、9/21 排除）→ 若不拍板，累计值会随「当天有没有人手写一句不入台账」漂移。
  本次的选择理由：自动化只能读到结构化流水，读不到未写下来的意图，故服从会话的明写声明。

**五、CSV `note` 半角逗号陷阱第三次触发（已修，写入 SKILL 补强警示）**
- 新增行 note 写了 `峰值 ∉ [32,33)` → `csv.DictReader` 撑出第 11 列、`note` 被静默截到「峰值 ∉ [32」，
  紧接着的 `--settle --write` 就把截断**固化**（v0.17.0 已写明此坑，本次仍踩中）。
- 修法：整行重写、区间改 en dash（`[32–33)`），并复查**全部**数据行 `列数 == 10`。
- SKILL §5.2 该警示补一条硬性步骤：**新增行后、跑 `--settle --write` 之前必须先做列数复查**
  （`--write` 会就地固化错误，截断/丢字不可逆）。

**六、累计（首推口径）**
- **38 腿 / 10 个结算日 / 14-24 / 投入 $3,681.36 / 回收 $2,535.88 / 净 −$1,145.48 / ROI −31.1% / 5 盈 5 亏**。
- 按方向：`NO` 18 腿 **11-7**（ROI −4.6%）；`YES` 20 腿 **3-17**（ROI −79.9%）。
- 错误族四族合计 **27 腿、胜率 3/27 = 11.1%**（伪安全 NO 由 4 战 0 胜改为 **5 战 1 胜**）。
- 盈利侧：9/21 10:40 的 NO32 是**第二条落在窗口内**的腿，与 9/20 的 YES33 合计 **窗口内腿 2-0**。
- 常规本金 1000 口径：累计 **−114.5%**；09-21 单日投入 **11.8%**（**09-09 以来最低**，回到敞口硬顶内）。
- `--open` 收口为空。

**七、双副本同步**
- 6 文件（`SKILL.md` / `README.md` / `CHANGELOG.md` / `PNL.md` / `scripts/hk_edge.py` / `scripts/data/rec_pnl_log.csv`）
  md5 两两一致；`git add` 明确列文件 → commit → push → `git ls-remote` 核对。


---

## v0.23.0 — 2026-09-21 14:45｜常规本金定为 1000 + 硬规则 28（薄簿报价翻转）

**⚠ 本版改变 `l_integrate.py` 的默认输出金额**（`--bankroll` 2500 → 1000）。
`hk_edge.py` 的默认值本就是 1000，本次只改提示语。SKILL.md 的规则与台账标注同步修正。

### 1. 常规本金 = 1000 USDC（硬规则 25 重写）
- Eason 2026-09-21 明确：「修改 skill 的常规本金为 1000（在当日没有另行提供指定本金的时候）」。
- **代码层**：`l_integrate.py --bankroll` 默认 **2500 → 1000**；`hk_edge.py` 提示语改为
  「按**常规本金** 1000 USDC 估算」（其默认值本就是 1000）。
- **规则层**：每轮必须写「本金 = X（来源：本轮给定 / 常规默认 1000）」，禁止无来源裸数字，
  禁止从历史预测 / `reviews/` 旧值 / 台账反推继承。用户当轮指定时用指定值。
- **台账按 1000 重算**（写入硬规则 25）：单日敞口硬顶（25–30%）已被突破 **3 次** ——
  09-09（**57.7%**，本次新查出）、09-16（91.2%）、09-20（**100.7%**）；
  **累计净亏 $1,254.41 = 常规本金 −125.4%**。
  口径：建议口径，非实际成交（实际成交量常为建议额 20–40%，符号与超限幅度不受影响）。

### 2. 新增硬规则 28：报「市场隐含」必须同报三项流动性证据
- 2026-09-21 12:15–12:51 同一小时内出现互为镜像的结论：32 档 mid 0.425 → 0.180 → **0.480**、
  33 档 mid 0.515 → 0.765 → **0.425**，反推 P(win) 摆动 **33pp**；
  而同期 **24h 成交量 55293.83 一字未动**、NO32 顶层深度仅 **19–33 份** → 是撤挂 / 换价，不是重定价。
- 三条必报：**① 顶层深度 ② 近 15 分钟 mid 变动 ③ 24h 成交量是否变动**。
  深度 <100 份 → mid ∈ 噪声；15 分钟 mid 变动 >0.15 → 判换价、市场口径不可用；
  成交量未变 → 无成交确认，一律降权。
- **例外**：`market_shape.py --date` 的**全档位**形状拟合（μ/σ/RMS）不受此限 ——
  它用全档中间价，RMS ≤3% 即可用（硬规则 24）；但**单档** mid 引用仍须过三问。

### 3. 禁止清单新增 #36 / #37
- **#36** 引用单档 mid 而不报三问（9/21 一小时内 P(win) 摆动 33pp，成交量一字未动）。
- **#37** 把「建议投入 / 常规本金 1000」写成 2500 口径的百分比。

### 4. §5.2 台账标注修正
- 9/20 行的「= 本金 −31.4%」（2500 口径）→ 「= **常规本金 1000 的 −78.4%**；
  单日建议投入为常规本金 **100.7%**」。

---
## v0.22.5 — 2026-09-21 14:40｜补硬规则 27：14:00 三锚（抖动 / 市场隐含 / 回归）+ 冷池标签的正确边界

**不改任何代码逻辑与输出数字**（纯文档补充）。

- **新增硬规则 27（一）**：`h ≥ 14:00` 用三条互相独立的轴定锚 —— A σ_flicker 开关、B `market_shape` 市场隐含 μ/σ、
  C 回归 `peak = −6.96 + 1.225 × rmax@14:00`（n=9，残差 sd 0.17）；D 热日分层（n=4）**只作保守下限、禁止定中心**。
  **关键对照**：14:00 回归斜率 **1.225 > 1**，而 11:30 的同型回归斜率仅 **0.664** ——
  前者不受天花板效应支配（冷凉日此刻已见顶、R 恒为 0），**两条斜率禁止互相套用**。
  同日 `R(rmax@14:00)` 全样本（n=10）均值 **+0.20** / 中位 **+0.15** / `P(R≥0.1)` = 60% / `R_max` = +0.5；
  **首算时把「当日未完成」的那一行算进了分布（n=11）**，剔后 n=10 —— 现列为发布前必查项。
- **新增硬规则 27（二）**：`d_rmax = 0` 与平台期。2026-09-21 rmax 12:20 触 32.8 后**平台 110 分钟**
  （`d_rmax` 一度到 10）→ 14:10 刷新至 32.9。**v40 与 `d_rmax` 在这段都会给错**：
  前者读「已过动能段」、后者读「接近锁定」。判据改为
  **`d_rmax` 大 **且** 网格已过峰 → 锁；`d_rmax` 大 **但** 网格仍在升（本例峰在 15:00）→ 只报区间。**
- **新增硬规则 27（三）**：**冷池标签 ≠ 已过峰**。同日最大回撤 1.2 °C 后**完全收复并创新高**；
  冷池规则只作「网格主表作废」的开关，不得解读为当日结束。
- **禁止清单新增 #34 / #35**（冷池读成见顶；11:30 与 14:00 斜率互相套用 / 用 n=4 分层定中心）。

## v0.22.4 — 2026-09-21 13:25｜补硬规则 16b：`d_rmax` 表缺 `g < 0.4` 行 + 高基准日 `gap` 分层

**不改任何输出数字**（纯文档补充，无代码逻辑变更）。

- **缺口**：硬规则 16 的危险率表最小门槛是 `P(R ≥ 0.4)`；但 `rmax` 贴近档位上界时
  （2026-09-21 13:15：`rmax = 32.8`，距 33.0 仅 **0.2**）要问的是 `P(R ≥ 0.2)` —— 表里没有这一行。
- **新增硬规则 16b**：`g < 0.4` 时**禁止**用 `P(R≥0.4)` 代替、**禁止**给单点；
  必须并列四口径（d_rmax 外推 / gap 分层 / 全样本 R 折减 / 市场隐含），只报区间。
  9/21 实测区间 **[0.22, 0.38]**（取 0.30）。
- **新增分层观察（n ≤ 2，只作方向锚）**：`R(rmax@13:00)` 按 `gap@13:00` 切开 ——
  `gap ≤ 0.6` 的 9/19 / 9/20 给 R = +1.0 / +0.8（都跨档）；`gap = 1.2` 的 9/15 给 R = 0.0。
  **高基准日里「已明确回落」与「仍在峰位」是两种形态，不可混用**；样本极小，
  只能把无条件的 `R̄` 往下调，不能单独定中心。
- **同日记录**：引擎首次出现「硬规则 26 floor 饱和 + 冷池判定」**双作废同时触发**
  （原始估计 31.33 < 已观测 32.8；最大回撤 1.1°C）→ 档位概率整体弃用，主判改走同刻实证 R + 市场隐含。

## v0.22.3 — 2026-09-21 12:55｜`--watch` 路径补 floor 饱和告警（硬规则 26 的漏网口）

**缺陷**：v0.22.2 把 floor 饱和检测加在了 `bucket_probs()` 与 `--date` 主输出上，
但**盘中实际使用的 `--watch` 路径另有一条独立打印分支**，未接同一告警。
2026-09-21 12:40 轮 `--watch` 输出「⇒ 今日峰值估计 32.80°C（下限 = 已观测最高 32.8°C）」
—— 该值与已观测下限逐字相同，即典型的 floor 饱和，却**不打 🚫**，
容易让使用者把「引擎认为不再升温」误读成一个有效中枢。

**改动**（不改变任何数字，纯告警）：
`scripts/hk_edge.py` §watch 分支，在峰值估计打印后加判据
`pred <= intraday_mx + 1e-9` → 打印 `🚫 [硬规则26] 点估计被 floor 饱和`。

**教训**：同一条规则若在两处独立打印路径实现，必须两处都接。
后续新增诊断告警时，先 `grep` 该指标的**全部** print 点，逐一挂钩。

---

## v0.22.2 — 2026-09-21 12:05｜引擎 floor 饱和检测（硬规则 26）+ 禁止清单 #32/#33

**触发**：12:00 轮，已实测峰值 32.0，而引擎 L 路径预测 31.67 —— **预测值低于已观测值**。
引擎遂以 `max(mu, floor)` 把点估计顶成 32.00（= floor 本身），据此报 32 档公允 **85.7%**、
P(win) 20.6%。同轮市场给 32 档 47.7%、回归给 30.7% —— **差 47pp，方向单一偏冷**。

这是引擎在窗口内失效的**第 3 种形态**（前两种：10:20 同刻偏冷 −1.36；L 路径与三件套
路径自裂 1.12）。若并入「多口径对照表」会凭空虚增一个极冷口径、拉低 P(win)。

**改动**

1. `bucket_probs()` 新增 `mu_saturated` / `mu_raw` 返回值：
   `mu < floor` 时标记饱和（不再静默 `max()`）。
2. 主输出在检测到饱和时打印 🚫 告警，并指示改用「同刻 R 路径 + 市场隐含」双口径、
   禁止并入多口径对照。
3. SKILL.md 新增**硬规则 26**（含三种失效形态对照表）。
4. §4 禁止清单 +#32（不核对「点估计 > `--observed-max`」就引用档位概率）、
   +#33（把 floor 饱和分布当独立口径撑样本量）。

**影响**：仅告警与文档，不改任何概率计算路径；正常日（点估计 > floor）输出逐字节不变。

## v0.22.1 — 2026-09-21 11:45｜根因定位（相对误差口径）+ 修 `market_shape.py --date` 崩溃 + 本金规则

**`hk_edge.py` 数值输出零改动（仅版本号）。**

来源：Eason 追问两点 ——「本金假设是不是错了？」「RMS 为什么会算错、如何防再犯」。
两条都查实，且都指向**未被发现的既有缺陷**，非本轮新增。

1. **74% 的确切来源已定位：相对误差口径**（旧代码把每个残差 ÷ 该桶 mid 再开方）。
   同一批 10:40 盘口、5 桶 30–34：

   | 口径 | 数值 |
   |---|---:|
   | 绝对 RMS（正确） | **1.46%** |
   | 绝对 L2（漏 ÷n） | 3.26% |
   | **相对误差伪 RMS** | **64.7%** ← 报出的「74%」 |

   逐桶相对残差：30 档 −5.9%｜31 档 −7.9%｜32 档 **+0.5%**｜33 档 −4.7%｜**34 档 +144%**。
   **34 档单独占总残差平方和 99.4%** —— 一个只占 **1.8%** 质量、mid 仅 0.0177 的薄桶，
   凭 −2.5pp 的绝对残差把指标从 1.46% 顶到 64.7%。
   **决定性佐证**：同一错误口径套到 00:20 盘口正好得 **15.9%**，与当时报的「对照 9/20 是 15.9%」
   逐字吻合 → **不是一次性笔误，是跨轮沿用的口径错误**。
   → **硬规则 24 新增第 7–9 条**：禁用相对误差口径；报 RMS 必报单桶贡献占比，>50% 须点名
   并考虑移出拟合桶集；口径正确但含薄桶时，结论是「主体可用 + 尾部是噪声」，不是「不可用」。

2. **`market_shape.py --date` 自上线起从未跑通过**（v0.22.0 的 `IndexError`）：
   `mp._levels()` 返回 **(bids, asks) 二元组**，旧代码当单列表用，`if not lv` 对空档永不触发。
   连带第二处缺陷：旧代码只取 **YES bid**，当天 10 档里 6 档无买单被静默丢弃 → 支撑集被削，
   **违反硬规则 24 自己的前提**。改为 YES/NO 两侧**中间价**（形状拟合要 mid，执行取价才要 ask/bid）。
   → 输出新增**逐桶贡献占比**列与单桶主导告警。

3. **硬规则 23 补「天花板效应」**：无条件 R 不得外推高基准日。
   9/21 的 11:30 基准 **31.5** 已是 12 天样本里的次高档，10:40 轮却仍用 10:20 的
   无条件 **+2.80** 外推。正确的两条路一致：**回归** `peak = 11.704 + 0.664×rm@11:30`
   （n=12，残差 sd 1.06）→ 32.62；**条件 R**（基准 ≥30.5，n=5，R 中位 1.10）→ 32.60。
   回归斜率 **0.664 < 1** 即天花板效应的量化形式。

4. **新增硬规则 25：本金必须会话内显式、禁跨会话沿用**。
   9/21 全天四轮裸用 `--bankroll 2500` 且未标注假设，Eason 当天未说过本金；
   2500 实为 **9/15 遗留值** + `l_integrate.py` 默认值，而本文件规则是
   **「未给则默认 1000 并标注假设」** —— 技能内部不自洽。
   同一笔 305 份 NO32（$158.60）：按 2500 是 **6.3%**，按 1000 是 **15.9%**，**风控含义差 2.5 倍**。
   另查出：台账 9/20 单日投入 $1,006.98 = 2500 的 **40.3%**、9/16 $912.20 = **36.5%**，
   **两次突破 25–30% 单日敞口硬顶** —— 要么真实本金 >2500，要么该硬顶已被突破两次而无人记录。

5. §4 禁止清单新增 **#27–#31**（相对误差口径、不报单桶贡献、新脚本不跑全路径、
   无条件 R 外推高基准日、未标注沿用本金）。

---

## v0.22.0 — 2026-09-21 11:00｜新增 `market_shape.py` 与硬规则 24（盘口形状 RMS 的规范算法）

**`hk_edge.py` 数值输出零改动（仅版本号）；新增一个独立脚本。**

来源：9/21 10:40 轮用**错误的 RMS 算法**判「市场形状不可用（RMS 74%）」，
据此弃用市场隐含 σ（0.75）、改用站点 σ 0.85 → 公允 0.58 / EV +11.6%，并据此建仓 NO32。
同一批盘口用正确算法（归一化 mid、同一支撑集 7 桶、正态 LSQ）重算 **RMS 1.38%**，
市场自评 NO32 公允 **0.534** → 实际入场 edge 只有 **+2.7%**。**一个算错的 RMS 把入场理由放大约 4 倍。**

1. **硬规则 24** —— 拟合与 RMS 必须用同一支撑集；mid 先归一化到 sum=1；报 RMS 必带桶集与归一化前 sum；
   **RMS ≤ 3% 即视为可用**（三批实测 0.74–1.38%）；判「不可用」前须用相邻批盘口交叉验证。
   另记：**盘口隐含 σ 是时变信号**（9/21 全天 0.90 → 0.75 → 0.68），必须与 μ 漂移一起逐批留档。
2. **`scripts/market_shape.py`** —— 把正确算法固化为可执行脚本（此前每次手算，正是出错来源）：
   输出 μ/σ/RMS（必带桶集与归一化前 sum）、逐桶对照、每档 YES/NO 的市场自评公允价；
   RMS > 3% 才打印警告。支持 `--market` 或 `--date`（现拉订单簿）。
3. §4 禁止清单 + 第 25/26 行。

同步四处版本号：`README.md` / `SKILL.md` frontmatter / `scripts/hk_edge.py` VERSION / 本文件「当前版本」。

---

## v0.21.2 — 2026-09-21 10:40｜新增硬规则 22（引擎同刻偏冷）与硬规则 23（同刻实证 R 表）

**纯文档，`hk_edge.py` 代码零改动（仅版本号），任何输出数字不变。**

来源：9/21 盘中窗口内复核。用 `scripts/data/forecast_log.jsonl` 复原 9/10–9/20 共 11 天，
得到两条此前没有的、可复算的量化规则：

1. **硬规则 22** —— 引擎在 **10:20** 给出的点估计 `pred` 与当日实际峰值之差：
   n=11，**中位 −1.36 / 均值 −0.93 / sd 1.23；近 6 日均值 −1.85；近 3 日 −2.27**（逐日加剧）。
   唯一例外是 10:20 站温 ≥31.5 的早峰日（9/10，误差 0.00）。
   → 10:30 前的引擎中枢必须 **+0.9 ~ +1.9** 再判档，**禁止**直接拿它与盘口价相减当 edge。
2. **硬规则 23** —— **R = 当日峰值 − 10:20 running max**，n=11：
   `0.0/0.1/1.6/2.0/2.6/2.8/3.1/3.1/3.2/3.8/4.4` → **中位 +2.80 / 均值 +2.43 / sd 1.34**。
   同日 ERA5 分位路径给的中位剩升仅 **≈+0.9**，**低 1.5–1.9 °C**（网格抹平日内振幅）；
   → 不得单独采信 ERA5 路径定盘中中枢；R<0.6 的两个样本（9/10 早峰、9/14 冷池）都不属热晴形态，热晴日不得用作下限。

**§4 禁止清单** +第 23/24 行（10:30 前把引擎点估计当无偏中枢；只采信 ERA5 路径不与同刻 R 对照）。

**验证方式**：`forecast_log.jsonl` 的 `rm` / `pred` 两列可原样复算（每新增一天结算后重算一次）。

---

## v0.21.1 — 2026-09-20 21:30｜09-20 结算回填（33°C provisional）：台账最大单日亏损 −$783.97

**纯台账数据 + 文档，`hk_edge.py` 代码零改动**（仅版本号）。

**一、结算回填**
- **09-20 = 33°C（provisional）**：结算站 10 分钟快照最高 **33.1**（14:40 / 15:00 两次触及，15:10 起回落），
  watch 1 分钟口径亦记 33.1；收盘后市场 **33 档 YES bid 0.999**、其余各档 NO 0.999（已定档）。
  官方 `dailyExtract_202609.xml`（21:35 复核，需带 User-Agent）**仍只出到 09-17** → 只能 provisional。
- **09-18（31°C）/ 09-19（33°C）仍为 provisional**，两日均为零腿结算日，档位转正只需改 CSV 文件头文字。

**二、09-20 流水照录 13 条 → 折叠 5 腿 → 2-3 / −$783.97 / −77.9%（本金 −31.4%）**
YES33 $25.80（11:05，**唯一窗口内腿**，+$34.20）｜YES34 $6.00（11:05，−$6.00）｜
NO32 $100.25（12:30，+$62.75）｜YES32 $440.00（14:15，−$440.00）｜NO33 $434.93（14:15，−$434.93）。

**三、首次观测到「同腿折叠反向放大亏损」**
13 条全量口径 **−$687.11**、首推口径 **−$783.97**：被折叠的 4 笔盈利加仓（+$588.44）
多于 4 笔亏损加仓（−$491.58），差 **+$96.86**。→ `SKILL.md` 口径 10 补注：
**首推口径的方向不定**（9/15–9/17 减亏、9/20 增亏），它只是记账规则，不是保守估计。
**只计窗口内 2 腿 = +$28.20**，与全量 / 首推**符号完全相反** —— 口径 10（窗口外留档是否入账）
应改判「不入账」的最强论据（**待 Eason 拍板**）。

**四、`SKILL.md` §5.2 更新**
- 战绩表 32 腿 → **37 腿 / 9 个结算日 / 13-24 / 投入 $3,563.36 / 净 −$1,254.41 / ROI −35.2% / 4 盈 5 亏**；
  方向拆分 `NO` 17 腿 10-7（−9.7%）、`YES` 20 腿 3-17（−79.9%）。
- 错误族从三族扩为**四族 26 腿、胜率 2/26 = 7.7%**：新增「**追价押当档**」1 战 0 胜
  （9/20 YES32 落在硬规则 15 的 `gap ≤ 0.5` 禁用区）；「伪安全 NO」3 战 0 胜 → **4 战 0 胜**（+9/20 NO33）；
  「右尾注」12 笔 1 胜 → **13 笔 1 胜**（+9/20 YES34）。
- 新增第五类归因：**「不是判据错，是执行时点错」** —— 09-20 建议金额与窗口**成反比**
  （窗口内 1.0% 本金 vs 窗口外 17.6%），被 `硬规则 21 ④` 反制。

**五、CSV 文件头**新增 09-18/09-19/09-20 判定依据、09-20 逐轮流水、口径 10 反向暴露的算账明细；
并记录官方 Daily Extract 的真实 JSON 结构（`{"stn":{"data":[{"month":9,"dayData":[[...]]}]}}`）。

**Git**：6 文件双副本 md5 一致；commit / push 见仓库历史。

---

## v0.21.0 — 2026-09-20 15:05｜最佳下注时段的四轴量化；edge 分「预测型 / 反应型」

**一、新增 `scripts/hourly_window.py` —— 把「最佳下注时段」从一句结论变成四条可复现证据**

按小时对齐四类**互不依赖**的量（n=10 天，09-11…09-20）：

| 小时 | 轴1 `R̄(h)`（P(R≥1.0)） | 轴2 最终档已锁定率 | 轴3 市场认账率 | 轴4 台账 ROI |
|---|---:|---:|---:|---:|
| 09 | — | — | 0.384 | **−57.5%**（1/4） |
| 10 | — | — | 0.305 | **−57.5%** |
| **11** | **+2.30（90%）** | **10%** | **0.343** | **+42.1%（3/4）** |
| **12** | **+1.49（70%）** | **10%** | 0.393 | **+78.9%**（1/3） |
| 13 | +0.70（40%） | 40% | 0.575 | **−100%（0/14）** |
| 14 | +0.20（**0%**） | 70% | 0.705 | −100% |
| 15 | +0.06（0%） | 90% | 0.897 | −100%（0/1） |
| 16 | 0.00 | **100%** | 0.875 | — |

- **轴1** 还剩多少没兑现；**轴2** 答案是否已写在墙上；**轴3** 市场什么时候才认账；**轴4** 真金白银。
- **最终档「首次达成」时刻中位数 = 13:50**（n=10）—— 一半以上的日子答案在 13:50 之后才出现。
- 三个独立阈值（信息足够 ≥2h 升温速率 / 认账率 ≤0.35 / 已锁定率 ≤10%）**全部落在 10:30–12:00**。

**二、硬规则 20 —— edge 分「预测型」与「反应型」，只有预测型可执行**

市场对一次边界跨越的定价延迟实测只有 **3–13 分钟**（2026-09-20 33 档 mid）：
`14:06 → 0.415` ｜ **`14:10` rmax 触及 33.0 → 同刻 `0.645`** ｜ `14:12 → 0.780` ｜ `14:23 → 0.985`。

我们的输入是 **10 分钟采样**的实况 CSV，加读数与下单往返 —— **结构上打不过**。
这就是台账「≥13:20 共 15 腿 0 胜」的**机制解释**：那一段提供的全部是反应型机会。

对照：预测型缺口的修复速度 ≈ **0.12/小时**（市场对最终档的定价 11 时 0.343 → 14 时 0.705，3 小时走 0.36），
存活期 ≥2 小时 → 可执行。**判据：盈利依赖单次越线 → 不可执行；依赖 `R̄ ≥ 1.5` 的框架错配 → 可执行。**

**三、硬规则 21 —— 单日最强证据（同一工具、同一模型、只差小时）**

| 时刻 | 腿 | 金额 | 结果 |
|---|---|---:|---:|
| **11:05（窗口内）** | 买 YES33 @0.430 | $25.80 / 60 份 | **+$34.20（+132.6%）** |
| 12:30（窗口外留档） | 买 NO32 @0.615 | $101 | −$101 |
| 14:15（窗口外留档） | 买 YES32 @0.200 | $440 | −$440 |
| 14:40（窗口外留档） | 买 YES34 @0.0113 | $2.81 | −$2.81 |

**仅靠「只在 10:30–12:00 执行」这一条纪律，当天从 −$507 变成 +$34。**
窗口外那三条的立论在当时都不算离谱 —— 它们输在**进场时刻**，不在判断本身。

**落地方式**：新增 1 脚本 + 2 条硬规则。`hk_edge.py` **代码零改动、任何输出数字都不变**。
SKILL.md 新增 §2.2-硬规则 20/21、§4.3 禁止清单第 21/22 行、§7-F 脚本清单一行、§3.3 指向新脚本。

⚠ **样本限制**：四轴的 `n` 都是 **10 天**，日内多个 10 分钟点高度自相关，**不构成独立样本** ——
本表只作方向与量级，**不是统计显著结论**。

---

## v0.20.1 — 2026-09-20 14:55｜跨站条件频率的两步换算；「下午才到 N」是独立负面信号

**背景**：14:40 轮我判 `P(≥34) = 3%`（`rmax = 33.0`）。14:40 rmax 刷新到 **33.1**，
同一时刻我用 VHHV 20 年样本做了新的条件化，把中央**下调到 2%**、区间上限从 8% 收到 5%，
一次正 EV 腿（`YES34 @0.010`）的全部可成交额只有 **$2.40**。

**一、跨站条件频率必须做两步换算（此前直接把 VHHV 频率当参考，是错的）**

| 站 | 9 月无条件 `P(日最高 ≥ 34)` | n |
|---|---:|---:|
| VHHH 赤鱲角（机场） | **23.0%** | 300 |
| HKO 天文台（结算站） | **8.7%**（9/11–9/25）/ 7.0%（全 9 月） | 150 / 300 |

**气候比 2.6–3.3 倍。** 再加**位置修正**：VHHV 是整数读数，`rm=33` 的真值在 `[33, 34)` 内
平均距 34.0 只有 **~0.5**（由 20.0% 跨档率反解，等效需求距离 **0.80**）；
2026-09-20 的 `rmax = 33.1` 距 34.0 是 **0.90**。

```
P_HKO(≥N+1 | rmax@h=x) = P_其他站(跨档 | rm=int(x)) × 位置修正 × 气候比
实测：20.0% × (10/20) × 0.30 ≈ 3.0%
```

**二、「下午才到 N」是独立的负面信号（本轮最重要）**

VHHV 9 月按**首次达到 33 °C 的时刻**分层：

| 首达 33 | n | 最终 ≥34 | 最终均值 | max |
|---|---:|---:|---:|---:|
| 早（≤12 时） | 91 | **69.2%** | 34.01 | 37 |
| 午（13–14 时） | 58 | **24.1%** | 33.26 | 35 |
| 晚（≥15 时） | 18 | **0.0%** | 33.00 | 33 |

严格一刀 —— 「h=14 时未到 33、14–15 时才首达」的真晚熟组：**n=26，跨档 0 次，最终值 26 天全 = 33.0。**

**机制**：首达时刻编码「今天大气还剩多少能量」。上午冲上 33 的日子午后仍能再爬 1 °C 以上；
14 时才勉强够到 33 的日子日变化曲线已见顶，剩余行程压在 +0.5 内（与硬规则 12 的 `R(rmax@h)` 阶梯一致）。

**2026-09-20 属于后者**（14:00 时 rmax 仅 32.6，14:10 首达 33.0）。HKO 自身两天同构：
09-12（32.6 → 33.1）、09-19（32.8 → 33.2）。

**这也解释了市场为何只给 1.0%** —— 市场定价里含这个「晚熟折扣」，而我此前四轮都没有它。

**落地方式**：纯文档 PATCH。**代码零改动、任何输出数字都不变**。
SKILL.md 新增 §2.2-硬规则 19（三段 + 四条纪律）、§4.3 禁止清单第 19/20 行。

---

## v0.20.0 — 2026-09-20 14:40｜新增 `r14_cond_table.py`；整数分辨率条件表与锁档后的同支付函数检验

**一、新增 `scripts/r14_cond_table.py`（可复现、无 API key）**
从 Open-Meteo archive（ERA5 / ERA5-Land，2015 起，0.1 °C 浮点）拉逐时序列，
按 **`rmax@h` 的分位**切分，输出 `P(最终日最高 − rmax@h ≥ g)`。
补上了一个一直缺的口径：**`_vhhh_asos.csv` 是整数分辨率，答不了「还差 g 度」这类问题。**

基准表（`--hour 14`，2015–2025 九月，n=3683）：

| q | thr（rmax@14） | P(R≥0.4) | P(R≥0.6) | **P(R≥1.0)** |
|---:|---:|---:|---:|---:|
| 0.50 | 26.3 | 17.2% | 7.9% | 2.0% |
| 0.90 | 30.6 | 17.5% | 6.9% | 0.8% |
| **0.95** | **31.1** | 20.5% | 10.0% | **1.1%** |
| 0.99 | 32.5 | 14.6% | 4.9% | **0.0%** |

**曲线在 0.6→1.0 之间掉一个数量级** —— 「距边界 0.4 度」与「1.0 度」是两种不同交易。

**二、硬规则 17 —— 整数分辨率的条件表不能回答「距边界还差 g 度」**
`_vhhh_asos.csv` 202138 条**全部整数**（小数位占比 0.0000%）。实测同一张表里
`P(R≥0.5) = P(R≥0.6) = P(R≥1.0) = 27.0%`，`p75 = p90 = p95 = 1.0` —— 三个阈值不可分辨。
它的 27% 量的是「从 `[32,33)` 里的平均位置跨到 `≥33.0`」，**平均剩余距离约 0.5 °C**；
而 2026-09-20 的 `rmax = 33.0` 恰好卡在 34.0 下界，**剩余距离整整 1.0 °C**，是同一 bin 里最不利的位置。
照搬它会把 `P(≥34.0)` 从 **3% 报成 27%**，`YES34 @0.009` 的 EV 从 +233% 变成 +2900%。
→ 回答「还差 g 度」只认 0.1 °C 口径：`watch.log` 的 R 阶梯 / `--watch` 站点 one-touch / 本脚本（下界）。

**三、硬规则 18 —— `rmax` 落档后先做同支付函数检验**
`rmax` 落进 `[N, N+1)` 后，`NO(N)` 的支付函数退化为 `{最终 ≥ N+1.0}`，
与 `YES(N+1)` 只差 `P(≥ N+2.0)`。**两条腿必须报价相近。**
2026-09-20 14:35 实测（`rmax = 33.0`）：`NO33` ask **0.020** vs `YES34` ask **0.009** ——
支付函数只差 0.3%，报价却贵 **2.2 倍（11pp）**。
该检验**不依赖「我方模型更准」**，只需要 `rmax` 已落档这一已实现事实。

**落地方式**：新增 1 个脚本 + 2 条文档规则。`hk_edge.py` **代码零改动、任何已有输出数字都不变**。
SKILL.md 新增 §2.2-硬规则 17/18、§4.3 禁止清单第 17/18 行、§7-F 脚本清单一行。

---

## v0.19.16 — 2026-09-20 14:25｜跳跃口径：`gap ≤ 0.5 °C` 禁用连续正态；`d_rmax` 取代 v40

**触发事件**：14:15 我方口径 N(μ=32.87, σ=0.35) 给出 **P(32 档) = 60.2%**（即「33.0 打不穿」的概率 64.5%），
市场同时只给 19–22.5%，我判「n=3 太薄、市场是噪声」。**10 分钟后 14:10 官方最高单跳 +0.4 到 33.0，32 档归零。**
**市场对，我们错，而且错得比市场多。**

**新增硬规则 15 — `gap ≤ 0.5 °C` 禁用连续正态定档。**
当 `gap = 档位上界 − rmax ≤ 0.5 °C`，问的是「今天会不会再跳一下跨过去」，这是**离散跳跃**问题。
正态把最终峰值当成围绕 μ 的连续对称分布，把约 60% 质量放进 `[32.87, 33.0)` 这个窄区间；
但真实约束是**最终峰值 ≥ rmax（硬地板）且路径块状**，一次 +0.4 就能填满整段。
**σ 模型在这里是「形状错」，不是「参数错」。** 替代口径 = `R = 最终峰值 − rmax(h)` 的经验分位（站点优先）。

**新增硬规则 16 — 用 `d_rmax` 危险率表判「当日是否已结束」，不要用 v40。**
`d_rmax` = 距最近一次刷新当日最高的 10 分钟步数。

| `d_rmax` | 0 | 1 | 2 | 3 | 4 | 5–6 | 7–9 | **≥10** |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| n（全时段） | 42 | 29 | 20 | 15 | 12 | 20 | 25 | ≥18 |
| P(R ≥ 0.4) | **57%** | 52% | 40% | 27% | 17% | 20% | 11% | **0%** |
| R̄ | +0.52 | +0.47 | +0.37 | +0.28 | +0.17 | +0.16 | +0.06 | **0.00** |

下午子样（h ≥ 14:00）：`d_rmax ≤ 2` 时 `R̄ = +0.11`、天花板 **+0.5**；**`d_rmax ≥ 10` → 18 个观测 R 全为 0。**

**为什么必须换掉 v40**（2026-09-20 正面反例）：

| 指标 | 14:00 读数 | 判断 | 结果 |
|---|---|---|---|
| v40（13:20→14:00） | **+0.1** | 「已过动能段，R 取 0」 | **错** |
| **`d_rmax`** | **0**（14:00 刚创新高） | 「仍在爬升」 | **对：10 分钟后 +0.4** |

v40 是固定窗口的**回望速率**，平台期本身就把 40 分钟速率压到近 0，于是把「刚跳完仍在高位」误读成「结束」；
`d_rmax` 度量「最近一次新高距今多久」，平台期它自然增大，但刚刷新后不会给出 0 以外的值。

**落地方式**：纯文档 PATCH。**代码零改动、任何输出数字都不变**，改的是「用哪种分布、用哪个判据」。
SKILL.md 新增 §2.2-硬规则 15/16、§2.9-x `d_rmax` 危险率表；§4.3 禁止清单新增第 15、16 行。

---

## v0.19.15 — 2026-09-20 14:15｜`R̄(14:00)` 必须按 rmax 水平分层看

**问题**：硬规则 12 给的 `R̄(14:00) = +0.150`（n=10）是**无条件均值**，被凉爽日主导。
2026-09-20 13:20 的 rmax 已到 **32.6**（样本内并列最热），套 +0.15 得 **32.75**；
但把同批样本按 `rmax@14:00` 切开后，热日那一组的 R 是 **+0.1 / +0.5 / +0.4（均值 +0.33）**，
对应 **32.93** —— 0.18 °C 之差横跨 33.0 边界。

**回测（同批 n=10，剔 9/14 冷池日）**：

| 组 | rmax@14:00 | R 逐日 | 均值 |
|---|---|---|---:|
| 高（n=3） | 32.4 / 32.6 / 32.8 | +0.1 / +0.5 / +0.4 | **+0.33** |
| 低（n=7） | 31.2–31.5 | 0.0 / 0.0 / +0.3 / 0.0 / +0.2 | **+0.07** |

**但两个锚都被 v40 门控压制**：当日 13:20→14:00 的 rmax 只涨 **+0.1**（**0.15 °C/h < 0.5**），
按 §2.2 属「已过动能段」→ R 应取 0。对照 09-19 同窗口 v40 = **0.6**、09-12 = **1.05**，
那两天此后都继续上行。→ 当日正确结论是「**报区间（32.7–33.1）、不出手**」。

**落地方式**：纯文档 PATCH。**代码零改动、任何输出数字都不变**。
新增纪律三条（SKILL.md §2.2-硬规则 12）：① h≥13:30 时无条件锚与分层锚并列报；
② 分层锚 n=3，不得单独定中心，只用于纠正无条件锚在热日上的低温偏置；
③ 必须配 v40 门控，两者互相压制时只报区间。
同步写入 §4.3 禁止清单第 14 行。

---

## v0.19.14 — 2026-09-20 14:00｜下午的峰值锚必须换成 `rmax(h)`

**问题**：硬规则 11 的「`μ = 7Mmax + Δ`（Δ 取无条件均值）」是在 **h ≤ 12:30** 的样本上立的。
到了下午，网格日变化曲线已经见顶（今日 7M 逐时峰 31.34 @13–14h），而站点仍在爬升 ——
此时 `7Mmax + Δ` **不含任何时间信息**。2026-09-20 14:00 实测：它给 **33.36**，
而同一时刻的站温锚只给 **32.69** —— 0.67°C 之差**直接跨过 33.0 边界**，
把一笔「σ 决定符号、实为无边际」的交易伪装成 **+28% 的假 edge**。

**回测（同批 n=8，剔 9/14 冷池日，预测当日最终峰值）**：

| 锚 | corr | 残差 sd | MAE |
|---|---:|---:|---:|
| `7Mmax + Δ(1.86)` | 0.895 | **0.373** | 0.274 |
| **`rmax@h + R̄(h)`（h=14:00）** | **0.980** | **0.196** | **0.163** |

**新增 `R(rmax@h)` 阶梯表**（`SKILL.md` §2.2-硬规则 12）：13:00 之后每半小时 R 均值掉一半 ——

```
h=12:00  +1.56      h=13:00  +0.775      h=13:30  +0.538
h=14:00  +0.188     h=14:30  +0.075
```

**落地方式**：纯文档 PATCH。**代码零改动、任何输出数字都不变**，改的是「用哪个锚」的判读纪律。

**新纪律**：h ≤ 12:30 用 `7Mmax + Δ`；h ≥ 13:30 用 `rmax(h) + R̄(h)`（σ 取表内 sd × 1.3–1.5）；
12:30–13:30 为交接带，两侧并列、跨度 >0.5°C 只报区间。
同步写入 §4.3 禁止清单第 13 行。

---

## v0.19.13 — 2026-09-20 13:20｜修冷池误报：0.5°C 落差的浮点陷阱

**症状**：2026-09-20 13:00，`hk_edge.py` 把当日主公允表 32 档及以上整段标「⚠作废(冷池日)」，
理由写「今日曾出现 0.5°C 的回落」。但当天 **全港 18 个分区雨量全为 0mm、UV 8、
无任何生效警告**，那 0.5°C 只是正午常规振荡（同类振荡 09-12 也有 0.5°C，见 SKILL §2.9）。

**根因**：`_load_cold_pool_gap()` 落盘时 `round(,3)`、内存里不 round，于是
`_cold_gap` 在比较时仍是全精度值。0.1°C 序列的两位小数相减带浮点残差：
```python
>>> 32.2 - 31.7
0.5000000000000036          # > 0.5 → True，冷池被触发
```
判定式写的是「**落差 > 0.5**」，本意是「恰好 0.50°C 不触发」，实际被浮点判成触发。

**修法**（`scripts/hk_edge.py`，冷池判定分支）：
1. 比较全部加容差 `_CPS_EPS = 1e-6`：`> _cold_gap + EPS`、`> 0.5 + EPS`、`<= 0.5 + EPS`；
2. `_cold_gap = round(_cold_gap, 3)` —— 内存值与落盘值同精度，消除双口径。

**对输出的影响**：仅在「当日最大回撤恰好等于 0.50°C」这一边界上不再误报；
回撤 > 0.5°C 的日子（09-14 的 1.5、09-15 的 1.4）行为完全不变。
其余所有数字（公允 P / EV / Kelly / σ / Δ）一字未改。

---

## v0.19.12 — 2026-09-20 12:55｜硬规则 11：Δ 是不变量，禁止用当日 L / GAP 修正

**改动类型**：纯文档（`SKILL.md` §2.2 新增硬规则 11、§2.9 新增 Δ/L/GAP 分解表）。**代码零改动** → PATCH。

- **触发**：12:20 rmax 跳到 **32.2**（样本内 12:30 最高水位，比原最高 31.7 高 0.5°C），
  33 档成为唯一分水岭。此时两个估计路径打架：朴素叠加法（`rmax + GAP + drift`）给 **32.77**，
  无条件 Δ 法给 **33.26** —— 差 0.49°C 横跨档界。
- **留一法回测（n=8）判定**：无条件 Δ 的 MAE **0.31**，分解法 **0.64** —— **分解法误差是无条件法的两倍**。
- **根因**：三分量强烈反号抵消 —— `corr(L, drift) = −0.75`（L 是站点相对网格的当前水位，
  drift 是午后相对网格的额外漂移，两者互补）、`corr(GAP, Δ) = +0.03`（网格当日已走行程对 Δ 无预测力）。
  `sd(Δ)=0.33` 而 `√(sd_L²+sd_GAP²+sd_drift²)=0.94` —— **总和比各部分稳 3 倍**。
- **纪律**：只用 `μ = 7Mmax + 1.77`（或 `rmax + 1.15` 交叉）；禁止用今日 L / GAP 修正。
  同时标注 `l_integrate.py`（从 L 建分布、L 先验取气候值 +1.00）**系统性偏低**（今日 33.04 vs 33.26），
  定位为交叉校验工具而非主口径。
- **执行后果**：市场已追平 —— 桶价拟合市场分布 **N(33.18, 0.47)**，我方 N(33.30, 0.40)，
  中心只差 **0.12°C**，而我方 μ 的标准误正是 0.12°C。表面 12pp 的桶价差**全部来自 σ**：
  换成市场的 σ=0.47，YES33 的 EV 从 +14.9% 掉到 +5.7%；σ ≥ 0.55 转负，而 sd(Δ)=0.33 的 95% CI 是 [0.21, 0.72]。
  按硬规则 8 判**无边际、不出手**。
- 本次运行写入 `data/decision_log.csv` 快照（12:55）。

---

## v0.19.11 — 2026-09-20 12:30｜硬规则 10：外推基准是 rmax 不是整点站温；新增 R(rmax@12:00) 表与正午分层

**改动类型**：纯文档（`SKILL.md` §2.2 新增硬规则 10、§2.9 新增两张样本表）。**代码零改动** → PATCH。

- **触发**：12:00 瞬时站温 31.3，但官方 1 分钟 CSV 显示 **rmax 已 31.7** —— 差 0.4°C。
  该差值在 33.0 边界上把 P(33 档) 从 **63%（基准 31.7）压到 53%（基准 31.3）**，
  足以翻转一条腿的 EV 符号。结算看的是 Absolute Daily Max 即 rmax，**外推必须用 rmax**。
- **附带**：`watch.log` 是 10 分钟采样，会漏掉两采样点之间的新高 → 必须直接拉
  `latest_1min_temperature.csv` 的 `HK Observatory` 行核对。
- **新增 R(rmax@12:00) 分位表**（n=7，均值 **+1.56**、sd 0.53）与**正午 rmax 分层**：
  `rmax@12:00 ≥ 31.3` 的 2 天全部到 **33**（R 均 +1.80）；`≤ 30.8` 的 5 天全部 ≤ **31.7**；
  **7/7 完美分离，无一落在 32 档**。高组 n=2 → 定为**方向锚，不作概率表**。
- **新增 12:00 站温样本表**（n=8）：同时给出 R(12:00)（sd 0.65）与 Δ=peak−7Mmax（**sd 0.28**），
  确认 **Δ 口径比 R 口径稳一倍**，且 Δ 与当天热度基本无关 → 首选 Δ。
- **新增警告**：禁止用 OLS `peak = a + b·站@12:00` 外推 —— 该回归在样本高端严重欠拟合
  （站 31.2/31.3 的两天实测 33.2/33.1，回归只给 32.5），今日恰是高端杠杆点。
- **二次触发硬规则 7**：`--watch` 报 `L = −0.80`、峰值外推 **31.70**（= 已观测最高，即称「已见顶」），
  与 Δ 口径 33.34 反号且 |L|>0.5 → **L 判废**，只取实况读数（rmax 31.7、全港最热 Peng Chau 34.4）。
- 本次运行写入 `data/decision_log.csv` 5 档快照（12:30，引擎默认口径）。

---

## v0.19.10 — 2026-09-20 11:05｜四外推方法回测排序：A 作废、D 降级、取 B/C 逆方差加权

**改动类型**：纯文档（`SKILL.md` §2.2 新增硬规则 9、§2.9 新增 h=10:50 样本表）。**代码零改动** → PATCH。

- **触发**：11:00 站温 30.2（样本内 10:50 最高），四个外推方法给出 **31.93–33.36**，跨度 1.43°C，
  正好横跨 32/33 档边界 —— 与 09:40 那次「μ 分歧横跨档界」同型，因此对四个方法做了同批历史日回测。
- **回测结果（n=8，剔除冷池日 9/14）**：
  | 方法 | 公式 | MAE |
  |---|---|---:|
  | C | `7M日最高 + 1.86` | **0.30** |
  | B | `0.138 + 1.085·站@10:50` | 0.43 |
  | D | `站@10:50 + 1.60·(7Mmax − 7M@10:50)` | 0.63 |
  | A | `站@10:50 + 2.64` | 0.96 |
- **新增纪律**：A 作废（MAE 是 C 的 3 倍）；D 仅在 `7Mmax − 7M@10:50 ≥ 1.2` 时可用
  （今日仅 1.08，k 缩放会把网格压扁午后的误差放大 1.6 倍）；中心估计取 B/C 逆方差加权
  （今日 → **μ = 33.13**）；**保守口径 = B 的读数**（32.90），仓位取两口径 Kelly 之间的偏保守值。
- **执行后果**：11:05 市场 33 档 ask 0.420，中心口径 P33 48.9%（EV +16.4%）、保守口径 39.4%（EV −6.2%）。
  市场隐含中心 32.88 恰落在保守口径上 → 该笔交易定性为**「赌 B/C 胜过市场隐含中心」**，按此表述与降仓处理。
- **另一发现**：`R(10:50)` 与 `站@10:50` 相关仅 r≈0.55，「上午热 ⇒ 午后还热」不成立；
  n=2 的类比不足以支撑仓位（已写入 §2.9）。
- 本次运行写入 `data/decision_log.csv` 6 档快照（11:05，引擎默认口径）。

---

## v0.19.9 — 2026-09-20 10:40｜硬规则 8：σ 敏感性检查先于仓位建议

**改动类型**：纯文档（`SKILL.md` §2.2 新增硬规则 8）。**代码零改动** → PATCH。

- **触发**：10:40 站点升至 29.9、盘口 32 档 mid 抬到 0.485，所有档位 |edge| ≤ 2.5pp。
  复核时发现同一个 μ=32.9，σ 取 0.55/0.65/0.72 会让 **31 档公允概率在 5.1%–10.2% 之间翻倍**，
  足以翻转该档的 edge 符号 —— 即此前几轮报出的「31 档有 +35% EV」完全可能是 σ 的产物。
- **规则**：报仓位建议前先用 σ±0.1 各跑一遍；结论会翻转 → 判「无边际、不出手」。
  σ 必须与来源一同写出（本日可用来源：回归残差 sd 0.34、升温经验 sd 0.48、合成 0.57，取 0.65）。
- **附带**：今日 09:40→10:40 盘口自行把 33 档 mid 从 0.483 抬到 0.390（32 档 0.381→0.485），
  「33 档贵 10pp」在两小时内被市场自己收走 —— 印证硬规则 6/7 的方向，也说明**早盘的 edge 常是未定价，不是错定价**。

---

## v0.19.8 — 2026-09-20 10:25｜硬规则 7（`--watch` 的 L 判废条件）+ 直测 k(h=10:50)=1.60

**改动类型**：纯文档（`SKILL.md` §2.2 新增硬规则 7、§2.3 新增直测章节）。**代码零改动** → PATCH。

- **硬规则 7 触发场景**：10:00 站点 29.3°C，`--watch` 的网格源 `best_match`（**单模式**）报 31.1°C
  —— 恰为当天 7 个成员中的**最大值**（7M 均值仅 30.00），BM 骑在最暖成员上，
  日变化曲线被压成「10 时已达其日最高 31.7 的 98%」。于是 `--watch` 报 **L = −1.80**，
  据此外推峰值 **29.9°C**，与 HKO 官方 33°C、与日最高 Δ 口径 33.0 相差 **3.0°C**。
- **根因**：`--watch` 用 `OM_HR`（`models=best_match`），而 `model_calib.bias` 与日最高 Δ
  均按 **7 模式均值**校准 —— β 口径不同源，v0.18.0 双边对齐的前提（L 日间稳定）被破坏。
- **纪律**：L 与近 10 日 Δ **反号且 |L|>0.5 → L 判废**，改走 §2.9 的 Δ 口径；
  `--watch` 只取其实况读数（站温 / running max / 全港最热站点），不取峰值外推。
  另记录第三方**晨间偏移回归** `Δ = 1.52 + 0.322·x`（x = 站@10:50 − 7M@10:50，n=8，残差 sd 0.34）。
- **k 直测**：用 HKO 站 10 分钟轨迹 ÷ 7 模式逐时网格做**同点位**直测，h=10:50、n=8 →
  **k = 1.60（中位 1.65）**，而非默认的 2.4（那是 h=13 的值，见 §2.3 硬规则 2）。
  11 时前用 2.4 会把站点口径 R 高估约 50%。**`--r-scale` 必须与 `--hour` 同时给。**
- **附带**：分位表是**无条件**气候分布。8 个晴热干净日实测 R 全部 ≥1.9（均值 2.64），
  而 `--watch` 打印的「站点口径 P(≥1.7)」仅 34% —— **干燥晴热 regime 必须用经验锚，不能直读分位表**。
- 本次运行写入 `data/decision_log.csv` 6 档快照（10:25，引擎默认口径）。

---

## v0.19.7 — 2026-09-20 09:40｜新增硬规则 6：`model_calib.bias` 与近 10 日 Δ 的 regime 分歧必须并列报两版

**改动类型**：纯文档（`SKILL.md` §2.2 新增一节）。**代码零改动，不改变任何输出数字** → PATCH。

- **触发场景**：2026-09-20 09:40 给当日定价时发现，引擎点估计 32.50（`bias=1.00`，73 天窗口）与
  近 10 个干净日的实测 Δ **+1.86 ± 0.32**（+1.27 ~ +2.37，无一低于 +1.27）相差 **0.86°C**，
  正好横跨 32/33 档边界。
- **后果**：同一份数据、同一套模型，`NO33 @0.520` 的 EV 在 μ=32.50 口径下是 **+51.7%**
  （看起来是巨量 edge），在 μ=33.27 口径下是 **−10%**。**符号翻转。**
- **三条独立口径收敛**：近 10 日 Δ → 33.27；市场归一 mid 隐含中心 → 33.08；
  `l_integrate.py --market` 反解 **市场隐含 L = +1.82** → 33.18。**只有引擎的 73 天校准站在对面。**
- **新增纪律（5 条，见 `SKILL.md` §2.2 硬规则 6）**：出结论前必拉 `past_days=12` 的 7 模式
  `daily temperature_2m_max` 与近 10 个干净日实测配对；偏差 >0.5°C 时必须并列报两版公允分布；
  用 `l_integrate` 反解的市场隐含 L 做独立第三方仲裁；与 §2.7 干燥晴天 Δ∈[1.0,1.7] 的规则同向，不推翻。
- 本次运行同时写入 `data/decision_log.csv` 6 档快照（09:40，μ 口径为引擎默认版）。
---

## v0.19.6 — 2026-09-19 21:30｜台账结算 09-19（零腿日）+ 09-18 仍 provisional + 补定义口径 11

**改动类型**：只改 `settle_status` 与文档（CSV 文件头 / SKILL.md 口径 11 / 记分牌注释），代码零改动 → PATCH。
**累计数字一位未变**：PNL.md 的 md5 与 v0.19.5 相同（`0b283815516a90c1deba5dc84c06f390`）。

- **09-19 结算 33°C（`provisional`）**：官方 XML 仍只到 09-17。判据 ① 结算站日内序列
  （`obs_1min_log.json` 10 分钟快照）当日最高 **33.1**（14:20 / 15:00 / 15:30 三次触及）、
  watch 1 分钟口径 Max Since Midnight 记 **33.2**（16:40），两者同落 [33.0, 34.0) → 33 档；
  ② 收盘后市场 **33 档 YES bid/ask = 0.998/0.999**（≥0.95 已定档）、其余各档 NO 0.999
  （`market_prices.py 2026-09-19 --depth 3`，事件已于 20:00 HKT 收盘，24h 量 85.5k）。
- **09-19 无建议腿入账 —— 台账第二例「零腿结算日」**。四条证据一致：① `decision_log.csv` 内**写入时间戳**
  为 09-19 的行 0 条（文件最后写入时点停在 **2026-09-17 14:14**）→ 推荐引擎当日未运行；
  ② 当日会话日志 `WorkBuddy/*/.workbuddy/memory/2026-09-19.md` 全盘不存在；
  ③ 会话检索对 09-19 返回 0 条；④ 当日 `watch.log` 只播实况与概率、无 Kelly 段。→ 按口径 11 留空。
- **09-18 仍为 `provisional`**：官方 `dailyExtract_202609.xml`（09-19 21:30 复核，**必须带 UA**）**仍只出到 09-17**
  （09-17 = 31.4 → 31，与已确认值一致）。该日台账无腿 → 档位确认后只需改文件头文字，无腿可翻转。
- **纠错（本次真正的发现）**：v0.19.5 里「判零腿看 `decision_log.csv` 有没有当日记录」的判据**有陷阱** ——
  该文件的 `target_date` 列会**提前写入未来市场**的快照（09-15 11:01 写的行 target_date 已排到 09-21），
  拿日期直接搜会得到 **6 条假阳性**。**必须用第 1 列写入时间戳判**。已在 CSV 文件头把 09-18 那段的措辞
  改正，并把完整判据沉淀为 §5.2 **口径 11**（v0.19.5 只引用了编号却没写进编号列表，本次补齐定义）。
- **累计 32 腿 / 8 个结算日 / 11-21 / 投入 $2,556.38 / 回收 $2,085.94 / 净 −$470.44 / ROI −18.4% / 4 盈 4 亏**
  （与 v0.19.5 完全一致）。NO 15 腿 9-6（+8.8%）、YES 17 腿 2-15（−75.7%）。
- **待跟进不变**：口径 9 / 10 仍待拍板；`PNL_actual.md` 仍缺 9/15–9/18 真实成交；口径 11 的
  「零腿日次日补录」边界仍未验证。

---

## v0.19.5 — 2026-09-18 21:30｜台账结算 09-17（转 confirmed）+ 09-18（零腿日，仅登记档位）

**改动类型**：只改 `settle_status` 与 `PNL.md` 的 provisional 标记，代码零改动 → PATCH。**累计数字一位未变**。

- **09-17 转 `confirmed`**：官方 Daily Extract（09-18 21:31 复核，**必须带 UA**）已出到 **09-17 = 31.4 → 31 档**，与 provisional 值一致 → 当日 5 腿 1-4 / −$88.94 **不变**，31↔32 翻转未发生。15 行 `settle_status` 由 `provisional` 改 `confirmed`（含 10 条加仓 / 留档行，`--settle` 会一并刷 status）。
- **09-18 结算 31°C（`provisional`）**：官方 XML 仅到 09-17。判据 ① 结算站日内序列（`obs_1min_log.json` 10 分钟快照）当日最高 **31.6**（14:10 起多点触及，15:20 最后一次），watch 1 分钟口径记 **31.7**，两者同落 [31.0, 32.0) → 31 档；② 收盘后市场 **31 档 YES bid 0.997**（≥0.95 已定档）、其余各档 NO 0.999（`market_prices.py 2026-09-18 --depth 3`，事件已于 20:00 HKT 收盘）。
- **09-18 无建议腿入账 —— 台账首例「零腿结算日」**。三条独立证据一致：① `scripts/data/decision_log.csv` 无任何 09-18 记录 → 推荐引擎 `hk_edge.py` 当日**未被运行**，不存在带 `金额 + 份数` 的建议输出；② 当日会话日志（`WorkBuddy/2026-09-08-15-36-14/.workbuddy/memory/2026-09-18.md`）不存在，全盘仅有的两份 09-18 记忆属 MS UST 评论与个股投研，与气象市场无关；③ 会话检索对 09-18 返回 0 条。当日 `watch.log` 只播实况与概率、不含 Kelly 仓位。→ 按「无法确定即不臆造」原则**保留原状**，只在 CSV 文件头登记档位与空缺原因。
- **累计 32 腿 / 8 个结算日 / 11-21 / 投入 $2,556.38 / 回收 $2,085.94 / 净 −$470.44 / ROI −18.4% / 4 盈 4 亏**（与 v0.19.4 完全一致）。NO 15 腿 9-6（+8.8%）、YES 17 腿 2-15（−75.7%）。
- **`PNL.md` 唯一文本差异**：09-17 行的 `31°C *` 去掉 provisional 星号；其余逐位相同。
- **待跟进不变**：口径 9 / 10 仍待拍板；`PNL_actual.md` 仍缺 9/15–9/18 真实成交。

## v0.19.4 — 2026-09-17 21:35｜台账结算 09-16（转 confirmed）+ 09-17（首日含「研究留档」腿）

**改动类型**：只回填结算数字 + 战绩表 / 归因文档，代码零改动 → PATCH。

- **09-16 转 `confirmed`**：官方 Daily Extract（21:31 复核）已出到 **09-16 = 31.5 → 31 档**，与 provisional 值一致；当日 4 腿 1-3 / −$336.90 **不变**，此前担心的 31↔32 翻转未发生。
- **09-17 结算 31°C（`provisional`）**：官方 XML 仅到 09-16。判据 ① 结算站日内快照最高 **31.2 @14:30**（14:30 / 15:00 / 15:30 三次触及，15:40 起回落）→ 31 档；② 收盘后市场 **31 档 YES bid 0.999**（≥0.95 已定档）、其余各档 NO 0.999。
- **09-17 台账 = 5 腿 1-4 / 投入 $216.98 / −$88.94（−41.0%）**：YES30 @0.110（10:04 窗口外预案）、YES33 @0.080（同）、NO31 @0.520（同）、**NO32 @0.460（11:44 窗口内，当日唯一赢腿 +$69.14）**、YES32 @0.4233（12:54 窗口外留档）。另照录 10 条同腿加仓 / 留档行（按「同腿只记首推」折叠，不入账）。
- **新增「口径 10」（待 Eason 拍板）**：窗口外的「预案」与「研究留档」是否入账。本次采用**照录 + 每条腿取当日首个带金额版本**（与 9/15 窗口外腿一律入账的先例一致，并把 v0.19.3「留档 Kelly 用于回溯最优下注时点」的数据沉淀进台账）。若只计窗口内可执行腿（11:03 NO31 @0.570 + 11:44 NO32 @0.460）= 2 腿 1-1 / 投入 $141.55 / **−$13.51**（差 $75.43）。
- **累计 32 腿 / 8 个结算日 / 11-21 / 投入 $2,556.38 / 净 −$470.44 / ROI −18.4% / 4 盈 4 亏**；NO 15 腿 9-6（+8.8%）、YES 17 腿 2-15（−75.7%）。
- **归因新增（重要）**：§5.2 里「**单档硬上限不依赖 μ**」的前提被 9/17 部分证伪 —— 该上限 `2Φ(0.5/σ)−1` **对 σ 高度敏感**，而日内 σ 随升温**单调收敛**（11:00 的 0.93 → 午后的 0.35）。「卖单档 YES（= 买该档 NO）」的 edge 全部来自所填的 σ：用**全天残差 sd** 去卡**午后的条件分布**，会把「当日最热那一档」的概率系统性低估 2–3 倍。9/17 是当日两个反例的对照组 —— 11:03 NO31 与 11:44 NO32 用的是**同一个** σ=0.93 硬上限论证，前者输（结算正是 31）、后者赢（结算不是 32）→ **赢的那条不是判据对了，是热的那一档恰好不是它押的那一档**。「伪安全 NO」族由 2 战 0 胜变为 **3 战 0 胜**。

## v0.19.3 — 2026-09-17 12:23｜窗口外研究观察须含留档 Kelly（文档，PATCH）

- 修订 §3.5「窗口外/不出手仍须披露 edge>8% idea」纪律：原「窗口外禁止给 Kelly 金额/份数」**作废**；改为 **窗口外研究观察仍须按 35% Kelly 给出 `金额 + 份数` 作为留档**，标注「研究留档，非执行指令」，用于回溯研究**哪个下注时点 edge 最稳、ROI 最高**。
- 区分「执行措辞」与「留档量化」：窗口外仍禁「现在买 / 立即下单 / 挂单成交」类执行指令，但允许量化留档。
- §3.3 窗口规则、§4.2-#11 禁止清单同步：由「禁带金额的执行指令」改为「禁带**执行措辞**的指令」，明确留档 Kelly 不受限。
- 数字 / 算法零改动（PATCH）。
- `.workbuddy` → `.codebuddy` 双副本；四处版本号 +0.0.1 → v0.19.3。

## v0.19.2 — 2026-09-17 12:05｜新增两条输出纪律（文档，PATCH）

**改动类型**：**纯文档 / 纪律，代码零改动** → PATCH。输出数字不变（模型概率表本就由 hk_edge.py 产出），仅新增「必须输出」的硬性要求。

### 1. 新增：每次更新预测必须输出「模型各温度档位公允概率表」
- 即 `hk_edge.py` 的「公允P」列，全部覆盖档位（含尾档），与市场价并排；缺此表 = 输出不完整。

### 2. 修正：「只播实况、不再给仓位」做法作废，改为「窗口外仍须披露 edge>8% 交易 idea」
- 出仓窗口 10:30–12:00 之外仍**禁止带金额/份数的执行指令**（§4.2-#11 硬禁区不变）；
- 只要有 `|EV| > 8%`，必须在结论里列出该交易 idea（档位+方向+模型公允P+市场价+EV），标注「研究观察，非执行指令」；
- 禁止因窗口关闭而把 edge>8% 的腿整段删掉；内部一致性 edge 与单档硬上限 edge 即使窗口外也必须点名。

### 3. 同步范围
- `.workbuddy` → `.codebuddy` 双副本；四处版本号 +0.0.1 → v0.19.2。

---

## v0.19.1 — 2026-09-16 21:45｜结算台账回填 09-15（转 confirmed）与 09-16，并补两条口径缺口

**改动类型**：**纯数据 + 文档，代码零改动** → PATCH。但**输出数字会变**（`PNL.md` 新增 09-16 行、
23 腿 → 27 腿、累计 −$44.60 → −$381.50），验证方式见下。

### 1. 结算回填

| 结算日 | 结算档 | 依据 | status |
|---|---|---|---|
| 09-15 | **31°C** | 官方 `dailyExtract_202609.xml`（09-16 21:32 复核）**09-15 = 31.4 → 31 档**，与 provisional 值一致 | provisional → **confirmed** |
| 09-16 | **31°C** | ① 结算站日内序列最高 **31.4 @14:30**（`obs_1min_log.json`；`intraday_log.json` 同源，watch 记 31.5）；② 收盘后市场 31 档 **YES bid 0.999**、其余档 NO 0.999（`market_prices.py 2026-09-16 --depth 3`）。官方 XML 尚未出 09-16 | **provisional** |

**09-15 无需重新归因** —— 官方档位就是 31，当日 6 腿 1-5 / −$334.31 不变（此前担心的 31↔32 翻转未发生）。

### 2. 09-16 建议流水入账（4 腿 1-3）

按「照录全部流水、同腿只记首推」口径（§5.2 口径 1）登记 7 行 → 折叠后 4 条腿：

| 腿 | 首推时刻 | 首推价 | 建议投入 | 结果 | 盈亏 |
|---|---|---:|---:|---:|---:|
| YES30 | 09:05 | 0.26 | $152.50 | ❌ 输 | −$152.50 |
| NO32 | 09:05 | 0.83 | $477.50 | ✅ 赢 | **+$97.80** |
| NO31 | 10:01 | 0.52 | $215.00 | ❌ 输 | −$215.00 |
| YES32（11:30 反手） | 11:30 | 0.21 | $67.20 | ❌ 输 | −$67.20 |
| **合计** | | | **$912.20** | **1-3** | **−$336.90（−36.9%）** |

10:01 的两条改价版（YES30 @0.30 $100、NO32 @0.81 $315）照录但折叠为 `↳ 加仓`。
**口径说明见 §5.2 新增的口径 9**：当日多轮修正时入账用「首次给出那一版」——
若改用 10:01 修正版，当日为 **−$308.31**（差 $28.59）。

### 3. 新增两条口径（§5.2）

- **口径 8｜平仓 / 减仓建议不单独建腿**（本次发现的口径缺口）：`cost_usd < 0` 的行是按
  「**开仓卖出**」的空头口径计价的；平掉在册多头腿会（a）与买入行同键被折叠，
  （b）计价相反 —— 9/16 的「卖 29 YES @0.020 回收 $7.28」会被算成 **+$7.28**。
  故 11:30 的四腿全平与 13:40 的卖 32 NO 只在 note / 文件头登记（后者记为 void 行）。
  **要跟真实平仓必须用 `PNL_actual.md`。**
- **口径 9｜同日多轮修正的入账基准**（见上）。

### 4. `SKILL.md` §5.2 战绩表刷新（23 腿/6 日 → **27 腿/7 日**）

- 累计 **−$381.50 / ROI −16.3%（4 盈 3 亏，逐腿 10-17）**；`NO` 13 腿 ROI **+8.2%**、
  `YES` 14 腿 **2-12 / −72.0%**。
- 错误分类同步：押注封顶 **7 战 1 胜**、右尾注 **10 笔 1 胜**、伪安全 NO **2 战 0 胜**
  → 三族 19 条腿胜率 **2/19 = 10.5%**；新增结论 **「9/15 与 9/16 是同一个错误的两次复发」**
  （两天都把立论放在 31 档之外，而 31 档正是唯一落下的格子）。

### 验证方式

```bash
py -3 scripts/rec_pnl.py                 # 逐日表应含 09-16 / 31°C* / 4 腿 / 1-3 / −$336.90
py -3 scripts/rec_pnl.py --open          # 应为空（收口自检）
py -3 scripts/rec_pnl.py --markdown      # PNL.md 与上表一致
```

`PNL.md` 已重新生成（新增 09-16 行、逐腿明细新增 7 行）。

---

## v0.19.0 — 2026-09-16 21:00｜出仓窗口由「09:00–11:30」收窄为「10:30–12:00」，≥13:20 列为硬禁区

**触发**：用户问「最优的下注时间是几点」。用 `scripts/data/rec_pnl_log.csv`
（建议台账，含每条腿 note 里的首推时刻）做了一次**按首推时刻切段的结算 P&L 分层** ——
这是本项目口径最硬的一次时段检验：不是模型指标，是真金白银。

**结果（n=26 条可解析时刻的腿，跨 9/09–9/15）**

| 首推时段 | n | 投入 | 净 P&L | ROI | 胜率 |
|---|---:|---:|---:|---:|---:|
| 09:00–10:29 | 4 | $261.40 | −$150.29 | **−57.5%** | 1/4 |
| **10:30–11:59** | 4 | $355.00 | **+$149.37** | **+42.1%** | **3/4** |
| 12:00–13:19 | 3 | $124.20 | +$98.02 | +78.9% | 1/3 |
| 13:20–15:00 | 14 | $429.72 | −$429.72 | **−100.0%** | **0/14** |
| ≥15:00 | 1 | $103.00 | −$103.00 | −100.0% | 0/1 |

**≥13:20 合计 15 条腿、0 胜。** 二项检验：真实胜率 50% 时 15 连败概率 0.003%；
真实胜率假设 30% 时概率 0.47%。→ **结构性，不是运气，列硬禁区。**

**改动（纯文档，代码无硬编码窗口）**
1. `SKILL.md` §3.3-1 标题与执行行：窗口 `09:00–11:30` → **`10:30–12:00`**，重心 11:00；
   删除「09:00 起」的依据（新证据是负的）。
2. §3.3-1 新增「实测 P&L 分层」小节（含上面的表 + 三条解读 + 样本弱点声明）。
3. §4 禁止清单第 11 条、§1 流程第 5 条同步为 10:30–12:00 / 13:20 硬禁区。
4. 全文版本号 → v0.19.0（SKILL / CHANGELOG / README / `hk_edge.py VERSION`）。

**样本弱点（写明，不遮掩）**：10:30–11:59 只有 4 条腿、来自 2 天（9/14 的 11:05 三条 +
9/15 的 11:00 一条），**不足以当铁律**。真正统计显著的只有 ≥13:20 的 0/15。
**同一次分析里另一条结论被自我否决**：原以为「09:00–11:30 是正权重窗口」有 a\* 支撑，
但把 decision_log 的样本从 4 天扩到 7 天后，上午 a\* 从 +1.37 掉到 **−0.12**（n=16）——
一个离群日（9/09）就能翻转符号 → **a\* 的时段统计不稳健，不能作为窗口依据**；
本次窗口依据改用 P&L，不再用 a\*。

---

## v0.18.0 — 2026-09-16 15:00｜σ 全部换成有样本外来源的值：t(5) 核、双边对齐 L、禁用 n≤5 样本内 sd

**触发：9/15–9/16 连着两天的「早上算出来的概率离谱」。** 复盘（n≈300 的长历史回测 + 可靠性图）
给出的结论不是「算错了」，而是**在没有统计支持的地方调参**。四条一起改，外加撤掉一条自己刚加错的。

| # | 旧做法 | 问题 | 新做法 |
|---|---|---|---|
| 1 | Δ 表用 n=4 的样本内 sd（0.19–0.27） | 同一量在 n≈300 的样本外是 **0.93–1.06，差 4 倍**；由此造的针尖分布连续两天被市场证伪 | 新增 `sd_guard()`：**n < 6 的样本内 sd 一律不采信**，回落先验并打印替换 |
| 2 | 残差核与 δ 卷积都用**正态** | 可靠性图：预测 10–20% 的档实际命中 **26.7%** —— 极尾比正态厚 | 改 **t(5)**：新增 `t5_cdf`（闭式）+ `t5_nodes`（等概率节点、归一化到 sd=1）；`bucket_probs` 与 `l_integrate` 的正态支路同步 |
| 3 | L 用「站温插值到整点」再减网格 | 可用样本只有 **1–3 个**，却在 ±0.3°C 尺度上调参，而主项缺口 1.2°C；09-16 它把 09:05 的 31.00（误差 −0.20）「修正」成 29.95（误差 −1.25） | 改为**双边同时刻对齐**：网格线性插值到各采样时刻，n 提到采样点数（实测 **n=31**）；补丁整体删除 |
| 4 | `SD_L_DEFAULT = 0.60` | 来自 09-15 的 **09h/10h 两个**整点样本（n=2） | 标注为**占位值、不可引用**；替换路径入 P2 |
| 5 | `l_integrate --bias` 默认 **−0.36** | 那是 **09-15 当天**的水位，等于把某一天的运气写成所有日子的默认输入 | 默认改 `None`，缺省回落气候先验 `calib['bias']`（+1.008） |

**撤掉一条（同一个提交里自己加、自己撤）**：曾给 δ 的 σ 加下限 0.90（= Δ(h) 跨日样本外 sd）。
**撤回理由是重复计入** —— 分位表本身就是 R 的**无条件**分布、已含全部偏暖偏冷的日子，
再叠 0.9 会把 need=0.5 处从 7% 膨胀到 33%，是**无依据的膨胀，比原缺陷更危险**。
正解需用 ρ̂ 自身的误差（现有 6 天样本 0.75–3.00，n 太小）→ 列入 P2，需先攒 `obs_1min_archive.json`。

**输出数字确有变化（故走 MINOR，非 PATCH）**

- 主公允表：t(5) 使**中心档更集中、次邻档变薄、远尾变厚**（σ=0.5 时 32 档 0.1% → 0.5%；μ=30.5/σ=0.77 时 30 档 48.4% → 45.5%… 中心档不变、次邻档降）。
- watch 的 L：09-16 14:30 实测由旧错位中位 **+1.80** → 双边对齐中位 **+1.47**（n=31）。
- `l_integrate --bias` 不给时由 −0.36 → +1.01。

**验证方式**（本次跑过、可复现）

```bash
py -3 scripts/hk_edge.py --version                      # 0.18.0
py -3 scripts/hk_edge.py --date 2026-09-16 --market "31:0.86,32:0.15,33:0.01"
py -3 scripts/hk_edge.py --watch --bankroll 2100         # 看「水位口径对照 n=31」一行
py -3 scripts/l_integrate.py --observed-max 31.5 --current 31.4 --hour 14
```

自检：`t5_cdf` 对 0 / ±1.476 / ±2.015 / 3.365 分别返回 0.5 / 0.1 / 0.05 / 0.99
（**首版把分子写成 u(3+5u²)，F₅(2.015) 只给 0.939 —— 端点自检查不出来，必须用分位点自检**）。

---

## v0.17.0 — 2026-09-15 21:45｜台账收口：`void` 不入账、首推口径由脚本强制、`--status`、结算 09-14/09-15

**触发：9/15 收盘按 §5.3 流程结算时 `rec_pnl.py` 交不出当日结果。** 该日 9 条
`outcome=void`（作废 / 撤回 / 未发出）的行被当成「未结算」→ 逐日表打成 `280-19+9开`、
当日盈亏与 ROI 全是「—」、全表合计把整天排除在外。同日另发现 9/15 的入账行里有 13 条是
**同腿加仓 / 改价**，逐条计入 —— 违反 CSV 头自己声明的「同腿只记首推」口径（NO31 一天记了 6 次）。

**三个 bug + 一处数据损坏，一次修掉**

| # | 问题 | 后果 | 修法 |
|---|---|---|---|
| 1 | `outcome=void` 不在 `win/loss` 里 → 被算作 open | 该结算日**永远收不了口** | 新增 `is_void()`；void 行不进成本 / 回收 / 胜负 / 未结算任何一项 |
| 2 | 同腿加仓行逐条入账 | 同一论点被计数 N 次，违反自身口径 | 新增 `classify()`：按（结算日+腿+方向+档位）只取**首次**，其余标 `↳ 加仓` |
| 3 | `--settle --write` 固定把 `settle_status` 写成 `confirmed` | 官方源未出时要手工改回，且会**顺带回填 void 行**（重复计数） | 新增 `--status {confirmed,provisional}`；`settle()` 跳过 void 行；变更逐条打印 |
| 4 | CSV 第 39/40 行 note 含**半角逗号** `[29.0,30.0)` | 列数 11 → 读入时 note 被**静默腰斩** | 改为 `29.0–30.0`（en dash）；复查全部 45 行列数 == 10 |

另：`save_rows` 用的 `csv.DictWriter` 默认 `lineterminator='\r\n'`，一次 `--write` 会把整个 CSV
从 LF 翻成 CRLF（git 里表现为整文件重写）→ 显式改为 `'\n'`。新增 `--all-legs` 可关闭折叠看全量流水。

**输出数字变化（因此不适用 PATCH，走 MINOR）**

- `PNL.md` 新增 2026-09-15 一行；合计由 17 腿 → **23 腿**。
- 胜-负计数由「按盈亏正负」改为「**按 `outcome`**」：卖出腿（`cost_usd<0`）赢的时候净额是
  小负数，按正负计会被误记成亏损腿。

**验证**：`py -3 scripts/rec_pnl.py` 打印「已折叠同腿加仓 13 条、作废/撤回 9 条」；
9/15 由 28 条记录折叠为 **6 条腿**、成本 **$209.60** —— 与 `reviews/2026-09-15_review.md`
的「台账口径（同腿只记首推）$209.60」**逐位一致**（独立交叉验证）→ 当日 **−$334.31**；
累计 **23 腿 9-14 / 净 −$44.60 / ROI −3.1% / 4 盈 2 亏**。

**结算回填**

- **09-14 → confirmed**：官方 Daily Extract 已出 `09-14 = 28.7 → 28 档`，与 provisional 记录一致。
- **09-15 → 31°C（provisional）**：① 结算站日内序列最高 **31.3 @14:10**；② 收盘后市场 31 档
  **bid 0.990**（≥0.95 视为已定档）；③ 官方源尚未出 09-15。

---

## v0.16.29 — 2026-09-15 16:05｜SKILL.md **结构重组**：口径单一权威化、失效规则归档、禁止清单集中

**触发：Eason 指出「SKILL.md 已膨胀到约 1.5 万字，同一口径在 6 个章节重复，且若干已被实况推翻的规则
仍留在正文（如 v0.16.21 的『日内回撤 ≥0.5 腰斩』）。下次误用的风险高于缺信息的风险。」**

**这是纯文档重组，`scripts/` 无任何代码改动，所有输出数字逐位不变。**

**改前 → 改后**

| 指标 | 改前 | 改后 |
|---|---:|---:|
| 行数 / 字符 | 2879 / 105,513 | 2,714 / 98,594 |
| 二级标题 | **47 个**（平铺） | **9 个**（分区） |
| 三级标题 | 69 | 35 |
| 「优先级最高」标签 | **6 处**（已失去区分力） | 0（改为「一票否决」清单） |
| L 口径定义 | **9 处**分散 | 1 处（§2.2） |
| R 基准定义 | 4 处，其中 **1 处与另 3 处直接矛盾** | 1 处（§2.1） |
| 气候先验 | 5 处 | 1 处（§2.7） |
| k 放大比 | 3 处 | 1 处（§2.3） |
| 已失效规则 | 混在正文，无标记 | 全部移入 §7-A 并标 ❌ |

**新骨架**：§0 怎么用这份文档（含 6 步盘中判读顺序）→ §1 事实层 → **§2 口径权威表（2.0–2.10，每个量
只有一处定义）** → §3 决策流程 → **§4 禁止清单（38 条一票否决，按类集中）** → §5 复盘与台账 →
§6 发版流程 → §7 附录（A 已推翻归档 / B 案例库 / C–G 工具与局限）。

**修掉的最严重一处：内部直接矛盾。**
`A3c「分位表的基准是当小时实测值」(v0.16.9)` 与 `「R 的基准必须是 running max」(v0.16.22)`
**物理推理完全相反**，且前者把**错误口径**命名为"严格口径"。v0.16.22 已用实况证伪，
但原文**没有任何失效标注** —— 读者按标题顺序读到 A3c 会直接采用错口径。
现已整节移入 §7-A-1 并注明推翻理由。

**§7-A 归档 13 条已失效规则**：A3c 基准口径、v0.16.21 腰斩、冷池两代错误判据、
σ_flicker 加权平均（应用 max）、d≥0.6 写死阈值、向市场收敛一半、7 条无条件口径力荐 NO31、
「官方修订纳入时滞」推论、≥90%卖/≤10%留、`--watch` L 未对齐、13:00–15:00 可判定格、
282 份 YES32 假设持仓、ASOS <1°C 阈值。

**§4 禁止清单 38 条**，分四类（概率与口径 16 / 头寸与仓位 12 / 数据源与实现 11 / 流程与记录 8），
每条一行并回指详细节。此前这些规则散落在 16 个 🚨 标题里，无统一入口。

**校验**：从旧文提取 933 个独特数值，新文覆盖率 **95.9%**；逐项复核后确认缺失者
均为格式差异（尾空格）或已合并的同义项。补回 5 处真遗漏：整节 A3d「盘后实质锁定判定
`need > R_max`」（含 28 YES 225 份持有 vs 平仓差 $33.59、风险收益 1000:1）、
v0.16.21「薄盘市场隐含 L 全天不可用」（含 15 分钟 5 个来回、深度 <200 份阈值）、
百年序列尾档验证表（1991–2025 vs 2016–2025，尾档差 2.5 倍）、
「Δ 累计值不含时间信息 + 网格 14 时见顶」纪律、案例库规则 0（「会继续升温」≠档位腿）。

---

## v0.16.28 — 2026-09-15 15:40｜气候先验必须做「季节内漂移」修正；官方 fnd 自身偏差当场标定

**触发：给 9/16 定价时，无条件气候先验给中心 31.84，而四条条件路径全部落在 30.3–30.9 —— 差 1.0–1.5°C，横跨两个整数档。**

- **气候先验 → 滚动漂移修正**：用「当月已结算日的实测日最高」对同窗口气候均值做差。
  9/16 实测漂移 **−0.94**（当旬已实现 31.34 vs 气候 32.28）→ 修正后中心 **30.90**，与另三条路径收敛。
  硬规则 4 条：必须同报「原始先验/修正后」；样本 ≥5 天才启用；漂移 ≠ 趋势（超 10 天不迁移）；
  修正后再与市场比，差额才是候选错配。
- **官方 fnd 自身偏差当场标定**：9/14 官方对 9/15 报 32、实测 31.4 → 官方偏暖 **+0.6**；
  对 9/16 官方报 31 → 修正 30.4。**样本仅 1 天，只作第二条交叉验证轴，不得单独采信。**
- 与 v0.16.27 不冲突：气候频率仍只能当先验；本条只是指出**先验本身可被目标日之前的已实现观测更新**。

---

## v0.16.27 — 2026-09-15 15:20｜D-1 的「NO31 81–86%」是基准率谬误：五层遗漏与 6 条规则（**优先级最高**）

**触发：Eason 追问「31 NO 的概率算出来一度高达 90%，是不是漏算了什么」。答案：是，五层。**

那个数字的出处是 **D-1 报告（§1 表）：`NO 31°C，公允 P(结算≠31) = 81–86%，无条件，主推`**，
以及 §3 表最极端的一格 **n=17 → P(非31) = 94.1%**。

### 五层遗漏

| # | 遗漏 | 量化 |
|---:|---|---|
| 1 | **用无条件频率替代条件分布** | 无条件 18.8% vs 条件 31.3%（市场）/ 36.1%（混合）/ 36.3%（脚本）→ 缺口 **12–17pp** |
| 2 | **7 条口径是嵌套切片，不是 7 次独立验证** | `n=341 ⊃ n=238 / n=58 ⊃ n=21 / n=37 ⊃ n=17` 全部嵌套 → 有效证据 ≈ **1 份** |
| 3 | **细切放大小样本噪声，且挑了最极端那格** | n=17 时 1 个观测 = **5.9pp**；那格 95% CI = **[0%, 17.1%]**，下界是 0 |
| 4 | **显著性检验用错对象** | 「市场 31.3% vs 气候 18.8%，z=5.9」比的是条件量 vs 无条件量 → 检验无效 |
| 5 | **把模型算出的众数标成"尾部风险"（致命）** | 报告称「中心落在 31.3–31.7 是尾部风险」，而它自己的混合中心就是 **31.30**、官方预报 32 |

### 量化：这笔仓位本来不该存在

| 成交价 | 报告声称 EV | 用市场先验的真实 EV |
|---:|---:|---:|
| 0.700 | **+16.0%** | **−1.9%** |
| 0.716（实际均价） | — | **−4.1%** |
| 0.760 | — | **−9.6%** |

**18pp 差距全部来自基准率的选择。若 D-1 以市场价为先验，这笔 NO31 不会建仓 —— $320.77 不会亏。**

### 6 条规则（已写入 SKILL.md，v0.16.27 节，优先级最高）

1. **无条件频率只能作先验**，必须经当日似然（实况 + NWP + 官方预报 + 前日雨型 + 市场）更新；
   未更新的气候频率必须标 `UNCONDITIONAL` 且禁止用于下注。
2. **嵌套切片不是独立验证**：引用多口径必须写明嵌套关系 + 最小公共样本 + 有效独立证据数。
3. **`n<30` 禁止生成主推腿**（扩展 v0.16.26 规则 4）。
4. **与「市场 + 自有模型」同时差 >10pp → 默认自己错**；「对方是负信息源」不是豁免理由。
5. **「市场 vs 气候频率」的缺口不是 edge** —— 必须独立论证「当日为何异常」（9/15 从未论证）。
6. **风险段与仓位段必须同源同口径** —— 把众数标成尾部风险是一票否决项。

---

## v0.16.26 — 2026-09-15 15:00｜收盘复盘：结算 31 档，真实头寸全额归零；6 条硬规则沉淀

**结论先行：9/15 结算 31 档（HKO 当日最高 31.4°C），真实头寸 $373.84 → $0，已实现 −100%（本金 −14.95%）。**

### 一、结果

| 腿 | 份数 | 真实成本 | 回收 | 盈亏 |
|---|---:|---:|---:|---:|
| 29° YES | 185 | $51.80 | $0 | −$51.80 |
| **31° NO** | **448** | **$320.77** | **$0** | **−$320.77** |
| 32° YES | 9.8 | $1.27 | $0 | −$1.27 |
| 合计 | | **$373.84** | **$0** | **−$373.84** |

市场 15:00 已把 31 档报到 bid 0.990 / 32 档 ask 0.020。官方 Daily Extract 滞后 ~3 天 → **provisional**。
当天 11 条建议 10 条亏损；台账口径（同腿只记首推）净 −$209.60，唯一正贡献是被自己 void 掉的 NO29 卖出。

### 二、核心归因：**全天两次立论都在 31 档之外，而 31 档是唯一落下的格子**

```
09:15–10:00  论点 A：「今天到不了 31 档」→ 买 NO31 @0.716 / YES30 / YES29
12:50        站点冲到 31.3，论点 A 被实况证伪 → 仓位纹丝不动
13:20–14:43  论点 B：「会从 31.4 再冲到 32」→ 连续四次加 NO31
A ∪ B = {<31} ∪ {≥32} = 「非 31 档」—— 正好就是 NO31 的定义
```

### 三、6 条硬规则（已写入 SKILL.md，v0.16.26 节）

0. **「会继续升温」不能直接映射成档位腿** —— 必须先算出「跨到目标档所需增量」并单独查其达成率。
1. **NO N 是伪安全**：必须分解成分支（`≤N−1` 会随升温单边清零），禁止用合并百分比评估安全性。
2. **同一条件格重复加仓上限 = 首次建仓金额**（9/15 四次加仓合计 $250.67 = 首推 2.5 倍，全部落在 `rm=31.4, gap≤0.5` 同一格）。
3. **撤回只作用于未成交挂单**，已成交腿另立条目，不得 void（9/15 把唯一正确的 NO29 卖出 void 掉了）。
4. **`n<30` 的条件格只报区间**，当日只用方向、不用幅度、不进 Kelly。
5. **「模型降级 → 市场降级」的 ~10 分钟窗口是已知不可修复**的结构性成本。

### 四、附：真实头寸的"意外保护"

用户的 NO31 均价停在 **0.716**（早盘成交），13:20 之后的四次低价加仓**一次都没执行** ——
若全部成交，NO31 成本会从 $320.77 扩到 $571.44，亏损扩大 53%。
**建议层面在同一格反复改价 4 次，本身就是该被规则 2 拦下的行为。**

---

## v0.16.25 — 2026-09-15 14:45｜gap 是台阶不是斜坡；网格模型是**边缘**概率不是条件概率（长期分歧解开）

**v0.16.24 发布后 10 分钟，状态就反转了 —— 而这恰好解开了前四轮的一个悬案。**

### 一、事件：10 分钟 −0.7°C，公允掉 6 倍

站点 14:20 的 31.1（gap 0.3）→ **14:30 的 30.4**（gap **1.0**）。
盘口 13 分钟内把 32 档 YES mid 从 **0.200 → 0.055 → 0.020**（10 倍）。

v0.16.24 基于 14:20 状态算出的 P(32)=0.42，在 14:30 之后正确读数是 **0.07**。

### 二、gap 的两侧是台阶（VHHH 九月，`rm ≈ 31`，数字 = P(升到下一档)）

| 时刻 | `gap ≤ 0.5` | `gap > 0.5` | 倍数 |
|---|---:|---:|---:|
| 14:00 | **46.4%** (n=28) | **10.5%** (n=19) | 4.4× |
| **14:30** | **30.8%** (n=26) | **5.0%** (n=20) | **6.2×** |
| 15:00 | 14.3% (n=21) | 4.5% (n=22) | 3.2× |
| 14:30（全部 rm 级别） | 27.9% (n=183) | 7.5% (n=147) | 3.7× |

两条同时成立：**① gap 跨 0.5 是台阶（掉 4–6 倍）；② 时间本身也在衰减**
（同为 gap≤0.5：14:00 46.4% → 14:30 30.8% → 15:00 14.3%）。
→ 引用任何概率都必须**同时给 hour 与 gap 侧**。

### 三、长期分歧解开：网格模型给的是**边缘概率**

```
14:30, rm≈31:  gap≤0.5 → 30.8%(n=26)   gap>0.5 → 5.0%(n=20)
               加权平均 = 19.6%
l_integrate 同时刻                        = 22.0%   ← 吻合
```

前四轮反复记录的「站点口径 43% vs 网格模型 24%，无法判定谁对」，
**根因是口径错误，不是模型错误**：网格模型的 R 分位表按「时刻」建，
**没有 `gap` 这个自变量**，所以它给的是条件概率的无条件平均（边缘概率）。
两者本来就不该相等。

**规则**：报概率必须声明是条件还是边缘 —— **下注用条件（gap 分侧）**，
**校准 / 算 Brier 用边缘**。

### 四、新硬规则：仓位建议必须绑定「数据时刻」并写作废条件

本轮实测：14:20 状态的 P(32)=0.42 在 15 分钟后被推翻成 0.07。
**建议的有效期 = 到下一个 10 分钟观测为止。** 每条建议必须附：

```
数据时刻：HH:MM（用的是哪一刻的实况）
作废条件：若下一批 0.1°C 实测 gap 越过阈值，本条立即作废并重算
```

### 五、当日结论（14:30 状态，gap = 1.0）

`P(31)=0.910 ｜ P(32)=0.070 ｜ P(33)=0.015 ｜ P(≥34)=0.005`（市场 `0.960/0.020/0.000`）

真实头寸：**成本 $373.84｜市值 $9.06｜公允市值 $41.01（未实现 −97.6%）**
（15 分钟前是 −80.5%）。动作：
- **持有 NO31 448 份，一份不卖** —— 公允 9.0% vs bid 2.0%，
  且即使按市场口径（P(32)=0.020）期望回收 $9.8 也与卖出的 $9.06 打平；
- 唯一小额正 EV（合计约 **$22**）：买 NO31 @0.060（246 份 / $14.76，EV +50%）、
  买 YES32 @0.030（206 份 / $6.18，EV +133%）、买 YES33 @0.001（1072 份 / $1.07，EV +1400%）；
- **不做**：买 YES31 @0.980 对冲（−7%）、卖 NO31 @0.020（−78%）。

未解：`gap>0.5` 那格 n=20（标准误 ±4.9pp），且 HKO/VHHH 的 R 幅度不同，
「5.0%」的置信区间很宽（约 1%–12%）；14:30 的 −0.7°C 归因未定
（Happy Valley 纹丝不动，像是局地海风，但那意味着**二次弹回**不能排除）。

---

## v0.16.24 — 2026-09-15 14:35｜「已创 X」的条件概率必须乘口径因子 r=0.54；真实头寸首次到位

**两条主线：一个是概率口径的系统性高估源，一个是 5 轮假设成本的清账。**

### 一、结果条件 ≠ 时间条件（本轮最重要的发现）

「已经创下 31.4 的日子，最终有几成到了 32？」直接读 `maxt_HKO.csv`：

| 窗口 | n | P(≥31.4) | P(≥32.0) | 结果条件 |
|---|---:|---:|---:|---:|
| 2015–2024 | 300 | 59.3% | 48.0% | **80.9%** |
| 2005–2024 | 600 | 52.0% | 38.8% | 74.7% |
| 1995–2024 | 900 | 41.0% | 29.3% | 71.5% |

HKO 九月日最高是**双峰**的：只有 **19.1%** 的日子最终停在 [31.4, 32)，
31.x 本身是个坑。于是 80.9% 看起来是极强的看多信号 —— **但它是错的**：
`{已≥31.4}` 里混进了大量「**晚**到 31.4」的日子（15、16 时才到，随后收工），
它们已经没时间了却照样计入分母。

**正确口径是 `P(final ≥ next | rm(h) = level, gap ≤ 0.5)`。** HKO 无逐时站史，
用 VHHH 逐时存档同时算两种口径，定义站点无关的形状因子：

```
r = P(final≥next | rm(h)=L, gap≤0.5) ÷ P(final≥next | final≥L)
```

VHHH 九月 n=330 实测：时间条件 46.4%（n=28）÷ 结果条件 85.5%（n=283）= **r = 0.54**。
→ 结果条件**高估 1.84 倍**。搬到 HKO：`80.9% × 0.54 = 43.9%`，
与「VHHH 时间条件直测 × 跨站折扣 0.90 = 41.8%」**独立收敛**。

**硬规则**：凡按**最终日最高**（outcome）切出来的条件频率，**一律 × 0.54 再使用**；
禁止把 outcome 条件概率直接当今日概率报出。否则「已经到、就此收工」的
那一档会被系统性低估。

附带锚点：VHHH 九月 `gap≤0.5`（n=193）→ P(创新高) = 37.8%，
新高时刻分布 **15 时 273 次 / 16 时 191 次 / 14 时 142 次** → **14:00 不是截止线，15:30 才是。**

新增 `scripts/reach_next.py`（双口径并列 + r 因子 + 合成区间）。
⚠ 同一脚本暴露的一条边界：**整数分辨率的 ASOS 不支持形态级条件化** ——
想切「早峰 + 深回撤 + 回到峰位」，`pk_t` 被平台期拉到很晚 → **n=0**。

### 二、真实头寸首次到位，前 5 轮的「假设成本」作废

用户发来持仓表截图（`YES Shares / AVG / NO Shares / AVG`），三处与假设不符：

| 腿 | 前 5 轮假设 | 截图真实 |
|---|---|---|
| NO29 | 172 份 @0.700 | **已平仓（不在表里）** |
| YES30 | 17 份 @0.340 | **已归零（不在表里）** |
| YES32 | **282 份 @0.060** | **9.8 份 @0.130** |

**组合的真实形态和我前几轮的描述完全不同**：YES32 的对冲早已不在，
实际是 **NO31 448 份 @0.716 的单腿二元赌注**（只在日最高 ≥32.0 时赢）。

新增 SKILL.md 硬规则《头寸截图里有 AVG 时，过去所有「假设成本」全部作废》：
表里没有的档位 = 已平仓、AVG 列即真实成本、每轮重新数表、
**成本绝对值不进入任何决策**（沉没成本）。

### 三、当日结论（14:30）

`P(31)=0.530 ｜ P(32)=0.420 ｜ P(33)=0.045 ｜ P(≥34)=0.005`
（市场 `0.725 / 0.200 / 0.008`，隐含峰值 31.41 ≡ 已观测最高 31.4 → 市场在抄实况）

真实头寸 **成本 $373.84｜市值 $72.86｜公允市值 $214.68**（未实现 −80.5%）。
**动作：一腿不卖**（卖出侧 EV −190% ~ −250%），
买 YES32 ≤0.280 最多 138 份（EV +50%）、买 YES33 ≤0.012 最多 113 份（EV +275%）、
**不买 YES31 @0.840 对冲（EV −36.9%，风险管理的正解是仓位规模不是买保险）**。

未解：站点口径（43%）与网格模型（24%）的分歧仍在，**没有独立证据能判定谁对**；
根因是「k 在无条件组上标定、用在条件组上偏小」，需 HKO 逐时站史直测。

---

## v0.16.23 — 2026-09-15 14:15｜条件变量改判为「当前 gap」；放大比 k 随时段上升；气候基线改近 10 年

**这是修正版**：v0.16.21 当天立的「盘中回落 ≥0.5°C → 概率腰斩」**当天就被实况推翻**。
09-15 日内最大回撤 1.4°C（31.4→30.0），按旧规则概率腰斩到 11.7%；
但 14:00 站点已回升到 31.1、只差峰位 0.3°C，实测条件应取 **46.4%** —— 差 4 倍，
而当天市场恰好按后者走（32 档 0.205→0.397）。

三条新硬规则：

1. **条件变量是「当前 gap」，不是「今天曾经回落过」。**
   VHHH 九月 n=330，14:00 按 `gap = rm − cur`：gap≤0.5 → P(创新高) 37.8%，
   gap>0.5 → 11.7%。按 rm 级别细分达 **4.4 倍**：rm=31 时 46.4% vs 10.5%。
   历史最大回撤降为**次级记录**，不得覆盖 gap 主判。
   附：VHHH 九月 14:00 后仍有 **38.8%** 的日子成峰（14 时 66 天 / 15 时 44 天 / 16 时 14 天）。
2. **放大比 k 不是常数，随小时单调上升。**
   同日同源：11 时 1.58 → 12 时 1.85 → 13 时 2.41 → **14 时 2.64** → 15 时 **4.93**。
   网格午后塌陷比站点陡 → 越晚 k 越大。**引用 k 必须与 `--hour` 同时给。**
3. **气候基线必须指定窗口，默认近 10 年。**
   `maxt_HKO.csv` 九月日最高：1995–2024 P(≥32)=29.3%；**2015–2024 P(≥32)=48.0%、P(≥33)=24.0%**。
   气候趋势抬高 18.7pp / 12.3pp —— 读基线还不够，还要指定窗口。

另立一条使用边界：

4. **`l_integrate` 在实况远超网格中心时整列降级。**
   当日 `g_rest + L = 30.40°C` 而实况 31.4 → 它给 31 档 73.5%，市场 71.5%，
   站点条件只给 ~54%。判据 `rm − (g_rest + L) > 1.0°C` 时改走站点条件频率。

修改：
- `scripts/station_r_probe.py`：新增 k 随时段表、`gap × rm 分档`表、
  `station_r()` 补 `gap` 字段、今日 base 改为**双重条件化**（gap + rm 级别）
- `SKILL.md`：重写腰斩专节（改为 gap 主判 + 反面教材）、新增 3 个专节、更新脚本表
- `scripts/hk_edge.py`：VERSION

当日结论（14:00）：实况深 V 二次拉升 30.0→31.1（gap 0.3），公允
**P(31)=0.58 / P(32)=0.38 / P(33+)=0.04**（四路锚定 46.4% / 40.4% / 48.0% / 24.2%）。
市场 10 分钟内 32 档 mid 翻三次（0.320→0.397→0.280），深度仅 43–52 份 → 薄盘噪声。
唯一动作：买 NO31 @≤0.345 最多 60 份、买 YES32 @≤0.345 最多 52 份（均受深度卡死，合计 $38.3）。

---

## v0.16.22 — 2026-09-15 13:45｜watch 与 l_integrate 同源；R 的基准定案为 running max

**这是修 bug 的一版**：`--watch` 与 `l_integrate.py` 在同一时刻给出相差 8 倍、
方向相反的概率（3% vs 24.4%），根因是 watch 支路有三处口径错。

### 修复

1. **R 的基准 = running max，不是当前瞬时值**（最严重）。
   分位表定义是 `R(h) = M − running_max(h)`（见 `build_remaining_rise.py` 表头）。
   watch 长期用 `need = b − 当前值`，涨势日两者相等所以从未暴露；09-15 回落 1.2°C 时
   need 被从 **+0.6 虚增到 +1.8**，等于隐含「已回落的部分会自动涨回去」。
2. **δ 归零**：站点越过网格全天峰值时（今天 31.4 > 29.49），网格的午后塌陷不再是信号。
   旧实现让 δ 被钳到 −1.00 下限，把口径失效转成硬惩罚。—— 与 v0.16.21 在
   `l_integrate` 修的同一个 bug，当时只修了一边。
3. **新增 `hk_edge.station_reach_prob()`**：watch 的概率从此与
   `l_integrate.bucket_probs_at_L` 的经验支路**逐字同源**（need÷k_R + δ 归零 + 回落 ×0.5）。
   ⚠ 两者仍不相等且这是结构性的：`l_integrate` 额外与「正态残差支路」各半混合
   （后者午后偏高达 43%），把 11% 抬到 24.4% —— **是同一模型的两个组件，不是两个证据**。

### 新增规则（`SKILL.md`）

- **R 的基准是 running max**（含 watch bug 的复盘表）
- **VHHH ASOS 是整数分辨率**（202138/202138 样本小数为 0）→
  `station_r_probe` 里「回落 ≥0.5」与「≥1.0」是同一子集（n 都是 120），
  `P(R≥0.6)` **不可分辨**；只能引用 ≥1.0°C 整数阈值
- **已经「确定赢」的腿不要卖**：变现不是理由，流动性有没有去处才是
  （撤销 13:00 轮「卖 NO29 换现金」的建议，那笔净损 $0.2 且现金无去处）

### 当日口径结果（13:45）

`P(31)=0.755 / P(32)=0.22 / P(33)=0.023`；市场 `0.785 / 0.205 / 0.007` → **无 edge**。
四条独立路线跨 11%–43%（经验支路 11% / VHHH 条件实测 25.8% / l_integrate 混合 24.4% /
正态残差支路 43%），市场 20.5% 正好落在中间。唯一动作：买 NO31 ≤0.220 最多 180 份。

### 未解

**k=2.4 是在无条件组上标定的**（R 均值 0.60/0.25），在「回落条件组」上不够 ——
经验支路 11% 与 VHHH 同条件实测 25.8% 仍差 2.3 倍。l_integrate 的 50/50 混合碰巧落在
24–26%，但那是两个偏差互相抵消，**不是标定**。需要 HKO 逐时站史才能直测。

---

---

## v0.16.21 — 站点口径 R 放大比 k=2.41（同点位）；回落腰斩；冷池 gap 幻影（2026-09-15 13:20）

### 🚨 四条新硬规则

1. **站点口径 R / 网格口径 R 的放大比 k = 2.41，且必须在同一坐标上算。**
   用 Iowa Mesonet 的 **VHHH 机场 ASOS** 逐时存档（2015–2025，20 万条）对算：
   日较差只差 1.39 倍，而 R13（13 时后剩余升温）差 **2.41 倍** ——
   网格不是等比压缩，它专吃日内尖峰。
   同日踩坑：先用「ERA5@**HKO** 点 ÷ **VHHH** 站」得到 4.68（恰好对上旧文的「4.7 倍」），
   **那是错的** —— 混进了站点差。同点位重算 = 2.41。
2. **盘中「冲高回落 ≥0.5°C」使剩余升温概率腰斩。**
   VHHH 九月 n=330 → 回落子集 n=120：P(R≥1.0) 从 51.5% 掉到 **25.8%**，R 中位从 1.0 归到 **0.0**。
3. **冷池 gap 必须是「最大回撤」，不是日较差。**
   旧 `_cold_gap_from_logs()` 取 `max(vals) − min(vals)`，而序列从早上 08:50（日最低附近）开始
   → 任何晴热天都触发，把主表上三档整段作废。2026-09-15 记下的 `gap = 3.7` 就是
   `31.3（12:50 峰） − 27.6（08:50 晨低）`，当天**从未**出现 3.7°C 的回落。
   改为 `max_t (running_max(t) − value(t))`。
4. **gap 的两个端点必须同源同刻。** 旧实现用「三源取最大的 `floor_max`」减
   「延迟 10–25 分钟的 0.1°C CSV 当前值」，转折点必然出错。现在一律用同一序列。
   → 这是本工具第三次栽在「跨源相减」（前两次：逐时偏差 vs 日最高偏差、best_match vs 7 模式平均）。

### 新增

- `scripts/station_r_probe.py` —— 站点口径 R 探针（可 `--refresh` 重下 ASOS；
  缓存 `data/_vhhh_asos.csv`，新增 `data/_era5_vhhh.json`）。
- `data/_era5_vhhh.json` —— ERA5 在 VHHH 坐标的逐时序列，专供同点位放大比。

### 修改

- `scripts/l_integrate.py` —— 四点口径修正，新增 `--r-scale`（默认 2.4）：
  ① 站点越过网格全天峰值时 **δ 归零**（旧实现被钳在 −1.00 下限，
  等于断言「今天比气候少升 1°C」，把口径失效转成硬惩罚）；
  ② R 阈值除以 `k_R`；③ base 改回 **running max**（旧代码用当前瞬时值，
  与分位表 `R = M − RM(h)` 的定义不符，今天多要了 1.2°C）；
  ④ 回落 ≥0.5°C 时概率 ×0.5。
  → 同日 32 档 **20.8% → 24.4%**，33 档 1.7% → 2.7%。
- `scripts/hk_edge.py` —— 冷池 gap 计算（两处）+ `VERSION`。
- `SKILL.md` —— 新增 5 个专节（放大比 / 回落腰斩 / 冷池 gap 两条 / 薄盘隐含 L / 整点对齐 L 符号异常）；
  薄盘警报从「<100 份不可信」升级为「<200 份不可信 + 价差 >0.15 只能用挂单 + 连采 3 次取最差侧」。

### 当日实证（13:00–13:20）

站点冲高至 **31.4**（12:40–12:50）后 13:00 回落到 **30.2**，全港同步回落
（TKO −1.0 / Waglan −0.8 / King's Park −0.6），典型海风重建 signature。
盘口 15 分钟内 31 档 mid 走完 0.520 → 0.880 → 0.540 → 0.880 → 0.710，
买卖价差最宽 **0.30** —— 本轮所有「市场隐含 L」均不可用。
三条独立路线收敛于 P(≥32) = 25.8% / 27.1% / 29–31%，采用 **P(31)=0.73、P(32)=0.24、P(33)=0.027**。

---

## v0.16.20 — 实况越过网格峰值；网格不再提供峰值，只提供形状（2026-09-15 12:33）

### 🚨 新增两条硬规则

1. **实况越过网格峰值后，网格只给「形状」，不给「峰值」**
   2026-09-15 12:20 站点 29.6 > 7 模式平均全天峰值 29.47（13h），且网格峰值时刻尚未到。
   → 禁止再用 `g_rest` 当峰值锚；峰值改走「校准 bias」+「条件经验 R × 振幅比」两条独立路径，
   并要求检验收敛（差 < 0.3°C 可信；> 0.8°C 按区间报）。
2. **`running_max` 距档位边界 < 0.5°C 时，L 积分会系统性抬高下边界档位**
   同日 12:33 实测 29 档：单点 19.0% vs 积分 25.2%（**+6.2pt**），
   机理是偏低 L 分支把整块质量推过边界落进下一档。
   → 该档必须以单点口径作为**下界**报出。

### 数据与结论

- 站点 12:20 = **29.6°C**（11:30 28.9 → 12:00 29.4 → 12:10 29.6 → 12:20 持稳），
  Happy Valley 31.5；全港 0–1 mm；`warnsum` 空；`flw` 11:45 提东北季候风 + 下午一两阵骤雨。
- 7 模式平均网格：11h 28.77 / 12h 29.26 / **13h 29.47（峰）** / 16h 28.83；best_match 12h 30.4 后塌陷。
- **幅度比修正**：`sd(站点日最高)/sd(ERA5日最高)` = 2.02/1.71 = **1.18**（2021–2024 九月）。
  与「ERA5 上尾薄 4.7 倍」是**两个不同的量**，不可互相替代。
- 市场 12:30：29° 0.030/0.060、30° 0.330/0.360、**31° 0.520/0.540**、32° 0.040/0.060
  → 归一 4.6/35.2/**54.1**/5.1%，隐含 **L = +1.73**（峰值 31.27），拟合 **RMS 15.2%**。
  市场是**单峰自洽**（非双峰），故不能再用「薄盘噪声」一笔打掉。
- 我方 L=+0.90：29° 25.2% / 30° **50.2%** / 31° **21.1%** / 32° 3.4%；中心 30.61；
  组合公允 $546.7 vs 市值 $390.2（**+40.1%**），市场自身口径 $460.9（+18.1%）。
- **建议**：卖 NO29 124 份 @≥0.935（回收 $116.6，公允−卖价 −19.2pt）；
  买 YES30 81 份 @≤0.360（$29.2）；NO31 448 份持有不加（已占成本 62.8%）；
  YES32 挂 @≥0.105 远价单。**31 档两个方向都亏 → 今天没有便宜的对冲。**
- `HKO fnd` 今日行自 11:30 起持续缺失（9/16–18 均 31）→ 市场对 31°C 的定价**无官方锚**。

### 其他

- `reviews/2026-09-15_prediction.md` 新增 §16

## v0.16.19（2026-09-15 12:11）—— L 的两套偏差不可混用

- 🚨 **新增最高优先级红线：`bias`（日最高偏差）≠ 当日整点对齐偏差（逐时）**
  两者的差 = 网格的午后塌陷幅度（对流参数化）。
  2026-09-15 实测：L̂(12h)=+0.14，网格 13h→16h 塌陷 −0.64（当日实际 0 mm、雷暴警告已取消）
  → L_day ≈ +0.78~+1.01，恰落回气候先验 +1.012（两条独立路径自洽）
- 后果：用逐时偏差均值去收缩会把峰值从 30.5 压到 29.7，**整整错一个档位**；
  v0.16.16 定的「L 必须收缩」方向对，**但被收缩的观测变量选错了**
- 当日实况：站点 12:00 = **29.4°C**（11:50 28.9 → 12:00 29.4，+0.5 一跳），
  **已超过 7 模式平均的 12h 值（29.26）并基本追平其全天峰值（29.47）**；
  `best_match` 12h 报 30.4 同时自报云量 83% → 自相矛盾的参数化伪影，再次坐实基线只认 7 模式平均
- 三口径收敛（L = +0.75 / +0.90 / +1.012）：峰值 30.22 / 30.37 / 30.48，**30 档全部 46%±0.5（全天最稳）**
- **市场拟合 RMS 从 11.9% 回到 5.5%** → 形状重新可信，市场隐含 L = +1.76（隐含峰值 31.30），
  不再能当「薄盘噪声」一笔打掉
- **P(31档) 在 sd_l = 0.30/0.46/0.60 下全为 20.5–20.6%**（右尾不受 L 不确定性影响）
  vs 市场 43.0% → **「覆盖 31 档 22 个点」是本轮唯一完全稳健的结论**
- 组合：模型公允 $549.0 vs 市值 $435.2（**+26.1%**）；市场自身口径 $460.9（+5.9%）
- 修正：§14 手算组合矩阵时**漏算了 NO29 在非 28 档的回收**，低估约 $70；脚本口径一直是对的
- 建议：NO31 448 持有不加；卖 NO29 48 份 @≥0.870（卖价高出公允 16.3pt，且与「做多 29 档」同向）；
  卖 YES32 全部 282 份 @≥0.080（公允 3.3%，卖价是公允 2.4 倍）
- reviews/2026-09-15_prediction.md 新增 §15

---

## v0.16.18 — 2026-09-15 · **市场 8 分钟内把 31 档推了 9 个点；条件基线说这是 17 倍**（MINOR，改数字）

**触发**：Eason 11:34 「更新预测」。头寸不变——
`NO29 172 / YES29 185 / YES30 17 / NO31 448 / YES32 282`。

### 本轮唯一事件：市场在 8 分钟内重估

| 档 | 11:26 | 11:34 | 变动 |
|---|---:|---:|---:|
| 29° | 0.250 / 0.290 | 0.200 / 0.240 | −0.050 |
| 30° | 0.280 / 0.340 | 0.220 / 0.260 | −0.060 |
| **31°** | 0.320 / 0.340 | **0.400 / 0.440** | **+0.080 / +0.100** |
| 32° | 0.090 / 0.110 | 0.110 / 0.140 | +0.020 |

市场隐含 L 从 +1.74 抬到 **+2.03**（隐含峰值 **31.60°C**）。
**同期没有任何支持它的新数据**：站点 28.8 停涨、全港 0 mm、无警告、
7 模式平均网格日最高 **29.60 → 29.46（下修）**、
HKO `fnd` 11:30 更新后**今日行被移除**（原 32°C 锚定依据消失）、
`flw` 提到「**東北季候風逐漸影響廣東東部沿岸**」（降温因素）。

### 🚨 独立裁判：条件基线说 31 档是 2.4%，不是 40%

`l_cond_check.py --rm11 28.8 --cur 28.8`（9 月、ERA5 n=330，条件子集 **n=125**）：

```
峰值 30.0 → 需 +1.2°C   条件下界频率 24.0%
峰值 30.6 → 需 +1.8°C   条件下界频率  8.0%
峰值 31.0 → 需 +2.2°C   条件下界频率  2.4%
峰值 31.6 → 需 +2.8°C   条件下界频率  0.0%   ← 市场隐含峰值
```

**历史上 9 月、11 时已升到 28.8 的日子（n=125），没有一次最终达到 31.6°C。**
即使把 ERA5 的振幅压缩（+2.2~2.4°C）考虑进去把频率放大 3–5 倍，也只到 **7–12%**。
**市场给 31 档 40.3% = 条件基线的 17 倍。**

另一面：市场很可能在锚定 HKO 今晨的官方预报 32°C（现已移除）。
**这是结算后复盘的第一顺位。**

### 四个 L 下的 P(31 档)

| 口径 | L | 峰值 | 29° | 30° | **31°** | 32° |
|---|---:|---:|---:|---:|---:|---:|
| 样本 sd 收缩 | +0.13 | 29.70 | 50.4% | 29.5% | **6.1%** | 0.7% |
| **基准折中** | **+0.20** | **29.77** | **49.1%** | **31.2%** | **7.0%** | **0.8%** |
| 保守收缩 | +0.46 | 30.03 | 42.5% | 36.9% | 11.4% | 1.5% |
| 气候偏差 | +1.01 | 30.58 | 26.9% | 41.6% | **22.7%** | 4.5% |
| 让市场对 | +1.74 | 31.31 | 15.9% | 34.8% | 32.5% | 12.1% |
| **市场** | **+2.03** | *31.60* | 21.1% | 23.1% | **40.3%** | 12.0% |

**即使完全相信「站点一定追到气候偏差」（L=+1.01），P(31 档) 也只有 22.7%，
仍低于市场 18 个点。** 两套独立方法（收缩贝叶斯 + 条件经验分布）同向。
但市场拟合 **RMS = 11.9% > 10%** → 按 v0.16.17 新规，**市场形状不可信，不能当先验**。
—— 这不是「我对市场错」的证明，而是「没有一方能自证」的证明。

### 新增两条纪律

1. **薄盘警报**：最优卖单 **< 100 份** 时，mid 的 5–10 点移动可能只是单笔订单，
   **不能当作信息**。本轮 31° 档最优卖单只有 **74 份**。
2. **网格峰值时刻随口径变化**：best_match **12h**、 7 模式平均 **13h**。
   触发条件必须写清用哪套口径，否则会**过早宣布锁定**。

### 结论（11:34 盘口）

市值 **$453.4（18.1%）**｜成本 $528.3｜**浮亏 −$75.0（−14.2%）**｜模型公允市值 $602.5（**+32.9%**）
市场自身口径 $514.3（+13.5%）→ 组合层 **+19.4pt**，**全部分歧在 31 档这一格**。

- **🔴 卖出 YES32 全部 282 份 @≥0.105**（公允 0.8–12.1%，中位 ~4%）→ 回收 $29.6
- **🔴 卖出 NO29 全部 172 份 @≥0.755**（公允 50.9% vs 卖价 0.760 = **偏贵 25pt**，且止盈 +8.6%）→ 回收 $129.9
- **✅ 买 YES29 @≤0.235，$60 / 255 份**（**EV +75.5%**，Kelly 给 $209/869 份，只给 1/3）
  —— 在 CLOB 上与「卖 NO29」是**同一笔交易**（互补）
- **31° NO 持有不加**：448 份 = 组合成本 **62.8%**，同一赌注的第 4 次加注
- **硬约束**：敞口封顶 **22%**；**12:00 前完成挂单，之后只做减仓判断**

评审：`reviews/2026-09-15_prediction.md` 新增 §14。

---

## v0.16.17 — 2026-09-15 · **网格基线取错了：过去 6 轮盘中分析全部用错了基线**（MINOR，改数字）

**触发**：Eason 11:24 「更新预测」。头寸 ——
`NO29 172 / YES29 185 / YES30 17 / **NO31 448（338→448）** / YES32 282`。

### 🚨 本轮唯一硬发现：网格基线与 `model_calib` 的 bias 不同源

`model_calib.json` 的 `bias = +1.012` / `resid_sd = 0.763` 是用 **7 个 NWP 模式的
「平均日最高」**（80 天）校准的，而 `hk_edge.OM_HR` / `l_integrate.load_context`
取的 `g_rest` 一直来自 **`best_match`**（服务端加权混合，不是那 7 个模式的算术平均）。

| 口径 | 12h | 今日日最高 | 11h 后最高 |
|---|---:|---:|---:|
| `best_match`（前 6 轮在用） | 30.4 | 30.4 | 30.4 |
| 7 模式平均（bias 的正确分母） | 29.60 | 29.60 | 29.60 |

**差 +0.79°C，恰好横跨 30/31 档分界线。** 等于凭空给峰值加了 0.79°C。
修正后日内三样本 L̂ 从 `+0.23 / −0.95 / −1.40` 变为 `+0.57 / −0.26 / −0.57`，
均值 −0.71 → −0.09 —— **「今天异常偏凉」有一半是自己造出来的。**

- `l_integrate.py` 新增 `--grid {multimodel,best_match}`，**默认 `multimodel`**
- 硬规：**任何改动 `MODELS` 列表，必须同步重算 `model_calib.json`**
- 硬规：**L̂ 绝对值持续 >1°C 且多时点同号 → 先怀疑基线不同源**

### 新增 `scripts/l_cond_check.py`

把 `running_max(h)` 当**条件变量**：「历史上 9 月到 h 时已经升到这个水平的日子，最终日最高是多少」。
同时诊断 ERA5 振幅压缩：**站点坐标下 ERA5 把 9 月日最高平滑掉 +2.2 ~ +2.4°C**
（ERA5 均值 29.23 vs HKO 站 31.64）。
→ R 表只能当「形状」用；**别拿「气候平均 bias = +2.4」去修当天的网格值**（会推出 32°C+ 的荒谬峰值）。
**气候平均偏差 ≠ 当日偏移量。**

### 新增：市场拟合 RMS 体检指标

反解市场隐含 L 时**必须同时报 RMS**：`<8%` 可信 · `8–10%` 仅方向 ·
**`>10%` 则市场形状本身不可信，不能当先验**。
2026-09-15 11:23 实测 **13.2%**（前一时点 8.4%），市场分布**双峰**
（29° 25.6% / 30° 28.9% / 31° 31.8%），而任何气温分布都该是单峰
→ **薄流动性被两组观点撕开的价格，不是一条信息。**

### 结论（11:23 盘口，基准 L*=+0.46）

市值 **$495.0（19.8% 本金）**｜成本 $528.3｜模型公允市值 $580.1｜期望回收 **+17.2%**
（市场自身口径 +3.9% → 组合层面 +13.3pt）。

- **🔴 卖出 YES32 全部 282 份 @≥0.095**（公允 2.0–5.3% vs 卖价 0.100 = 公允的 2–5 倍，回收 +$26.8）
- **NO31 持有不加**：448 份成本 $331.5 = 组合成本的 **62.8%**，35% Kelly 只给 $195 → **已超配 1.7 倍**。若一定要加：`NO31 @≤0.680，$50 / 74 份`
- **28° YES 不做**：名义 EV +100% @0.036，但要赢需日最高 <29.0，而站点 11:10 已 28.8
- **🔴 市场反向证据：HKO 官方 9 天预报今日 max = 32°C**，市场很可能在锚定它；
  而实测距 32 需再涨 3.2°C。**结算后必复盘「预报 vs 实况」谁对。**

评审：`reviews/2026-09-15_prediction.md` 新增 §13。

---

## v0.16.16 — 2026-09-15 · **L 必须做贝叶斯收缩**（推翻了本工具的核心口径）+ `l_integrate.py` 上手（MINOR，改数字）

**触发**：Eason 11:00 「更新预测」。头寸增至**五腿** ——
`NO29 172 / YES29 185（新）/ YES30 17 / NO31 338（267→338）/ YES32 282`。

### 1. 🚨 修正：当日的 L 不能直接用，必须向气候偏差收缩

`watch()` 一直用「今日已采样时段的整点对齐中位偏差」当 L。v0.16.10 立这条规时的理由是
「封顶在气候值会在干燥晴热天硬砍 1.4°C」—— 但那是针对 `min(cur_bias, 气候偏差)` 这种
**硬截断**，不是贝叶斯收缩。**当年把两件事混为一谈，修得过正。**

正确做法是层级模型（三个参数今天全部可测，不需要新数据）：

```
L ~ N(μ_prior, τ²)，  L̂ ~ N(L, σ²)   →   L* = (μ_prior/τ² + L̂/σ²) / (1/τ² + 1/σ²)

μ_prior = +1.012  ← model_calib.json 的 bias（80 天，站点日最高 − 7 模式平均日最高）
τ       =  0.763  ← model_calib.json 的 resid_sd（该偏差的逐日标准差）
L̂ / σ            ← 当日整点对齐偏差序列（+0.23 / −0.95 / −1.40）
```

| σ | L* | 收缩前 → 收缩后 |
|---:|---:|---|
| 0.48（均值的 SE） | **−0.216** | 峰值 29.45 → 30.18 |
| 0.60（中位的 SE） | **−0.197** | NO31 公允 95.7% → **86.5%** |
| 0.84（原始 sd） | **+0.119** | NO31 EV@0.760 **+25.9% → +10.4%** |

**τ（0.763）与 σ（0.48–0.84）同量级 → 数据压不过先验，收缩是强制性的。**
不收缩会把 edge 夸大 **2.5 倍**。五腿账本中 `NO31` 占市值 56.6%，
整条赌注都压在这个参数上，这个偏差直接决定仓位。

**落规（SKILL.md 新增专节）**

1. 任何用当日 L 出的结论，**必须同时给「收缩前 / 收缩后」两版**，并注明 τ 与 σ。
2. **σ 不得小于「逐小时摆动 / √n」** —— 今日逐时偏差一小时摆动 1.18°C。
3. **何时放弃先验**：`L̂` 落在 `N(μ_prior, τ²)` 的 5% 尾区 **且** 样本数 ≥3 且同号
   → 当日先验失效，此时下调 `μ_prior` 本身，而不是只把它当权重。
4. **未解**：`bias` 量的是「日最高」偏差，`L̂` 量的是「逐时」偏差，两者均值不一定相等
   （今天三个逐时样本在 `N(+1.012, 0.763²)` 下是 0.5% 分位事件）。
   在 `obs_1min_archive.json` 攒够逐时站点序列、建出「逐时偏差表」之前，**τ 只是工程近似**。

### 2. `scripts/l_integrate.py`（新增）：把「L 积分」从手算固化成脚本

v0.16.15 的「L ~ N(中心, 0.6) 积分」是靠手算做的，每轮重复且易错。新脚本一次做完四件事：

- 对 `L ~ N(L_hat, sd_L)` 网格积分（每点：经验 one-touch〔δ 当随机量〕与正态截断各半混合）；
- **反解市场隐含 L**（`--market`）——「我方与市场的分歧值多少摄氏度」的单一量化口径；
- **组合结算回收矩阵**（`--hold`）——识别「多腿同档归零」缺口；
- **按真实可成交价排序 EV**（`--book`：买 YES 用 YES ask、买 NO 用 `1 − YES bid`）。

**两个顺手修掉的口径 bug：**

- **分布中心不能用 `Σ k·P_k`**：档内质量紧贴现测值时集中在下缘，
  `Σk·P_k` 系统性低估 0.4–0.5°C。实测 `29.59`（错）vs `30.06`（尾和积分），
  而模型点估计是 `30.04` —— 改用尾和积分 `E = floor + Σ ∫ P(max≥x) dx`。
- `--watch` 输出的「浮盈」列因 f-string 里混了中文引号而输出乱码数字 —— 已修。

### 3. 坐实：`rhrread` 的整数是**向下取整**，误差 0 ~ −0.9°C

11:00 时 `rhrread` 报 **28**，同一时刻 0.1°C 源报 **28.5**。
整数源的误差正好是**档位归属**的量级。**落规**：

- 结算站温度只认 `latest_1min_temperature.csv`（0.1°C / 10 分钟）；
  `rhrread` 只当「有没有数据」的探针与兜底。
- **必须审计兜底路径**：`fetch_hko_1min` 失败时 `hko_t` 会退回整数，
  此时**所有用到 `hko_t` 的量（L、峰值、one-touch）全部系统性偏低 0.5°C**，
  回退发生时必须显式告警。
- 该 CSV **发布滞后 10–25 分钟**（11:00 的值 11:08 才见到）——
  整点后 10 分钟内拿不到新值是正常的，**不要误判为数据源故障**。

### 4. 本轮结论摘要（详见 `reviews/2026-09-15_prediction.md` §12）

- 网格峰值 12h = 30.4 后午后塌陷（13h 28.6）；站点 11:00 = **28.5**，三个整点偏差
  `+0.23 → −0.95 → −1.40` **单调扩大**，「站点跑在网格之下」是今日主导特征。
- 市场在 11:00 整体上移（30 档 30.5%→35.2%、28 档 12.1%→7.1%），隐含 L ≈ +0.15~+0.39。
- **收缩后**：模型中心 **30.11** vs 市场 30.49（分歧从 0.87°C 缩到 0.38°C，方向不变）。
- 组合期望回收 **$481.3** vs 清算价 $435.6（**+10.5%**）；市场自身口径 $453.4（+4.1%）。
- 建议：五腿持有；**NO31 @≤0.760 $60/79 份 + YES29 @≤0.310 $75/242 份**（合计 $135，敞口 22.8%）；
  `28° YES` 名义 EV 最高但被 σ_flicker 守卫点名偏乐观 —— 不做。

---

## v0.16.15 — 2026-09-15 · L 的整点对齐修正**落到代码** + 市场内部一致性信号的样本外验证（MINOR，改数字）

**触发**：Eason 10:26 「更新预测」。头寸增至**四腿** —— `NO29 172 / YES30 17 / NO31 267 / YES32 282`。

### 1. 修正：`--watch` 的水位 L 被整点错位采样系统性抬高 0.9°C

此前的规则只说「L 是时钟敏感的」（观察到 +1.60 ↔ +0.30 抖动），本次查清了机理：
**网格值是整点瞬时值，而采样点是任意 10 分钟边界**（08:50 / 09:20 / 09:50 / 10:10）。
早间升温速率 ≈ +1.5°C/h，于是 `采样点 − 整点网格` 天生正偏：

| 小时 | 网格 | 站点样本(时刻) | 直接偏差 | 整点对齐偏差 |
|---|---:|---:|---:|---:|
| 08h | 26.0 | 27.6 (08:50) | +1.60 | **≈ 0** |
| 09h | 27.4 | 27.7/28.0 | +0.30/+0.60 | **≈ +0.2** |
| **10h** | **28.9** | **27.9 (10:10)** | **−1.00** | **≈ −0.9**（官方 10:00 = 28.0） |

**中位数 +0.60 → 整点对齐后 ≈ −0.30**，点估计被抬高 **0.9°C > 残差 sd 0.76**。
→ 今日峰值估计从 **31.00** 修正为 **30.1（对齐）/ 29.5（10h 实测差）**，众数档从 31 档移到 **30 档**。

**代码改动（`scripts/hk_edge.py` 的 `watch()`）**

- 新增**站点序列线性插值到整点**再减网格的口径：`samp` 按分钟排序，对每个整点小时
  在相邻采样点之间插值得到 `hko@h:00`，再算偏差；整点落在采样区间外的小时直接跳过。
- `robust_bias` 三级回退：**对齐样本 ≥3 → 取中位；1–2 个 → 取均值；0 个 → 退回错位中位**。
- 水位改用 `L = robust_bias`（此前是 `L = robust_bias if len(devs)>=3 else cur_bias`，
  会在样本不足时悄悄退回**单点**偏差）。
- 当 `|错位中位 − 对齐中位| > 0.2` 时打印提示行，把两个口径都暴露给使用者。
- 实测效果（10:20）：`ⓘ 整点对齐修正：错位中位偏差 +0.30 → 整点对齐 −0.36°C（n=2）`；
  峰值估计 31.00 → **30.04**，众数档 31 → **30**。

**保留的口径风险**：整点对齐样本今日只有 2 个（09h +0.23、10h −0.95），
小时之间摆动 1.18°C —— 说明「偏差」本身不稳定，L 的 1σ ≈ 0.6，与残差 sd 同量级。
故概率输出应按 **L ~ N(−0.36, 0.6)** 积分，而不是单点代入（见下）。

### 2. 验证：「市场内部一致性检验」首个样本外命中，且是双向

v0.16.14 立的方法在 26 分钟后被市场自己证实：

| 档 | 10:00 mid | 10:26 mid | 我们反推值 | 结果 |
|---:|---:|---:|---:|---|
| 30 | 0.270 | **0.330** | **0.313** | 向上收敛 → 命中 |
| 31 | 0.345 | **0.255** | **0.244** | 向下收敛 → 命中 |

**但代价是这个 edge 消失了 —— 信号存活期 ≲ 30 分钟。** 同日三例（09:15 NO31、10:00 两档）
全部在下一轮刷新前被抹平。已立纪律：**本信号必须当场执行，隔一轮即作废。**

### 3. 对 9/15 的直接影响：edge 归零，四腿全持有

六情景混合公允（A 经验 one-touch δ封顶 / B 不封顶 / C 网格+L=+0.60 / **D 网格+L=−0.30** /
E 网格+L=−0.90 / F 市场隐含，权重 .25/.10/.10/**.25**/.10/.20）给
**28:10.7% 29:29.3% 30:33.8% 31:18.8% 32:5.0%**，逐档与市场的差额**全部 ≤ 4.8pp**（低于一个点差的两倍）。
**六情景对 31 档的估计跨度 3.9%–38.0%（34pp），比我们与市场的差额大 7 倍 → 「谁更准」不可判定。**

- 四腿全部**持有**（NO29 公允 0.707 ≈ bid 0.710；NO31 0.812 > bid 0.730；YES30 0.338 > 0.320；
  YES32 bid 高于公允但只值 $3.3）。
- 期望回收 **$362.2（+6.7%，公允口径）/ $359.8（+6.0%，市场口径）** —— 两口径重合 ＝ 无 edge。
- **无一条候选腿在六情景下全为正**（v0.16.14 的「全正」判据首次导致「无稳健腿」的空结论）。
  次优为 **NO31 @≤0.760，5/6 情景为正、混合 +6.8%**，但 C 情景 −18.4% —— 正 EV 完全建立
  在「L 被高估」这一个判断上，故仅作「若必须出手」的 idea，$100 上限。

---

## v0.16.14 — 2026-09-15 · 市场内部一致性检验 + 「五情景全正」判据 + L 时钟对齐（MINOR，方法论，无代码改动）

**触发**：Eason 10:00 「更新预测」。头寸不变（NO29 172 / NO31 267 / YES32 282），
但市场在 30 分钟内向左重定价 —— 32 档 mid 0.145 → 0.075（**−48%**），隐含中心 30.79 → 30.56。

### 1. 新增 SKILL.md §「市场内部一致性检验」

**旧问题**：所有 edge 论证都建立在「我方中心比市场准」上，而这个前提当天无法验证。

**新方法**：先用市场自己的逐档 mid 反推其隐含中心 μ_m 与隐含 sd σ_m，再用**市场自己的参数**
反推「这一档本该报多少」，与其实际报价比。差额即 edge，**不依赖任何模型优势**。

实测（2026-09-15 10:00）：

| 项 | 值 |
|---|---|
| 市场隐含中心 μ_m | 30.56°C |
| 市场隐含 sd σ_m | 1.24°C |
| 用 μ_m/σ_m 反推 P(30 档) | **31.3%** |
| 市场实际报价 P(30 档) | **26.4%** |
| 缺口 | **−4.9pp** |

→ 任何以 30.56 为中心的平滑分布都给 30 档 31–42%，市场只报 26.4%。
**30 档是市场自己定价体系里的便宜货。** 据此给出本轮唯一出手腿：YES30 @≤0.290，298 份 / $86.4。
反向也成立：31 档市场报价 33.7% > 反推 24.4% → NO31 holdings 应持有、绝不减（实测公允 0.731 vs bid 0.650）。

### 2. 新增「五情景全正」判据

盘中不再只给一个公允分布：列 4–5 个情景（**必须含一个明确的悲观情景**），加权后**逐情景单独算 EV**。

- **某腿在全部情景下 EV > 0 → 才叫稳健腿，可以出手。**
- 只在部分情景为正 → EV 只是情景权重的函数，不是 edge，**不出手**。

实测：YES30 五情景 +10.5% ~ +51.1%（全正）；NO31 在 S2/S3 为负（−2.4% / −5.1%）。

### 3. 新增 `--watch` 的 L 时钟对齐规则（**尚未落到代码**，已列入待办）

L = 「最新站点读数 − 网格同小时值」。当读数是 09:50 而网格取 09h 值时，错位 50 分钟把 L 虚高：
实测 **+0.60（错位） vs +0.25（整点对齐）**，峰值估计跨度 0.35°C，与残差 sd 0.76 同量级。
手动复核必须用「同一整点的站点值减网格值」。

### 4. 本轮结论

- **中心不再是分歧点**：五情景混合中心 30.61 vs 市场 30.56（差 0.05°C）。分歧全在**形状**上。
- 三腿全部**持有**（NO29 公允 0.785 vs bid 0.780；NO31 0.731 vs 0.650；YES32 0.070 vs 0.070）。
- 组合期望回收 $349.9 vs 现市值 $327.4 → **+6.9%**；31 档缺口仍是 −47.5%（P=26.9%）。
- 新增 §9 到 `reviews/2026-09-15_prediction.md`（9 小节）。

---

## v0.16.13 — 2026-09-15 · 推翻 v0.16.11 的「NO31 无条件正 EV」（MINOR，方法论纠错 + 盘中重定价）

**触发**：Eason 09:07 报头寸 `NO31 = 267 份`，要求更新 9/15 预测。

### 1. 核心纠错：**无条件气候频率不能给「某一天」定价**

v0.16.11 曾据「七条历史口径全部正 EV」力荐 **NO31 @≤0.700（$500 / 714 份）**。今日实况落地后重算：

| 口径 | P(31档) | NO31 公允 | 买 0.630 的 EV |
|---|---:|---:|---:|
| 脚本 `--date`（含实况，sd 0.76） | 41.8% | 0.582 | −7.6% |
| 中性（mu 31.20, sd 0.95） | 38.4% | 0.616 | −2.2% |
| **四口径加权混合** | **36.1%** | **0.639** | **+1.4%** |
| 市场隐含（mu 30.89, sd 1.24） | 28.1% | 0.719 | +14.2% |

v0.16.11 的七条口径 P(31档)=5.9%–21.4%，**全部是跨日无条件气候频率**；
当日条件分布给 36–42%。七条口径高度相关，**不是七次独立验证，是同一基准率的七个切片**。
实际结果：NO31 从 0.700 跌到 0.630，**入场即 −10.6%**。

**新增 SKILL.md 红线节**「🚨 无条件气候频率不能给某一天定价」，并规定气候频率只允许用于
① 逐档差额扫描找候选错配档、② 极端情景敏感性下界；**不得**直接当当日公允概率算 EV。

### 2. 新增工具性结论：**保险腿的赢面对中心呈 U 形**

持有 NO31 的赢面（sd 0.95，截断 27.7）：mu 30.0→82.4%、31.3→60.4%（**谷底**）、32.5→71.2%。

→ **当中心估计恰好等于该档中心时，保险腿处于最不利位置，买入必为负 EV。**
开保险仓前必须先画这条 U 形曲线，确认当前中心不在谷底。

### 3. 9/15 盘中重定价（09:15 HKT）

- 实况：天文台站 27.6°C @08:50，已观测最高 27.7；累积雨量仅 Tai Po 2mm / Yuen Long 2mm，其余 16 区 0。
- 网格日变化：09h 27.4 → 11h 29.9 → **12h 30.4（峰）** → 13h 28.6（云量 83%、降水概率 64%）。
  **峰值窗口 11:30–12:30，13:00 后被对流打断。**
- 四口径中心：脚本 31.04 / `--watch` 外推 31.97 / 官方 fnd 31.97 / 市场隐含 30.76 → **混合 31.30**。
- **今日被错配的档**：32 档（公允 19.9% vs 市场 11.6%，差 −8.3pp → YES32 @≤0.135 EV **+52.7%**，
  但可成交量仅 297 份 / $38.6）；29 档（公允 8.2% vs 市场 17.1% → NO29 @≤0.835 EV +10.6%）。
- 31 档与 30 档无 edge（差额 −0.9pp / −2.3pp）。

### 4. 结构性发现：脚本残差 sd 0.76 vs **市场隐含 sd 1.24**

由市场 mid 分布反推 `Σp(x−μ)² = 1.530 → sd 1.237`。今日 30/31/32 三档的全部分歧都收敛到这**一个参数**。
**待办**：若市场 sd 系统性大于回测残差 sd，说明残差 sd 漏掉了「跨模式分歧」，
应把集合离散度 ±0.39 显式卷积进去（已列入 `reviews/2026-09-15_prediction.md` §7.9 待回填）。

**无代码改动**；新增 `reviews/2026-09-15_prediction.md` §7（盘中更新，9 小节）。

---

## v0.16.12 — 2026-09-14 · 9/14 结算回填 28°C（provisional）+ 9/13 备注解除 provisional（PATCH）

**来源**：每日收盘结算自动化（21:31 运行）。**代码零改动，只回填台账数字与状态**。

### 1. 2026-09-14 结算档 = **28°C**（`provisional`）

- 判定依据三条互校：① 结算站 1 分钟序列 `scripts/data/obs_1min_log.json` 当日最高 **28.6**（14:10）
  → `floor` 落 28 档 [28.0, 29.0)；② 收盘后市场 **28 档 YES bid/ask = 0.998/0.999**（≥0.95 视为已定档），
  其余档 NO 0.999；③ 官方 `dailyExtract_202609.xml` 21:35 复核**仍只到 09-13**，09-14 未出 → 只能标 `provisional`。
- 三条腿全部为赢：`NO30 +$81.48` / `NO31 +$26.90` / `YES28 +$115.99` → 当日 **+$224.37（+80.1%）**，
  是台账 5 个结算日里的最高单日。

### 2. 台账数字变化（这是本次唯一改变输出数字的地方）

| 口径 | 改前 | 改后 |
|---|---|---|
| 累计净盈亏 | $65.34 | **$289.71** |
| 累计投入 | $937.60 | $1,217.60 |
| 累计 ROI | +7.0% | **+23.8%** |
| 逐腿胜-负 | 5-9 | **8-9** |
| 结算日 | 4（3 盈 1 亏） | 5（**4 盈 1 亏**） |

### 3. 顺带更正

- 9/13 那条腿的备注原写「官方 Daily Extract 未出故 provisional」，现已由官方源证明
  `09-11=32.6→32、09-12=33.1→33、09-13=31.7→31`，三日的 `settle_status` 均为 `confirmed`
  → 备注文字更新，**不影响任何金额**。
- `rec_pnl.py --settle --write` 会把 `settle_status` 固定写成 `confirmed`，故仍沿用既有流程：
  先跑 `--settle` 回填，再手工改回 `provisional`（台账文件头已写明该坑）。

**输出数字影响**：`PNL.md` 与 `rec_pnl.py` 汇总数字全部变化（见上表）；`hk_edge.py` 的预测/EV 输出**不变**。

---

## v0.16.11 — 2026-09-14 · 先验基线的条件化：当日雨量 > 前日雨量；市场右尾定价错配（MINOR）

**来源**：9/15 预测需求（本金 2500，D-1）。用户要求「即使不建议下单，也要摆出有 edge 的下注 idea」，
逼出了两个此前没有的量化工具。详见 `reviews/2026-09-15_prediction.md`。

### 1. 新增 `scripts/postrain_prior.py`

拉 HKO Daily Extract 月 XML（2015–2026；分析窗口取每年 **8/25–10/5**，n=477），
输出「前日雨量 × 当日雨量」二维条件下的站点日最高档位频率。首次运行即推翻一条长期隐含假设：

- 「前日≥20mm」整体均值 30.28°C（比全样本 31.61 低 1.33°C），但**拆开当日雨量后**，
  「前日≥20 & 当日<2mm」均值回到 **31.59°C**（n=22）、「前日≥35 & 当日<2mm」31.32°C（n=15）。
- → **前日大雨几乎不压制次日峰值；真正的开关是当日雨量。**
- 踩坑记录：XML 雨量列会出现 `'Trace'`，末行是 `'Mean/Total'`，两者都要先过滤再 `float()`。

### 2. 新增 SKILL.md §「先验基线的条件化」，并固化「三情景加权逐档差额法」

2026-09-14 实测（三情景加权 = 0.55×当日<5mm + 0.35×5–20mm + 0.10×≥20mm）：

| 档 | 气候 n=477 | 三情景加权 | 市场 mid | 差 | 判读 |
|---:|---:|---:|---:|---:|---|
| 29 | 8.8% | 9.9% | 6.5% | −3.4pp | 略低估 |
| **30** | 14.0% | **16.3%** | **16.5%** | **+0.2pp** | **定价精准，无 edge** |
| 31 | 19.3% | 19.9% | 31.0% | **+11.1pp** | 高估 |
| 32 | 22.4% | 22.3% | 30.5% | **+8.2pp** | 高估 |
| 33 | 16.6% | 13.6% | 11.5% | −2.1pp | 略低估 |
| 34 | 7.1% | 6.3% | 0.7% | −5.6pp | 严重低估（流动性幻觉，不碰） |

**市场把 33°C 及以上的概率整体搬到 31/32，但 30 档定价精准。**
30 档的三情景跳变（11.6% → 23.3% → 17.5%）经加权后 16.3% ≈ 市场 16.5%
→ **市场把雨量不确定性定价进去了**；单看「前日≥20 & 当日中雨」那一格（n=24 给 30 档 37.5%）
会误判出 +121% 的假 edge。差额 > 10pp 才是候选腿。

### 3. 对 9/15 的直接影响

**NO 31°C @≤0.700 在三情景下全部正 EV（+11.4% / +14.3% / +32.1%）**，
是本次唯一「无条件正 EV」的腿；盈亏平衡点 P(31档) < 30%，而市场定价 31.0% 正压在线。
脚本模型（mu 30.90）给出的四腿合计 $1753 = 70% 本金，**已判定不可执行**（超 25–30% 上限 + D-1 的 a* = −0.19）。

---

## v0.16.10 — 2026-09-14 · 盘后「实质锁定」判定 + 成交口径台账（PATCH，文档与台账，不改数字）

**来源**：9/14 平仓复盘（`reviews/2026-09-14_prediction.md` 附录 Q）。用户在 16:00 后
手动平掉 28 YES 225 份 @≈0.851，而当时的证据已足够判定 **28 档实质锁定**。

### 1. 新增 §A3d「盘后实质锁定判定」

```
R_max(h) = max{ R : remaining_rise_cdf[month][h] }
need(b)  = b − hko_t                      # 严格口径（§A3c），基准=当前值
need(b) > R_max(h)  →  P(结算 ≥ b) < 1/335 ≈ 0.3%，该档实质锁定
```

- 九月 h=16 的 `R_max = 0.6°C`，且 p97 及以下**全为 0.0**；
- 9/14 16:00 实测：`need(29) = +1.0 > 0.6` → 29 档锁定，P(28 档) = 99.99%（严格）/ 95.0%（宽松）；
- **操作含义**：锁定后**持有到结算优于平仓**（本例差 $33.59，下行仅 $0.03，风险收益比 ~1000:1）；
  要离场应在锁定**之前**离，而不是之后。

**未改任何数字** → PATCH。判定式本身放在 SKILL.md 供人工判读，暂不写进代码。

### 2. 新增 `PNL_actual.md`（实际成交口径台账）

9/14 暴露出**建议台账与实际持仓完全脱节**：

| 口径 | 腿 |
|---|---|
| `rec_pnl_log.csv`（建议） | NO30 $150 @0.648 / NO31 $100 @0.788 / YES28 $30 @0.2055 |
| 用户实际持仓 | 28 YES 225 @0.117 / 29 YES 148 @0.314 / 31 NO 97 @0.880 |

两套**无一条重合**，导致台账算出「9/14 +$224.37」而实际成交是「+$140.27」。
新增 `PNL_actual.md` 手工维护成交口径，**不受 `rec_pnl.py --markdown` 覆盖**。

### 3. 口径对照结论（9/14 实测）

- 成交口径：成本 $158.16 → 回收 $298.43，**净 +$140.27（+88.7%）**，为有记录以来最佳单日；
- **利润 100% 来自 28 YES 一条腿**（+$165.15）；29 YES 与 31 NO 合计 **−$24.88**；
- 29 YES（148 份 @0.314）从建仓起方向就错（29 档合理概率从未超 6%），15:39 平仓 @0.0679
  拿回 $10 是当日最正确的单笔操作。

### 4. 待办

- `rec_pnl_log.csv` 增加 `source`（`rec` / `actual`）列，使两张表可合并统计；
- 用户提供 28 YES / 31 NO 的实际成交均价后回填 `PNL_actual.md`。

---

## v0.16.9 — 2026-09-14 · `--watch` 越线概率的 need 基准改为「当前值」（MINOR，改变输出数字）

**归 MINOR 而非 PATCH**：按本文件定义 PATCH「不改变任何数字」，而本次**改变了 `--watch`
打印的越线概率**（同日 15:30 实例：`到 29.0°C 概率约 5%` → **`0%`**）。

**问题**：`--watch` 用 `need = b − intraday_mx`（基准 = **当日已观测最高**），
而分位表 R 的原生定义是

```
R = 日终 max − 当小时实测值        ← 基准是「此刻的站点温度」
```

用峰值作基准，隐含假设「已经回落的部分会自动涨回去」—— 站点已过峰时**系统性高估越线概率**。

2026-09-14 15:30 实测（站点 27.8、已实现峰值 28.7、目标 29.0）：

| 口径 | need | h=15 网格概率 |
|---|---:|---:|
| **严格（当前值 27.8 起算）** | **+1.2** | **0%**（335 样本最大 R 仅 0.600） |
| 宽松（峰值 28.7 起算） | +0.3 | 1.0%（+δ 卷积宽化后 5%） |

**改动**：
- 主输出改用**严格口径** `need = b − hko_t`（`hko_t` = 当小时实测值）。
- **并列打印宽松口径**作为对照。两口径并存不是冗余，而是**把口径风险显式化**：
  严格口径在「站点确实已回落」时唯一正确；宽松口径在「`hko_t` 陈旧
  （分区 CSV 滞后约 1h）、站点实际更高」时更接近真值，充当敏感性上界。
  同日 14:51 时点两口径曾相差 **23 倍**（0.3% vs 7.0%），单报一个数字等于藏起这个风险。
- 同时修正 SKILL.md 里 `text_readings_e.htm` 的**两个不同 URL**
  （§A3b 用 `textonly/v2/forecast/`、§v0.15.8 用 `wxinfo/ts/`）。实测两者都返回 200
  但**版式不同**；且**站名写作简写 `HK Observatory`，不是 `Hong Kong Observatory`** ——
  写全称的正则会静默匹配失败。已统一到脚本实际使用的 `textonly/v2/forecast/` 并标注该坑。
- 新增 SKILL.md 章节 **A3c**：分位表基准口径铁律 + 两口径对照表 + 判读纪律。

**同日复盘（附录 P，15:39）**：站点 27.8、官方 Max 28.7、已回落 0.9（仍在冷池）；
**全港最热从 30.3（14:40）降到 28.9（15:30）—— 全港同步降温 1.4°C**，UV 5→2、全港零雨。
本轮公允 28/29/30/31 = **97.55 / 2.0 / 0.25 / 0.15 %**，P(≥29.0) = 2.45%（市场 8.5%）。
动作：28YES(225) 持有（净 EV +$39.7）、31NO(97) 持有（+$0.6）、
29YES(148) 平仓（卖 148 份 @0.0679 得 $10.0，临界 P(29档)=6.79%）；不开新仓（已过窗口）。

---

## v0.16.8 — 2026-09-14 · 官方 Max Since Midnight 写进脚本自动采集（PATCH）

**只把 v0.16.7 的人工规则落到代码，核心概率算法零改动。**

v0.16.7 把「当日最高以官方 `text_readings_e.htm` 的 Max Since Midnight 为准」写成了
**人工规则**（SKILL.md A3b），但脚本没实现 —— 于是 `--watch` 仍然打分区 CSV 的 28.6，
而官方是 28.7。规则写在文档、实现没跟上，等于没写。本次补齐。

**改动**：
- 新增 `fetch_official_max()`：解析 `text_readings_e.htm` 里 `HK Observatory` 行
  （形态 `HK Observatory 28.0 83 28.7 / 26.1 -3.3` → 当前 28.0 / Max 28.7 / Min 26.1）。
  **静默失败**：页面结构或网络异常一律返回 `None`，不打断主流程。
  （顺带补上 `import re` —— 此前脚本未导入该模块。）
- `load_observed_max()` 改**三源取大**：官方 Max Since Midnight + 0.1 °C 分区 CSV running max
  + `--watch` 整点日志。三个源都只会**低估**当日最高，取大者覆盖所有漏采方向。
  新增模块级 `_OBS_MAX_SRC` 记录实际采信的源（仅供输出显示）。
- `--watch` 头部新增来源标注：
  `实况源: 官方 Max Since Midnight 28.7°C（14:40）+ 分区气温CSV 0.1°C/10分钟`

**实测**：改前 `--watch` 打「今日已观测最高 **28.6**」，改后打 **28.7** ✓

⚠ 注意：官方页 URL 是 `https://www.hko.gov.hk/textonly/v2/forecast/text_readings_e.htm`
（v0.16.7 文档里写的 `.../wxinfo/ts/text_readings_e.htm` 亦可达，本次统一为脚本实际使用的那个）。

---

## v0.16.7 — 2026-09-14 · 当日最高改用官方 Max Since Midnight（PATCH，文档+口径）

**只改「当日最高取哪个数」的口径，核心概率算法零改动。**

2026-09-14 14:30 实测：官方 `text_readings_e.htm` 给 **Max Since Midnight = 28.7**，
而分区 CSV 的 running max 只有 **28.6**，**差 0.1 °C**。这不是舍入噪声 ——
分区 CSV 只给「最新一笔」，当日最高靠 10 分钟轮询累积（**漏采即低估**）且滞后约 1 小时；
官方页面是**结算同源**（同一 HKO 总部站、同一 0.1 °C 口径）。

在 28.7 距 29.0 只剩 0.3 °C 的那天，这 0.1 °C 直接改变「还差多少才越线」的分子。

**规则（写入 `SKILL.md` 新增章节 A3b）**：
`--observed-max` **取「官方 Max Since Midnight」与「分区 CSV running max」的较大者** ——
官方值通常更准，分区值作为「轮询确实看到过更高点」的兜底，取大者两个错误方向都覆盖。

**取法**：`https://www.hko.gov.hk/wxinfo/ts/text_readings_e.htm`（纯文本页，无鉴权），
结算站行名 `HK Observatory`，取该行第一个数字即 Max Since Midnight。

**当日复盘（附录 N，14:41）**：站点 28.1（14:30）、官方 Max 28.7、已回落 0.6（过峰）；
**雷暴警告已取消**、`icon` 62→**54**（转晴）、官方预报 "Sunny intervals in the afternoon"、
雨集中离岛 10mm / 元朗 12mm（**市区 0**）—— 下行与上行证据并存。
市场 **6 分钟内把 28YES 从 0.406 推到 0.670（+65%）**，隐含中心 28.9 → 28.4，P(≥29) 降至 31%。
本轮公允 **28/29/30/31 = 85 / 13 / 1.8 / 0.2 %**（P(≥29) = 15%，含转晴上行溢价）。
动作：28YES(225) **持有**、31NO(97) **持有**、29YES(148) **平仓**（卖 148 份 @0.1876 得 $27.8，
临界 P(29档) = 18.64%）；**不开新仓**（已过 09:00–11:30 窗口）。

⚠ **本轮最大教训：市场重估是跳变的。** 13:36/14:03/14:35 三轮判「市场严重低估 28 档」方向正确，
但**等市场认同的 6 分钟里，28YES 的 edge 从 EV +57% 塌到 +15%** ——
**实况口径一旦给出明确结论，边界的兑现速度可能远快于预期，犹豫的代价就是 edge 本身。**

---

## v0.16.6 — 2026-09-14 · 冷池作废改为「日内记忆」+ 作废范围扩展到上档（MINOR）

**两个 bug，都是 v0.16.5 当天上线后 27 分钟内被实盘打出来的。** 核心概率算法仍零改动。

### bug 1：冷池判定按「当前落差」→ 站点反弹后静默解除（严重）

v0.16.5 的判据是即时差值 `floor_max − fl_cur > 0.5`。13:36 落差 1.0 → 触发；
到 14:03 站点从 27.9 反弹到 **28.5**，落差缩到 **0.2–0.3** → **作废自动解除**，
表格立刻恢复输出 `28YES ⚠持YES止盈-74%`、`31NO ⚠持NO止盈-19%` —— 回到 v0.16.3 那个错误状态。
**根因**：冷池是「今天已经发生过的事」，不是「现在还差多少度」。用瞬态值判定一个历史事实，必然被反弹抹掉。

**修法 —— 日内记忆（只增不减）**：新增 `scripts/data/cold_pool_state.json` 存当日出现过的**最大**落差，
配 `_load_cold_pool_gap()` / `_save_cold_pool_gap()` 每次运行读-更新-写：

```python
_cold_gap = _load_cold_pool_gap(today) or 0.0
if (floor_max - fl_cur) > _cold_gap:
    _cold_gap = floor_max - fl_cur
_save_cold_pool_gap(today, _cold_gap)
if _cold_gap > 0.5:
    _cold_pool = True
    _cold_relaxed = (floor_max - fl_cur) <= 0.5   # 冷池已退 → 放宽，但不解除
```

跨日自动复位（`date` 字段不匹配即返回 `None`）。新增 `_cold_relaxed` 状态：冷池退去后**作废不解除**，
但标记从 `冷池日` 改为 `冷池退·封顶档`，并启用保守混合（见 bug 2）。

### bug 2：31NO 落在作废范围外，被网格公允 72.5% 判成「持有 / 可加仓」

作废档原为 `{floor(当日最高), +1, +2}` = [28,29,30]，31 档**在外面**。
但冷池对分布的影响是**整条右移的**：网格给 31 档 `27.5% / 72.5%`，而 `--watch` 实况口径给 `P(≥30) ≈ 0`
→ 持 31NO 在网格下表里显示为「持有，公允 72.5%」，实际是**极端有利**（实况近乎锁定），
网格却因为低估 31 概率而在相反方向上给出保守结论。

**修法**：作废覆盖**扩展到所有上档**，持仓检查里改为 `b >= _vb`：

```python
if _cold_pool and _vb is not None and b >= _vb:
    _tag = '冷池日' if b <= _vb + 1 else '冷池退·封顶档'
    ...  ⚠ 网格公允作废（{_tag}）→ 看 --watch
```

### 附带修正

- **σ_flicker 守卫在冷池退时不再被跳过**（`if _cold_reason and not _cold_relaxed:`）。
  v0.16.5 只要冷池成立就跳过守卫，等于**放弃唯一的实况口径**；冷池退时应继续用。
- **随机游走起点必须是当前值**，不是当日最高：`_d_ref = fl_cur if fl_cur is not None else floor_max`。
  9/14 那次误报 `28YES 公允 0.5%` 并打「止盈」，根因就是把起点设成了当日最高。
- **保守混合在冷池退时额外并入 `1 − r0.probs[fl_b0]`**：`fl_pc_use = max(fl_pc, fl_grid_pc, 1 − P(≤floor_max))`。
- **薄盘 `bid = 0` 不判止盈**（`_no_liquidity = mk <= 0.0`）—— 无买盘时「市价高于公允」无意义。

**实测对照（2026-09-14 14:21，已观测最高 28.7 / 当前 28.4，落差 0.3）**

| 腿 | 修前（v0.16.5） | 修后（v0.16.6） |
|---|---|---|
| 28°C YES 225 | `公允 2.7% ⚠持YES止盈-74%` | `n/a ⚠ 网格公允作废（冷池日）→ 看 --watch` |
| 29°C YES 148 | `公允 23.3% ⚠持YES止盈-19%` | `n/a ⚠ 网格公允作废（冷池日）→ 看 --watch` |
| 31°C NO 97 | `公允 72.5% 持有/可加仓` | `n/a ⚠ 网格公允作废（冷池退·封顶档）→ 看 --watch` |

**验证方式**：手工注入 `cold_pool_state.json` 两组状态（`gap=1.2` 非 relaxed → 三条腿全作废；
`gap=0.2` relaxed → 输出正常且走保守混合），逐行比对；跨日复位用 `date` 字段不匹配测试。

**当日复盘（附录 M，14:03–14:21）**：站点 14:00 **28.4–28.5**、当日最高 **28.7**、当日最低 26.1；
全港最热深水埗 30.2 / 横澜岛 30.0 / 赤鱲角 29.9；油尖旺雨量 0（午后转晴）；
`icon 62`（雨）、UV 5；雷暴警告 13:25 延长至 **14:30**。
盘口 28 YES `0.077/0.110`（mid 0.093）、29 `0.400/0.428`、30 `0.410/0.440`、31 `0.100/0.120`、32 `0.007/0.017`，
隐含中心 **≈29.9**；扫单均价 28 YES 0.129 / 29 YES 0.439 / 30 YES 0.508 / 31 YES 0.277 / 32 YES 0.078。
本轮实况口径公允 **28/29/30/31/32 = 62/27/9.5/2/0.4（中心 29.0）**。
三条腿 **0 份动作**；29YES 可挂 74 份 / $24.4 @0.33+ 限价减仓（非必须）。
期望派息：本轮公允 $274.9 > p\\* $242.4 > 市场隐含 $166.0 > 全平 $158.2。

---

## v0.16.5 — 2026-09-14 · 冷池日作废标记提到主表那一行 + ⚠止盈全表屏蔽落到代码（MINOR）

**改了主表的渲染口径：作废档不再输出动作 / EV / Kelly / 止盈。** 核心概率算法零改动。
触发源：v0.16.4 立了「冷池日网格公允表作废、⚠止盈全表屏蔽」的规则，但**只实现在持仓检查那一段**，
主表照旧输出 `28°C 2.7% 17.6% 买 NO ▼ +18% $296 / 359份 ⚠持YES止盈-84%` ——
读者第一眼看到的仍是那条被污染的结论，规则形同虚设。三处：

1. **冷池判定提前到主表输出之前**（原来在 `if a.hold` 分支内），因此：
   - **不给 `--hold` 也能看到作废提示**（此前只有喂了持仓才会打印）；
   - 主表里作废档 `{floor(当日最高), +1, +2}` 的动作列直接改标 **`⚠作废(冷池)`**，
     EV / Kelly 两列留空 —— 不再生成「买 NO $296 / 359 份」这种基于作废公允的仓位建议。
2. **`⚠ 止盈` 标签全表屏蔽**（`if not _cold_pool` 包住 warns 生成）。
   v0.16.4 的文档写的是「全表生效」，代码只覆盖了持仓检查 → 本次补齐。
   实测同一命令：修前 31 档打 `⚠持NO止盈-16%`、33 档打 `⚠持YES止盈-54%`，修后两者均消失。
3. **主表后新增冷池说明块**（不依赖 `--hold`）：列出作废档位，并明说
   **「整条网格分布都被高估（含 31 档及以上）→ 上表动作 / EV / Kelly 本次运行整体不可作为下单依据」**
   —— 作废只划到 +2 档，但偏差是全表的，必须一起声明。

**实测对照（2026-09-14 13:40，已观测最高 28.7 / 当前 27.9）**

| 档位 | 修前 | 修后 |
|---|---|---|
| 28°C | `买 NO ▼ +18% $296/359份 ⚠持YES止盈-84%` | `⚠作废(冷池)` |
| 29°C | `买 NO ▼ +16% $108/165份 ⚠持YES止盈-31%` | `⚠作废(冷池)` |
| 30°C | `买 YES ▲ +14% $29/79份` | `⚠作废(冷池)` |
| 31°C | `买 YES ▲ +117% $56/464份 ⚠持NO止盈-16%` | `买 YES ▲ +117% $56/464份`（⚠ 已屏蔽） |

**验证方式**：同一命令跑修前 / 修后，逐行比对 `公允P` 与 `市场价` 两列 —— **逐位一致**
（概率计算未动）；差异仅在动作 / EV / Kelly / warns 四列的渲染。

**当日复盘（附录 L，13:36–13:41）**：站点 13:20 已观测最高 **28.7**、当前 **27.7–27.9**（落差 1.0），
主表作废档 [28,29,30]。市场两分钟内把 28 档 YES 从 `0.150/0.176` 砸到 `0.117/0.156`（−22%），
隐含中心 29.90 —— 与「已实测最高 28.7、距 29.0 仅 0.3、正午 UV 4、雷暴警告延至 14:30」脱节。
扫单均价与中价差距依旧致命：30 档 mid 0.355 vs 扫单 $60 均价 **0.466**、31 档 mid 0.105 vs **0.263**。
本轮公允 **28/29/30/31/32 = 62/27/9.5/2/0.4（中心 29.0）**；三条腿 **全部持有、0 份动作**。

---

## v0.16.4 — 2026-09-14 · 冷池日作废规则落到代码 + 撤回「落差 >1.5°C」阈值（MINOR）

**改了代码，也改了规则阈值。** 三处：

1. **阈值撤换（上一版写错）**：v0.16.3 用 `当日最高 − 当前 > 1.5°C` 判「深冷池日」。
   2026-09-14 13:08 实测：站点已从 26.2 反弹到 27.9，落差只剩 **0.8°C** → 规则不触发，
   同一条作废的公允表（28 档 **2.7%**、29 档 23.3%）又被照样读出来，并打出
   `28°C YES ⚠止盈（现价高出公允 73%）`、`29°C YES ⚠止盈（−21%）` —— **两次报错都指向同一条腿**。
   新判据：**当天出现过 ≥0.5°C 的回落即算冷池日**；判的是「冷池已经发生过」，不是「落差还多大」。
2. **作废规则落到代码**（v0.16.3 只在文档里说了，代码没落实）：
   - 守卫跳过的分支现在算出作废档位 `{floor(当日最高), +1, +2}` 并打印「这几档不要读」；
   - 持仓检查里落在作废档位的腿不再输出公允值与 EV，改为 `⚠ 网格公允作废（冷池日）→ 看 --watch`；
   - **冷池日全表屏蔽 `⚠止盈` 标签** —— 网格分布整条被高估，任何「市价高于公允」的结论都不可信。
   （修前 28/29 两腿被误判为止盈；修后显示 `n/a`，31 NO 显示「⚠ 止盈提示已屏蔽」。）
3. **新增方法学章节《下午的「还会不会再涨」不能查 ERA5 分位表》**：该表格点是海面格、峰在 12–13 时，
   `by_month['9']['13']` 的 `P(R ≤ 0.3) = 85%`、`mean R = 0.127` 是**形状错误**，4.7 倍尾部放大补不回来。
   可用的替代法：**只取比值**——ERA5 9 月 h=13，「13 时低于 running max ≥0.5°C」的子集
   `P(R > 0.3)` 从无条件 20.3% 降到 **5.9%（比值 0.29，n=51）**，这个比值可迁移、绝对值不可迁移。
   并要求同时读三条「天空条件」：`rhrread.icon`（63=雨）、`uvindex`（正午 UV 仅 4 → 云量大）、
   `flw.forecastDesc`（"Mainly cloudy with a few showers"）。

**当日复盘（附录 K，13:15）**：市场一小时内把整条曲线搬向暖侧（28 档 0.218→0.103、30 档 0.332→0.460，
隐含中心 29.90→30.14），与已实现状态（最高 28.7 定格、当前 27.9、正午 UV 4、官方报多云有骤雨）脱节。
本轮公允 **28/29/30/31/32 = 30/33/24/9/3**（中心 29.75）；三条腿（28YES 225 / 29YES 148 / 31NO 97）
**全部持有、0 份动作**。p* 期望派息 $188.4 > 市场隐含 $150.7 > 立即平仓 $128.2。

---

## v0.16.3 — 2026-09-14 · 深冷池日主公允表作废规则 + 气候基线按旬段/雨量档细化（PATCH）

**不改变任何输出数字**（文档 + 纪律）。三处：

1. **新增硬规则：`当日已观测最高 − 当前站点温度 > 1.5°C` 时，主公允表最近 2–3 档一律作废，
   改读 `--watch` 的实况外推；该情形下 `⚠ 止盈` 提示不得采信。**
   触发源：2026-09-14 12:08 站点已实测最高 28.7 / 当前 26.2 / 本区原地 35mm 强对流，
   主公允仍给点估 30.53°C、28 档 2.7% 并打「28YES 止盈」，而同一次运行的 `--watch` 给峰值 28.60°C。
   **两个引擎方向相反且无任何提示** —— σ_flicker 守卫跳过之后读的仍是那条不含实况的公允表。
2. **气候基线章节补「旬段 × 雨量档」两层查表**：9/11–9/20 的 P(最高<29.0) 只有 **5.5%**
   （全月是 10.8%），9/11–9/18 分层后 全部 5.7% / ≥20mm 25.0% / ≥40mm 40.0%。
   硬约束：**9/11–9/18 的 88 天里日最高从未低于 28.2°C**（P(<28.0) = 0/88）。
3. **修正上一版的一处事实错误**：v0.16.0 笔记写「2026-09-14 油尖旺午夜起仅 1mm → 外流冷池、瞬变」，
   但 12:02 同一分区已累积 **35mm**（黄大仙 55mm）。已改写并加纪律：
   **分区雨量是累计量，雷暴警告生效期间必须每 10–15 分钟重取，不能拿单次读数当天气型结论。**

---

## v0.16.2 — 2026-09-14 · 头寸建议强制双单位（PATCH）

**不改变任何输出数字**（纯文档）。`SKILL.md` 新增硬规则：**所有头寸建议（开/加/减/平/对冲/止盈）
一律写成 `$金额 / N 份 @价`，禁止只给金额，也禁止用「减半」「加一点」这类相对量词代替份数。**
原有规范只覆盖了 Kelly 开仓三件套，未覆盖平仓/减仓/对冲/头寸点评，实测确实漏过
（当天写过「对冲约 $22.8」「合计 ≤$280」，都没带份数）。

---

## v0.16.1 — 2026-09-14 · SKILL.md 补「剩余升幅分位表的两个口径陷阱」（PATCH）
**不改变任何输出数字**（纯文档）。在 `SKILL.md` 的 (4) 节新增两条硬约束：

1. **分位表减的是 `running_max`，不是当前值。** `remaining_rise_cdf.json` 定义为
   `R(h) = M − running_max(h)`。冷池场景下当前值与 running max 能差 1.8°C（2026-09-14 11:30
   站点 26.9 / running max 28.7），用错基准会把「回到风暴前水平」误判成罕见事件。
2. **Open-Meteo 的 HK 坐标在 archive 里是海面格点。** 2015–2025 年九月日均最高均值仅
   **25.09°C（sd 4.60）**，比天文台站气候值低 **6.5°C**，样本含「九月日最高 6°C」的荒谬值。
   绝对水平一律丢弃，只用差分/形状；实测 `P(R≥0.3)=80.2% / ≥1.3=27.8% / ≥2.3=8.3%`。

同时把 (3) 节「凹陷类比法」的权重建议改为**不建议单独承担 50% 权重**：
2026-09-14 11:05 用它压出 28 档 50%，五条独立口径（气候基线 10.8%、回归 30.6°C、分位表 20%、
市场 18.4%）集体否决，11:45 定稿 20%。

---

## v0.16.0 — 2026-09-14 · σ_flicker 守卫加「适用性检查」+ 新增站点月度气候基线法（MINOR）

**改动数字：仅在一个此前未被标定的区间内改变输出；标定区间内逐位不变。**

### 1. 修 bug：σ_flicker 守卫的随机游走起点用错了

**现象（2026-09-14 11:28 实测）**：站点当日已观测最高 28.7°C，但**当前实测只有 27.2°C**
（被雷暴外流冷池压下去）。`--hold` 分支的守卫把 d 算成 `29.0 − 28.7 = 0.3`，
报「剩余 24 步至少越线一次 = 99.5%」→ 把 `28YES` 的公允压到 **0.5%** 并打 `⚠止盈（现价高出公允 98%）`。
**这是错的**：随机游走的起点是**当前温度**，不是当日最高值 → d 应为 `29.0 − 27.2 = 1.9`。

**成因**：9/12 标定时站点全天贴着边界（当前值 ≈ 当日最高），两种写法数值几乎相同，看不出问题；
只有「站点已从当日最高回落 > 0.5°C」（对流冷池、午后锋面）时才会暴露。

**修法**：`--hold` 分支先读一次实时站点温度（`fetch_hko_1min(record=False)`，不污染 running-max 日志），
若 `当日最高 − 当前值 > 0.5°C` → **跳过守卫**并打印跳过原因；
否则行为与 v0.15.9 完全一致（即 9/12 标定口径不变）。

```
σ_flicker 守卫已跳过：当前实测 27.2°C 比当日已观测最高 28.7°C 低 1.5°C（回落/冷池）
  —— 守卫的随机游走起点不成立，跳过
```

> ⚠ **仍未解决**：把 σ_flicker（10 分钟级差分）当扩散系数外推到 **多小时的零漂移随机游走**，
> 在物理上并不成立（忽略日间升温趋势）。因此本守卫**只在贴近边界（|当日最高 − 当前值| ≤ 0.3）时**可信；
> 回落幅度大时应改用「当前值 + 剩余升温分布」估越线概率。这条纪律已写进 SKILL.md。

### 2. 新增《站点月度气候基线》方法（SKILL.md）

HKO `Daily Extract` 的月 XML（`https://www.weather.gov.hk/cis/dailyExtract/dailyExtract_YYYYMM.xml`）
给出逐日的 `[日, 气压, 日最高, 日平均, 日最低, 草温最低, RH, RH, 雨量]`，**11 年 9 月 = 351 天**，
一条 curl 就能拉全 → 可直接算**结算站自己的**无条件 / 按雨量分档的档位基线，不必依赖 ERA5 网格：

| 样本（2015–2025 年 9 月，n=351） | 均值 | P(最高<29.0) | P(≥31) | P(≥32) |
|---|---:|---:|---:|---:|
| 全部 9 月日 | 31.60 | **10.8%** | 66.4% | 47.3% |
| 雨日 ≥10mm（n=98） | 30.18 | **27.6%** | 31.6% | — |
| 大雨 ≥20mm（n=80） | 29.96 | **31.2%** | 27.5% | — |

**用法**：条件预报（NWP / 官方）给中心，本基线给**上包络 / 先验**。
**2026-09-14 的教训**：11:05 那轮把 28 档给到 50%，而基线（即便是大雨日）只有 27–31% ——
在没有任何气候先验的情况下，凭空给出 5 倍于基线的冷侧概率。
**纪律：任何「最高温停在 29 以下」这类封顶判断，必须先读该月基线，再谈天气条件。**

> ✅ 同一批 XML 还**证实了 9/11 / 9/12 / 9/13 的结算档**（32.6→32、33.1→33、31.7→31），
> `rec_pnl_log.csv` 里三日的 `provisional` 已全部改为 `confirmed`（**累计战绩不变：14 腿 5-9、+$65.34、ROI +7.0%**）。
> 该源滞后性也不如旧记录那么严重：2026-09-14 11:26 拉取时已含 9/13。

---

## v0.15.8 — 2026-09-13 · SKILL.md 补《接口字段速查》：三个数据源的字段陷阱（PATCH）

**不改变任何输出数字**（纯文档）。在 `SKILL.md` 加一节《接口字段速查（手工取数时踩过的坑）》，
收录手工复核取数时的三个坑 —— 不写清楚，下次直连接口第一次调用就 `KeyError`：

1. **Open-Meteo 多模式**：`models=a,b,c` 时 `daily` 的键**带模式后缀**
   （`temperature_2m_max_ecmwf_ifs025`…），且**没有**顶层各模式子字典（那是单模式才有的形状）。
2. **HKO 九天预报 `fnd`**：字段名是 `forecastMaxtemp`（**小写 t**）+ 嵌套 `{value, unit}`；
   `forecastDate` 为 `YYYYMMDD` 字符串，**当日的条目可能已从列表移除**（只剩未来 9 天）。
3. **HKO `CLMMAXT`**：`rformat=csv` 首行是 BOM + 中文标题，含 **3 行表头**，
   末两行是 `*** 沒有數據` / `# 數據不完整` 图例（别当数据行）；**该源滞后约 10 天**
   （9 月中旬只能取到 8/31）。

台账部分本次**无新增腿、无档位变更**：9/13 唯一腿 **YES28** 已于当日回填，结算 **31°C**（`provisional`）。
复查时间 **2026-09-13 21:31**，官方 Daily Extract 仍只到 **09-10**，故 09-11 / 09-12 / 09-13
三日继续保持 `provisional`，未转 `confirmed`。

---

## v0.15.7 — 2026-09-13 · 补两条复盘/台账操作规范：Brier 对「无根据的确定性」盲；CSV note 禁半角逗号（MINOR）

**不改变任何输出数字**（纯文档）。两条都来自 9/13 当日的实际失误，已写进 `SKILL.md`：

1. **Brier 检测不到「无根据的确定性」**（进《C. 校准与复盘》）。9/13 的 δ 饱和 bug 让 `--watch`
   在 13:00–13:30 连续 4 条报 `P(≥31)=99%`，而当天结算恰好是 31 —— 这 4 条在 Brier 上**几乎不扣分**。
   Brier 只看「押对哪一侧」，不看「幅度有没有依据」。→ 复盘必须并行做**时点一致性检查**：
   把 `forecast_log.jsonl` 每条概率与同一时点实况并排审，**≥95% / ≤5% 的极端值逐条摘出来查**。
2. **CSV `note` 禁半角逗号**（进《每日建议台账》）。`rec_pnl_log.csv` 无引号，`note` 里的
   `[28.6,29.0)` 会撑出多余列，`--settle --write` 回写时静默腰斩 note 且不报错。
   区间改用 en dash / 波浪号；**不要用 Edit 工具改 CSV 行**（文本替换不感知字段边界），
   改用脚本整行重写 + `csv.reader` 复查列数。
3. **顺手同步《每日建议台账》里的战绩表**：13 腿 / 3 日 → **14 腿 / 4 日**（5-9，净 +$65.34，
   ROI +7.0%），并把亏损归因按 family 重写 —— 「押注封顶」**4 战 0 胜**、
   「上望更高一档」右尾注 **5 笔全负**。此前该表与 `PNL.md` 已不一致。

另记一条本次查到的事实：**官方 Daily Extract 约滞后 3 天**（9/13 查询时最新只到 **9/10**），
故 09-11 / 09-12 / 09-13 的结算档至今仍为 `provisional`。

---

## v0.15.6 — 2026-09-13 · 修 δ 平移的概率饱和 bug：δ 改为随机量卷积（MINOR）

**这是「会改变输出数字」的修复**（故记 MINOR 而非 PATCH）。起因：9/13 盘中 `--watch` 连续
报 **P(≥31) = 99%**，与实况（站点已横盘 2 小时、再升 0.6°C 才够线）明显不符。

根因（`hk_edge.py` 原第 779 行）：`prob = cdf_upper(tbl, need − δ)` 把 δ 当**确定量**整体
平移经验分布。当 **δ > need** 时阈值穿到分位表最小值(0)以下 → `cdf_upper` 返回
`1 − q[0][0] = 1 − 0.01 = 99%`。物理上这等于「今天几乎必然再升 δ」，抹掉了分位表在 R=0
处的原子（h=13 有 **60%** 的日子当日最高已经出现）。

修法：把 δ 当**随机量**，`R_today = R_hist + Δ`、`Δ ~ N(δ, (DELTA_REL_SIGMA·|δ|)²)`，
对 ε 求平均 `E[cdf_upper(tbl, need − δ − ε)]`（新函数 `cdf_upper_delta`）。
Δ 的 1σ 取成与 |δ| 成正比（`DELTA_REL_SIGMA = 0.8`）——δ 小则不确定小，平滑、无硬阈值。

效果（9月 h=13 分位表实测）：

| 场景 | need | δ | 旧版 | 新版 |
|---|---:|---:|---:|---:|
| 9/13 13:20（站点 30.4 横盘，关键案例） | 0.60 | +0.77 | **99%** | **67%** |
| 9/13 13:40（站点 30.9，只差 0.1） | 0.10 | +0.17 | 99% | 78% |
| 早晨小 δ | 1.00 | +0.20 | 3.0% | 3.1%（几乎不动） |
| 尾档 need≫δ | 2.00 | +0.77 | 0.2% | **4.1%** |

- 早晨窗口（δ 小）行为不变 → 已校准的 09:00–11:30 出仓纪律不受影响。
- 尾档反而被抬到更接近站点口径，与「网格尾部偏薄、概率是下界」的既有结论自洽。
- 文档：`SKILL.md`「日内盯盘 `--watch`」的 δ 段与「已知局限」同步更新。

## v0.15.5 — 2026-09-13 · 盘中实测两条 one-touch 外推陷阱（网格降水未兑现→L 虚高；h≥11 δ 平移失真）（PATCH）

两条均在今日 9/13 实盘外推中发现，不改变任何输出数字，仅补进已知局限
（见 `SKILL.md`「日内盯盘 `--watch`」的「水位 L」段与「把 one-touch 存活阶梯换算成档位概率」段）。

一、网格模拟降水未兑现 → 水位 L 虚高约 1°C
- 今晨 09–10 时 NWP 网格报降水 1.9–2.1 mm/h，但全港实测 0 mm（骤雨未落地）。
- 时段偏差 `devs` 掺入「本该降温却没降」的样本，水位 L 整体抬高约 1°C，点估计被顶高、δ 推到限幅上限，30/31 档公允被压低。
- 纪律：用 L 前必须先核 `rhrread` 实时雨量；全港有雨站为 0 时 L 不可信，改用「实测升速 × 剩余时长 + 已观测最高截断」兜底。

二、经验分位表 h≥11 做 δ 平移会失真（R=0 原子 20%）
- δ=0 给 29 档 62%、δ=+0.2 给 28 档 0%，两个都不可用；网格口径 + δ 平移只会放大失真。
- 纪律：h≥11 且需外推当日剩余升温时，不要再对分位表做 δ 平移，改用「实测升速 × 剩余时长 + 已观测最高截断」直接给点估计与区间，分位表只作下限参考（不改动 v0.13.0 的 one-touch 主口径）。

## v0.15.4 — 2026-09-12 · 持仓检查加 σ_flicker 守卫；固化「每次更新预测都附头寸」约定（MINOR）

起因：用户提出以后每次发「更新预测」都会附上现有头寸，让我主动提醒止盈。
但当时的 `--hold` 在近边界处会给出**与事实相反**的结论，不修就等于把用户往坑里带。

一、修掉持仓检查的假信号（本次唯一的代码改动）
`--hold` 的 `fair` 直接取 `r0['probs']`（网格/分位表口径），而网格在近边界处低估越线
1–2 个数量级。9/12 14:00 实测：已观测最高 32.6、d=0.4，网格给 33 档 25.3%，
真实越线 ≈71–75%；持 NO33 @0.70、现价 0.945 时，旧逻辑算
`fair≈100% → EV +7% → 持有观察`，两小时后该腿归零。

新增 σ_flicker 守卫（仅 `--hold` 分支，仅当日生效）：
- `p_越线 = max(网格, 抖动)` —— 恒取 max，不分档；理由见 SKILL.md（站点 = 网格 + 单点额外方差，
  分布单调加宽 → 真值 ≥ 两个口径各自单算）
- 覆盖**边界那一对档位**：`YES b0` / `NO (b0+1)` 是「押封顶」侧，网格偏乐观；
  `NO b0` / `YES (b0+1)` 反被低估，别按公允值止损；抖动项 <30%（d 大）时守卫不动作
- 新增 `--steps-left N`：默认按当前时刻算到 15:30；回放当天盘中的历史时点必须显式给
- 主概率表**不覆盖**（仍网格口径）——保守方向：新开仓 edge 会被低估，不会被高估

回放验证（9/12 14:00，`--steps-left 9`）：
  旧：`33°C NO  fair 100%  EV +7%   持有观察`      → 归零
  新：`33°C NO  fair 28.6% EV −70%  ⚠ 止盈`        → 与「15:30 全港冲 33」的事实一致
  `32°C NO  fair 71.4% EV +1090% 仍低估 → 持有`    → 该腿确实反败为胜 +$142
回归：未来日 / 无 `--hold` / d 大的三分支均不变、不报错。

二、固化用户约定（SKILL.md 新增《固定约定》一节）
用户每轮给：档位 / 方向 / 入场价 / 份数，**加仓单独报**（9/12 漏记 $54 加仓 → 当日由
+$24.22 记成「其实 −$29.78」）。我每轮必做：① `market_prices.py --depth 8` 取**可卖出价**
（不是 mid）；② `hk_edge.py --observed-max … --hold …`；③ 写台账。
必须同时报「可报价浮盈 + 可成交量」。用户没给头寸就明说「本轮不做止盈检查」，不猜。

版本：四处版本号同步至 0.15.4。

---

## v0.15.3 — 2026-09-12 · 止盈/止损不能用概率阈值（90%/10%）—— 台账检验（PATCH）

纯文档。`hk_edge.py` 除 VERSION 一行外未动，模型概率 / EV / Kelly 全部不变。

起因：用户提出一条经验规则 ——「持有头寸概率涨到 90% 以上就止盈，跌到 10% 以下不止损、
搏尾部」。用台账 11 条有首推价的腿做了检验。

一、两半其实是同一个命题，不可能只对一半
「≥90% 卖」= 断言市场高端高估；「≤10% 留」= 断言市场低端低估；两者等价于
「市场的隐含分布太窄」。而《σ_flicker》那章给的正是这个方向（9/12 市场给 33 档
5-8%，实际 ≈75%）。所以 0.90 / 0.10 这两个数字本身没有信息量。

二、检验结果：规则的全部收益来自 1 条腿
模拟「碰到 0.90 就按 0.90 平仓」：
  09-09  Δ −$59.00（砍掉赢腿 NO31）
  09-11  Δ −$19.07（砍掉 NO30 / NO31 / YES32）
  09-12  Δ +$132.43（救下 NO33，0.70→0.93 后归零）
  合计   Δ +$54.36
原因：0.90 分不出「峰已过、0.90 就是真实概率」与「还有升温窗口、0.90 是虚高」。
前者（09-09 NO31、09-11 三腿）吃掉 $78；后者（09-12 NO33）贡献 $132。
→ 决定符号的是 d（已观测最高到下一整数线的距离）与剩余升温窗口，不是概率水平。

三、第二半（≤10% 不止损）在同一份台账里是 1 胜 1 负 1 灾难
  09-12 NO32 @0.36  13:50 判死、15:30 反转  +$142.22  （规则对）
  09-12 YES31 @0.28 便宜后继续归零            −$69      （规则错）
  09-09 YES30 @0.19 0.370→0.060 一路补仓     −$155      （全书最大单笔亏损）
区分它们的不是「便宜」，是「方向」：顺趋势做 NO 砸下去能回来，逆趋势买具体档的 YES 砸下去
就是真死 —— 与《出仓时段》「盈利集中在顺上升方向做 NO」同源。

四、写入 SKILL.md 的结论
新增《止盈 / 止损：判据是「可成交价 vs 你的公允概率」，不是 90% / 10%》一章，
把既有那条「只要卖出价 > 公允价，多卖一份多赚一份」补上失败模式与 d 条件，
并明确「止盈与止损在二元合约里是同一个动作」。

样本限制如实标注：13 腿 / 3 结算日，多数腿无盘中价记录，是否曾被触发只能对赢腿
和已知的 NO33 判定。待办：台账加 peak_price 列，之后才能对所有腿自动回测退出规则。

---

## v0.15.2 — 2026-09-12 · 9/12 复盘（结算 33°C）：结算档一错，整段归因翻转（PATCH）

**`hk_edge.py` 除 `VERSION` 常量外一行未动 —— 模型概率 / EV / Kelly 全部不变。**
唯一代码改动在 `review_brier.py` 的 `--flicker` 判读列（原来写死的阈值与表内数字自相矛盾）。

### 背景：9/12 结算档从 32 更正为 33（v0.15.1）之后，复盘结论必须重做

用同一份 `data/decision_log.csv` 重跑，**一个档位之差把领先方整个对调**：

| 结算档 | 模型 Brier（9/12 上午） | 市场 Brier | 结论 |
|---:|---:|---:|---|
| 32°C（v0.15.0 用的错值） | 0.0792 | **0.0556** | 市场领先 29.9% → 「模型有纳入时滞」 |
| **33°C（正）** | **0.1927** | 0.2357 | **模型领先 18.3%** |

- **v0.13.8 写进 SKILL.md 的「9/12 上午市场反超、差距来自官方修订的纳入时滞」作废。**
  观察本身仍成立（市场 07:50 先重定价到 0.59，模型 09:18 还在 0.40），
  但两家都把众数押在 32 —— **都错**，模型只是错得少（Brier 0.19 vs 0.24）。
- 新增纪律：**结算源（HKO Daily Extract）滞后 2–3 天，依赖结算档的统计结论在官方数据落地前
  只能标 provisional，不得写进 SKILL.md 当永久结论。** 本次实查 `dailyExtract_202609.xml`
  只含 1–10 日 + 月均值/常值两行 → 09-11 / 09-12 仍 provisional。
- 上午「模型领先」的适用条件从「距下一整数档 ≥0.6°C」**收紧为 ≥1.0°C**。

### 真正的漏算项是 d=0.4，不是 d=0.1

9/12 14:00 已观测最高 32.6，距 33.0 差 **0.4**：

| 口径 | P(越线) | 结果 |
|---|---:|---|
| 分位表（ERA5 网格） | 0–1%（模型给 6.5%） | ✗ |
| 市场 | 5–8%（33YES 报价 0.05–0.08） | ✗ |
| **σ_flicker 抖动模型（剩 10 步）** | **75.2%** | ✓ 15:30 越线，**结算 33°C** |

→ **33YES @0.05–0.08 是 9/12 全天最大的一笔机会（12–20 倍），被我们自己的规则边界屏蔽掉了。**

`review_brier.py --flicker` 的判读列改为**由概率反推**（原来写死 d≤0.3 主导 / d≥0.6 可用，
与表内数字冲突：d=0.6 那行标「抖动项 <30%」，实际剩 10 步 **37.3%**）：

| 抖动项 P（剩 10 步） | 对应 d | 处理 |
|---|---|---|
| ≥70% | **d ≤ 0.4** | 抖动主导：**不输出概率、禁止反向** |
| 30–70% | d ≈ 0.5–0.6 | 过渡区：**取 `max(分位表, 抖动)`** |
| <30% | d ≥ 0.8 | 分位表可用 |

**为什么是 max 不是加权平均**：站点 = 网格 + 单点额外方差，分布单调加宽 →
越线概率真值 **≥ 两个口径各自单算**。v0.14.0 的「中间按 0.5 折」两头不靠
（9/12 d=0.4 落在该区间，模型实际输出仍 ≈ 纯网格的 6.5%）。

### 一天两个同量级尖峰，第二个才决定结算档

| 时刻 | 13:40 | **13:50** | 15:20 | **15:30** |
|---|---:|---:|---:|---:|
| HKO | 31.7 | **32.6** | 32.2 | **33.1** |
| 共振范围 | — | **本站局地**（全港几乎不动） | — | **全港同步**（赤鱲角 33.5 / 大陇 33.4 / 打鼓岭 33.2 / 元朗公园 33.2 / 上水 33.0） |

- 新增第四条纪律：**「等最后一个尖峰」（9 月晴天约 15:30 之后）** ——
  最后一个尖峰出现前，任何「该档已定格」的结论都不成立。
  这推翻了此前「13:00–15:00 内可判定格」的写法。
- 新增判别角度：**判「已定格」先看这个尖峰有多少站共振**（单站可被证伪，全港同步不可）。

### 记录完整性的两个洞

1. **9/12 午后 `decision_log.csv` 零记录** —— 当天最贵的 13:50 / 14:10 / 15:30 全在午后，
   但该文件只在 `hk_edge.py` 带 `--market` 运行时落盘，当天午后只单独跑了 `market_prices.py`。
   → 纪律：**每次出建议前必须用 `--market` 跑一次 `hk_edge.py`。**
2. **台账不记加仓 = 不记加仓的亏损** —— 9/12 的 YES31 首推 $15（10:15），
   13:50 同一腿另加仓 $54（0.010）并全额归零；$54 不入账 → 当日显示 **+$24.22**，
   若计入则 **−$29.78**。已在 CSV 该行 note 中标注，并在 SKILL.md
   《每日建议台账》计入口径补第 5 条。**逐腿胜负是主信号，金额只作量级参考。**

### 文件

- `SKILL.md`：《复盘的硬证据》章节重写（含新小节《⚠ 结算档错了，整段归因会翻转》
  《日志缺口》）；《漏算的那一项》全文更新（σ 0.355 / n=30、四口径对照、判读规则）；
  《峰值窗口内不要钉档位》补全天双尖峰表与第四条纪律；新增《9/12 结算 33°C：全天成绩与错误归因》；
  《每日建议台账》补第 5 条。
- `scripts/review_brier.py`：`--flicker` 判读阈值改为概率反推 + 判读口径说明 + docstring 补 9/12 反例。
- `scripts/data/rec_pnl_log.csv`：9/12 YES31 行 note 补加仓口径提示（金额与胜负不变）。
- 四处版本号同步至 0.15.2。

### 样本限制（如实标注）

σ_flicker 仅 **2 天 / 30 个差分**，越线 2 例、未越线 1 例；σ 随天气型变化。
抖动模型是**零漂移随机游走**，在午后已转雨/明确降温时会高估 → **表值是上界**。
台账为建议口径非成交口径。**方向可信，数值不可信。**

---

## v0.15.1 — 2026-09-12 · 更正 09-12 结算档 32 → 33：台账由「−$3.44」翻正为「+$71.64」（PATCH）

**代码零改动**（`hk_edge.py` 除 `VERSION` 常量一行外未动，`rec_pnl.py` 未动），
只回填了台账数字 + 更正文档。既有输出数字：模型概率、EV、Kelly 全部不变。

### 错在哪
v0.15.0 建账时在 **14:57** 回填 2026-09-12，用的是当时**时点快照**最高 **32.6°C** → 记成 32 档。
但结算量是**全天**的绝对最高：当日 **15:30 全港同步升到 33.0–33.5°C**（赤鱲角 33.5 / 大陇 33.4 /
打鼓岭 33.2 / 元朗公园 33.2），本站单点 **33.1°C**，落在 `[33.0, 34.0)`。
**用峰值窗口内的时点快照代替全天最高 —— 正是 SKILL.md《峰值窗口内不要钉档位》自己警告过的错误。**

### 证据链（三个独立来源一致）
1. **结算站全天快照最高 33.1°C**（`watch.log` 15:30，同时刻全港多站 ≥33.0，非孤立尖峰）；
   且结算源来自 1 分钟数据、快照是 10 分钟抽样 → **结算值 ≥ 快照最高**，即 ≥ 33.1。
2. **Polymarket 09-12 市场**：33 档 YES bid/ask = **0.997 / 0.999**（≥0.95 判已定档），
   其余 10 档 NO 均挂 0.999。
3. 口径互校：官方 Daily Extract 的 `Absolute Daily Max` 与市场结算一致（09-09 = 32.2 → 32 档、
   09-10 = 31.9 → 31 档，两次都对得上）。

### 数字改了什么（逐腿）

| 腿 | 首推价 | 建议投入 | 旧结果 | 新结果 | Δ |
|---|---:|---:|---|---|---:|
| 09-12 NO33 | 0.7000 | $103.00 | ✅ +$44.14 | ❌ −$103.00 | **−$147.14** |
| 09-12 NO32 | 0.3600 | $80.00 | ❌ −$80.00 | ✅ +$142.22 | **+$222.22** |
| 09-12 YES31 | 0.2800 | $15.00 | ❌ −$15.00 | ❌ −$15.00 | 0 |

**当日由「亏 −$50.86 / ROI −25.7%」变为「盈 +$24.22 / ROI +12.2%」**；
**累计由「净 −$3.44、ROI −0.4%、2 盈 1 亏」变为「净 +$71.64、ROI +7.7%、3 盈 0 亏」**（13 条腿不变）。

### 顺带修正
- `settle_status`：官方 Daily Extract 目前**只发布到 09-10**（已实查 `dailyExtract_202609.xml`），
  故 09-11 的 `confirmed` 降级为 `provisional`（其 32 档由市场 0.9975 与快照 32.6 佐证，数值不变）。
  09-11 / 09-12 待官方值出来后重跑 `--settle DATE=N --write` 转 confirmed。
  注：`rec_pnl.py --settle` 落盘时把 status 固定写 `confirmed`，本次是在写盘后手工改回 `provisional`。
- 归因段落重写：`NO` 的 3 笔亏损**全部是「押注封顶」**（9/09 NO32、9/11 NO32、9/12 NO33），
  与 `YES` 的 5 笔「上望更高一档」右尾注互为镜像 —— 都是逆着当日已确立方向下注。
  盈利仍集中在「顺已确立的上升方向做 NO」（9/09 NO31、9/11 NO30+NO31、9/12 NO32）。
- SKILL.md《峰值窗口内不要钉档位》补一条**对称教训**：该纪律两个方向都成立，
  13:50 的 32.6 判 NO32 死刑、15:30 的 33.1 又判 NO33 死刑，方向对错都只是一枚硬币。

> 教训值 > 数字本身：**台账的第一次「翻正」不是方法变好了，而是记账时点选错了。**
> 收盘后再回填，别用盘中快照定档。

---

## v0.15.0 — 2026-09-12 · 每日建议盈亏台账：从「散在会话里的复盘」变成可累计的记分牌（MINOR）

`hk_edge.py` 除版本号外一行未动，所有既有输出数字不变。新增 1 个脚本 + 1 个**纳入 git 的数据文件** + 1 个仓库根目录记分牌。

### 问题
「每天给出去的建议，整体是赚还是亏？」这个问题此前**没有答案** —— 逐日复盘散落在会话记录里，
既不能累计，也无法回答「这套方法长期有效吗」。单日结论还容易自我美化：9/11 的持仓口径是
「5 赢 6 输」，按腿去重后其实是「3 赢 2 输、ROI +22.3%」，两种数法差了 3 倍的印象。

### 新增
- `scripts/rec_pnl.py` —— 读台账出「逐日胜/负 + 累计曲线 + 按方向拆分」；
  `--settle DATE=N --write` 回填结算档（默认只试算）、`--auto` 用 `maxt_HKO.csv`、
  `--open` 列未结算、`--json`、`--markdown` 重生成 `PNL.md`。
- `scripts/data/rec_pnl_log.csv` —— 台账，一条腿一行，**明确纳入版本控制**
  （`.gitignore` 里加了「保留」注释；与隐私性被忽略的 `decision_log.csv` 区别开）。
- `PNL.md` —— 仓库根目录的记分牌，GitHub 首页可读。
- `SKILL.md` 新增《每日建议台账：胜 / 负与累计 ROI》，含计入口径与「每日收盘必做」四步。

### 计入口径（防自我美化的关键）
每条腿取「**首推价 + 建议仓位数**」，同腿后续加仓/改价/重新推荐不重复计入；
论点翻转后重新建仓仍算同一腿。**记录的是建议不是成交** —— 薄盘实际只能成交建议额的 20–40%，
故为**收益上限口径**，但**符号（胜/负）不受影响**。未结构化留存的 9/07–09/08、9/10 不硬凑，在 CSV 里注明。

### 首份战绩（13 条腿 / 3 个结算日）
| 结算日 | 结算档 | 腿 | 胜-负 | 投入 | 净盈亏 | ROI | 当日 |
|---|---|---:|---:|---:|---:|---:|:--:|
| 09-09 | 32°C | 5 | 1-4 | $577.30 | +$12.70 | +2.2% | 盈 |
| 09-11 | 32°C | 5 | 3-2 | $156.00 | +$34.71 | +22.3% | 盈 |
| 09-12 | 32°C * | 3 | 1-2 | $198.00 | −$50.86 | −25.7% | 亏 |
| **合计** | | **13** | **5-8** | **$931.30** | **−$3.44** | **−0.4%** | 2 盈 / 1 亏 |

**按方向拆开才是这次新台账最有价值的输出**：`NO` 7 腿 **4-3**、ROI **+25.1%**；
`YES` 6 腿 **1-5**、ROI **−76.7%**。YES 的 5 笔亏损里有 5 笔是「上望更高一档」的右尾注。
→ 与《峰值窗口内不要钉档位》《σ_flicker》同源：**亏损来源稳定且可命名，不是随机噪声。**

`*` 9/12 结算档暂定（结算站已观测最高 32.6 + 市场 32 档 0.93；官方 Daily Extract 隔日出）。

### 版本
四处版本号同步至 0.15.0。样本限制如实标注：3 个结算日**无统计显著性**，且 9/09 与 9/11 结算档相同。

---

## v0.14.0 — 2026-09-12 · 找到「距边界 0.1 却判 9%」的漏算项：σ_flicker（MINOR）

`hk_edge.py` 除版本号外一行未动，所有既有输出数字不变。新增一个诊断子命令 + 一章方法论修正。

### 问题
9/12 13:44，已观测最高 **31.9**（距 32.0 只差 **0.1**），市场把 31 档打到 `0.0015`（P≈99.85%），
我们按分位表给 **9%**。差距 11 倍。市场对还是我们对？

### 答案：市场对。漏算的是「站点 10 分钟级抖动」
分位表 `remaining_rise_cdf.json` 是 **ERA5 网格**口径。网格是 9–31 km 的空间平均，
单点的局地抖动（阵风、云影、建筑热羽）**在平均中被抹平**，所以网格口径下
「距边界 0.1–0.3」看起来是稀有事件。

实测（10 分钟相邻差分，午后，n=17）：

| 日期 | σ_flicker | 最大单步 |
|---|---:|---:|
| 9/12 | 0.357 | **0.9** |
| 9/10 | 0.303 | 0.4 |
| 合并 | **0.330** | — |

按 N(0, 0.33)，**「剩余窗口内至少越线一次」**：

| 距下一档 d | 剩 6 步 | 剩 10 步 |
|---:|---:|---:|
| **0.1** | 94.4% | **99.2%** |
| **0.2** | 85.2% | **95.8%** |
| **0.3** | 70.0% | 86.6% |
| 0.6 | 19.0% | 29.7% |

三个口径同场对照：

| 场景 | 距 d | 分位表（我用） | 抖动模型 | 市场 |
|---|---:|---:|---:|---:|
| 9/11 13:50 | 0.2 | **25%** | 95.8% | **97%** |
| 9/12 13:44 | 0.1 | **9%** | 99.2% | **99.85%** |

**市场两次都准，误差 <2pp；我们的口径低估 1–2 个数量级。**
且 d ≤ 0.3 时 **d ≤ σ_flicker** —— 越线是基准情形，不是尾部事件。

### 新增 `review_brier.py --flicker`
算出 σ_flicker 并输出「距边界 d → 越线概率」判读表（d≤0.3 标「抖动主导：禁止反向下注」）。

```bash
py -3 scripts/review_brier.py --flicker
```

### `SKILL.md` 新增《漏算的那一项：站点的 10 分钟级抖动 σ_flicker ≈ 0.33 °C》
把已有判别钩子数字化：**d ≤ 0.3 → 禁止反向下注；d ≥ 0.6 → 分位表可用；中间按 0.5 折。**

### 边界与反例（不要把市场当成全知）
9/10 13:20 达 31.8（d=0.2），剩 6 步 → 抖动模型给 85.2%，**实际未突破**（当日结算 31 档）。
即 5–15% 的失败情形真实存在，市场价 97–99.85% 仍系统性偏高；但 d ≤ 0.2 时反向下注 = 赌这个失败，期望上仍错。

> **样本限制**：σ 只有 **2 天 / 17 个差分**、反例 **1 次**。σ 随天气型变化（晴热静风日最大）。
> 当前只在 d ≤ 0.3 时作「禁止反向」的硬开关，**不做精细概率输出**。待 10+ 天样本再收紧。

## v0.13.8 — 2026-09-12 · 修 `--help` 崩溃 + Brier 时间分层复盘 + 新增 `review_brier.py`（PATCH）

不改任何既有输出数字。一处代码 bug 修复 + 一个新复盘脚本 + 一章实战复盘。

### 1. 修 bug：`hk_edge.py --help` 直接崩溃（ValueError）

`--bankroll` 的 help 字符串用了 f-string 展开百分号：

```python
help=f'本金(USDC)，给出 {KELLY_FRAC:.0%} Kelly 建议下注额'   # ❌ 展开成 "35%" 后
```

argparse 会对 help 再做一次 `%`-格式化，遇到裸 `%` 立刻抛
`ValueError: unsupported format character 'K' (0x4b) at index 16`。
**只要敲 `--help` 就崩**，且 `--help` 崩在 `parse_args` 内部，用户没有任何可用信息。

修法（`hk_edge.py` 约 878 行）：把百分号写成 `%%`，不再依赖 f-string 的 `%` 格式符：

```python
help=f'本金(USDC)，给出 {KELLY_FRAC*100:.0f}%% Kelly 建议下注额'   # ✅
```

### 2. 新增 `scripts/review_brier.py`：收盘复盘用

`--score` 打的是 `forecast_log.jsonl` 的 **one-touch 阶梯**口径，回答不了
「模型和市场谁更准、在一天的哪个时段更准」。新脚本改用 `decision_log.csv`
（逐时点成对记录 `fair_p` 与 `market_p`），与结算档比对得逐时点 Brier，并按上午/午后分层。

```bash
py -3 scripts/review_brier.py --truth 2026-09-11=32   # --truth 可省，自动读 maxt_HKO.csv
```

结算档默认从 `maxt_HKO.csv`（= 结算源本身）取 `floor(值)`；该源滞后约 10 天，近期日须手填。

输出顶部打印**实际读取的路径与行数**。这一条是踩坑加的：本技能双副本（`.workbuddy` 活 / `.codebuddy` git 源）
各自的 `scripts/data/` 是**独立本地状态**，跑错副本会静默读到几天前的旧日志、结论整个错掉且不报错
（2026-09-12 实际踩过：`.codebuddy` 的 `decision_log.csv` 停在 09-08，149 行 vs 活副本 457 行）。

### 3. `SKILL.md` 新增《复盘的硬证据：模型的 Brier 随一天推进而衰减》

| 日期 | 结算 | 上午（<12时） 模型 / 市场 | 午后（≥12时） 模型 / 市场 |
|---|---|---|---|
| 9/10 | 31°C | **0.1038** / 0.1411 → 模型领先 **26.4%** | （无当日午后记录） |
| 9/11 | 32°C | **0.0917** / 0.1215 → 模型领先 **24.5%** | 0.0543 / **0.0016** → 市场领先 **97.1%** |
| 9/12 | 32°C | 0.0792 / **0.0556** → 市场领先 29.9% | — |

**这是「出仓窗口收敛到 09:00–11:30」的第一条直接量化证据**（此前只有逐日 a\* 的间接证据，p≈0.31 不显著）。

9/12 上午市场反超的原因已查明、且不是模型精度问题：隔夜 HKO 官方预报 **30→32°C**（一次性 +2°C），
市场 07:50 即重定价到 0.59，模型 09:18 仍停在 0.40 —— 差距全部来自**官方预报修订的纳入时滞**。
→ 新推论：上午的「模型优势」以「官方预报没有隔夜大改」为前提；若开盘前官方 max 与模型中心差 ≥1°C，
当天上午先默认跟市场，等 `--watch` 实况积累 2–3 小时再切回模型。

同章另记两条 9/11 教训：
- **幸存者偏差**：当天 mu=32.0、结算 32°C 貌似命中，但快照最高 32.5，靠 +0.5 余量落在档内；
  若为 31.9 则当天 5 笔结论全部翻转 → 边界日不要用「点估计命中」自我评价。
- **两次反向操作**：13:50 押「不是 32」、14:10 押「高于 32」，45 分钟内方向相反、全输 →
  峰值窗口内默认空仓。

## v0.13.7 — 2026-09-11 · 修好发版流程的路径陷阱，清理孤儿文件（PATCH）

`hk_edge.py` 除版本号外一行未动，任何输出数字不变。改动全在 `SKILL.md`「发版流程」章节。

### 背景：一个自己造成的孤儿文件

仓库根目录出现了一份未跟踪的 `hk_edge.py`（58 937 B，`Sep 11 14:42`），
内容是 **v0.13.5 时点的完整脚本**，与 `scripts/hk_edge.py` 的差异**只有 `VERSION` 一行**
（`0.13.5` vs `0.13.6`）——是 v0.13.5 发版时同步命令的产物。

**根因**：双副本同步命令把不同目录的文件塞进了同一个目标：

```bash
# ❌ 错误：最后那个参数是 $S/（根目录），于是 scripts 下的文件被散到仓库根
cp "$D/SKILL.md" "$D/README.md" "$D/CHANGELOG.md" "$D/scripts/hk_edge.py" "$S/"
```

同批的 `SKILL.md` / `README.md` / `CHANGELOG.md` 本就该在根目录，等于原地覆盖自己、无害；
唯独 `scripts/hk_edge.py` 成了散落孤儿。此后每次 `git status` 都多一行 `?? hk_edge.py`。

### 处置

- **文件已删除**。删除前已核对：其 md5 `99a2235ed36310bf0ab3eeef2331f199`
  与 `git show 21d5f93:scripts/hk_edge.py` **逐位相同**，即内容在 git 历史中有完整对应，
  删除**完全可逆**（`git show 21d5f93:scripts/hk_edge.py > hk_edge.py` 即可还原）。
  该文件从未被 git 跟踪（`git log --all -- hk_edge.py` 为空），也未被任何脚本引用。

### `SKILL.md`「发版流程」修订

1. **第 4 步新增**（原 4 步扩为 6 步）：「双副本同步：`cp` 的目标路径必须逐文件写全，
   不同目录的文件不许塞进同一条命令的同一个目标」——含本次踩坑的 ❌ 反例与 ✅ 正例，
   并要求同步后用 `md5sum` 逐文件比对（四文件全 OK 才算成功）。
2. **第 5 步新增**：只 `git add` 明确列出的目标文件，**不要 `git add -A` / `git add .`**；
   推完用 `git ls-remote origin main` 核对（`gh` CLI 未认证）。
3. **第 6 步新增**：收尾自检 `git status --short` 应只剩预期的 `M`，**不应出现任何 `??`**。
4. **修正第 2 步**「同步三处版本号」→「**同步四处**」，补上最易漏的
   `CHANGELOG.md` 自身的「当前版本：」行。

---

## v0.13.6 — 2026-09-11 · 出仓时段分层、a\* 概率校准、单日敞口上限（PATCH）

`hk_edge.py` 除版本号外一行未动，任何输出数字不变。全部改动在 `SKILL.md`，
新增独立章节「出仓时段与仓位上限」，首次用**已结算四天（9/08–9/11）的 440 行快照**做时段分层量化。

### 核心量化结果

1. **edge 只在当天上午存在。** 分桶 Brier（模型 vs 市场）：提前一天 D-1 = 0.6112 vs **0.5914**（模型差）；
   当天上午 = **0.6430** vs 0.6768（模型好）；当天下午 = 0.2496 vs **0.1412**（模型差，误差为市场 1.77 倍）。
2. **模型概率最优权重 a\***（最小二乘，`p* = a*·模型 + (1−a*)·市场`）：
   上午 **+0.695**（edge 打 7 折）｜下午 **−0.053**（零信息）｜提前一天 **−0.189**（**负**信息）。
3. **11 时是全天优势峰值**：模型胜天率 **75%**（4 天里 3 天），a\* = 1.37；剔除 9/09 后 2.11。
   08 时 a\* = −1.20（样本不足），14 时胜天率 **0%**、a\* = −0.13（网格已见顶）。
   09–11 时合计胜天率 7/11 = 64%；13–14 时合计 1/5 = 20%。

### 新增纪律（3 条）

1. **出仓窗口收敛到 09:00–11:30**（重心 10:30–11:30）。13:00 之后与「提前一天」只做实况播报，不出仓位建议。
   机制：11 时同时满足「实况已积累 2–3 小时升温速率」与「网格日变化曲线尚未见顶」，13 时后两者同时失效。
2. **概率必须按 a\* 打折后再算 EV / Kelly**（上午 `0.70×模型 + 0.30×市场`）。
   名义 EV 与实际期望增长裂口极大：上午名义 +131% vs 实际 +1.5%/腿；下午名义 +446% vs 实际为负。
3. **单日所有腿累计建议金额 ≤ 本金 25–30%（硬顶）**，超限按 a\* 排序砍低权重腿。
   实测 9/09 累计敞口 **55% 本金**（浮亏 −28.5%、全天仅 +2%、当日 a\* = −0.67），9/10 单批即 40%。

### 结论：不提高 Kelly 系数

期望对数增长 g(k)：上午 k = 0.35 / 0.5 / 0.75 / 1.0 → +1.50% / **+1.56%** / +1.31% / +0.73%（几乎无差别）；
下午 → +3.78% / +0.95% / **−7.57%** / **−25.4%**（k ≥ 0.5 转负）。**维持 35% Kelly**。

### 顺带修正

- 「峰值窗口（13:00–15:00）不出建议」条目的窗口扩大为「整个下午 + 提前一天」，并加章节交叉引用。
- 纠正一处被结算推翻的表述：11:05 的 NO31 不是「被市场追平」，持有至结算为 **+85% ROI 的赢单**。

### 样本限制（已在正文标注）

日内快照高度自相关，**有效样本仅 4 天**；逐日上午 a\* = 1.40 / −0.67 / 1.11 / 1.32（一天翻转符号），
二项单尾 **p ≈ 0.31 不显著**。方向可信、幅度不可全信，攒够 15–20 天再重估。

---

## v0.13.5 — 2026-09-11 · 实战纪律补强：边界日、峰值窗口、网格见顶判定（PATCH）

`hk_edge.py` 除版本号外一行未动，任何输出数字不变。全部改动在 `SKILL.md` 的「已知局限 / 交易纪律」，均来自 2026-09-11 当天 Polymarket 实盘复盘。

### 新增纪律（4 条）

1. **边界日禁止反向下注 + 两问判别钩子**。实况距下一整数档 < 0.3 °C、且仍在当日升温窗口（≤ 15:30）时，「市场 ≥ 90%」视为强证据，禁止仅凭分位表反向开仓（13:50 的 NO32@0.040 被 20 分钟内证伪归零）。两问缺一不可：① 距下一档 ≥ 0.6 °C 才有下注空间；② 网格日变化曲线见顶后不论距离多远都不下注。
2. **峰值判定看网格日变化曲线顶点，不看 Δ 累计值**。Δ（站点 − 网格）是累计量、不含时间信息，外推峰值必错（14:10 的 NO32@0.140 浮亏 −50%）。三日网格全在 14 时见顶，网格见顶后站点同日不再创新高。
3. **峰值窗口（13:00–15:00）只播报实况、不出仓位建议**。9/11 三次建议两次错、一次被市场一小时内追平；模型对 1 小时内 0.2–0.6 °C 尺度的近端变化无预测力。
4. **尾档交叉验证改用结算站百年序列**（`maxt_HKO.csv`，1884 至今）。无条件气候基线不依赖任何模式 / 校准；近期窗口（近 10 年）尾档比 30 年均值高 2.5 倍，定尾档必须用近期窗口。

### 新增「已知局限」

- `--watch` 概率输出与自身点估计口径不一致（δ 被限幅到 +1.0，丢 1.88 °C），标记待修，附不改代码的对策（集合离散度 < 0.4 °C 时把 `--watch` 概率当严格下界，改走 `--date` 口径）与彻底修复条件（限幅改自适应 + 重跑 Brier 验证）。

---

## v0.13.4 — 2026-09-10 · 彻底消除计划任务每次触发弹出的命令框（PATCH）

**`hk_edge.py` 除版本号外一行未动，任何输出数字不变。**

### 现象

用户报告：计划任务每次触发，屏幕弹出一个 `cmd.exe` 黑框，框里是乱码，
如 `'鍛戒护妗嗐€?rem' 不是内部或外部命令`。

### 根因（两个独立缺陷叠加）

1. **每次触发必弹窗。** 任务动作是 `watch.bat`。`.bat` 只能由 cmd.exe 解析，而 cmd.exe 是控制台
   子系统程序，Windows 必须给它一个控制台；任务以「交互」身份运行，这个控制台就是屏幕上那个框。
   触发频率实测 `PT30M` / `PT6H` / 起始 11:00 —— 每天 11:00–17:00 每 30 分钟一次，与用户看到的
   14:00 / 14:30 / 15:00 / 16:00 / 16:30 完全吻合。

2. **框里的乱码。** `watch.bat` 被写成了 UTF-8 编码 + LF 换行，而 cmd.exe 按系统 ANSI 代码页
   （中文 Windows = GBK）解析 .bat。UTF-8 中文注释解成乱码，同时 LF 换行让 `rem` 词元被粘到
   上一行行尾，于是 cmd 把注释当命令执行，刷出满屏「不是内部或外部命令」。

两项都用对照实验复现过：同一段注释，**UTF-8+LF 版**输出 4 行乱码报错（与用户截图逐字相同），
**ASCII+CRLF 版**输出 `PARSE_OK` 且零报错。

### 修法

1. **`watch.bat` 重写为纯 ASCII + CRLF**（实测 1263 字节 / 20 CRLF / 0 裸 LF）。
   仍会弹窗 —— 这是 `.bat` 的固有属性，无法规避 —— 但不再产生任何报错。

2. **新增 `scripts/watch_silent.py`，计划任务改为直接由 `pythonw.exe` 启动它。**
   `pythonw.exe` 是 GUI 子系统程序，Windows 根本不为其创建控制台，**零窗口**。
   实测：手动 `Start-ScheduledTask` 后前后两次枚举窗口均为 0 个可见控制台。

   **为什么不直接指向已有的 `watch_silent.vbs`**：`WScript.Shell.Run(..., 0, False)` **立刻返回**，
   计划任务会在 1 秒内判定「已完成」，`MultipleInstances=IgnoreNew` 随之失效 —— 每 30 分钟再叠一个
   新循环，最多 12 个并发打 API。直接启动 `pythonw.exe` 时，任务全程保持 `Running`
   （实测 `LastTaskResult = 0x41301`），`IgnoreNew` 才真正成为看门狗：只在上一轮已死时才重启。

3. **`watch_silent.py` 自带 PID 锁**（`.watch_loop.pid` + 存活性检查），防手工双击与计划任务撞车。
   硬杀后残留的锁无害：启动时检查 pid 是否仍存活，已死则接管。

4. **日志改由 Python 自己打开，放弃 shell 重定向。** 这条是被实测逼出来的：cmd.exe 的 `>>`
   重定向句柄**拒绝写共享**，所以只要旧式循环还活着，其它进程 append `watch.log` 一律
   `PermissionError [Errno 13]`（2026-09-10 16:35 实测）。Python 的 `open()` 允许共享，
   全部走 `watch_silent.py` 后这类争用消失。同时把失败兜底从 `os.devnull` 换成 `watch_err.log`
   —— 一个跑完却什么都没记的启动器，比一个会报错的更糟。

5. **修 `stop_watch.vbs`。** 原匹配条件是「命令行含 `hk_edge.py` **且**含 `--loop`」，而
   `watch_silent.py` 是导入 `hk_edge` 自行调用 `main()`，命令行里两者都没有 —— 老条件会对正在
   运行的循环报「没有找到」。现额外匹配 `watch_silent.py`（已实测命中），硬杀后顺带清理锁文件。

### 顺带发现（写进 SKILL.md 排障节）

**不要在一个 cmd.exe 正在执行的 .bat 上做就地改写。** 排查过程中我重写了 `watch.bat`，正在执行的
cmd 重新读取了文件内容，同一个 cmd 又起了一个 `python.exe --loop`（16:35:07，父子关系已核实）。
计划任务的动作条目不存在这个问题 —— 它在启动时被计划任务服务快照。

## v0.13.3 — 2026-09-10 · 推翻「向市场收敛」纪律，补模式偏冷量化（PATCH）

**只改文档，`hk_edge.py` 一行未动，任何输出数字不变。**（三处版本号已同步核对。）

### 推翻一条会造成亏钱的旧纪律

原「已知局限」写着：*「若自己的估计连续 3 次朝同一方向被市场修正，视为自己有系统性偏差，
主动向市场收敛一半」*。**9/10 收盘复盘证明它是错的，会导致从正确位置漂移。**

实证（2026-09-10，争议档位 32°C）：

| 时刻 (HKT) | 市场隐含 P32 | 我方 P32 |
|---|---:|---:|
| 09:31 | 44% | 14.5% |
| 11:17 | 61% | 27.7% |
| 14:34 | 74.5% | 58%（按旧纪律上调后） |
| **结算** | **0%**（实测 31.8 → 31 档） | — |

市场全天单边上修、最后归零；**早上那个 14.5% 才是对的**，而按旧纪律上调到 58% 同样错。

→ **新纪律**：模型与市场分歧、且模型输入（NWP / 实况）没有新数据落地时，**不许改模型**。
市场日内单边移动是叙事驱动，不是信息驱动；只在有新数据时更新先验。

### 新增：NWP 集合偏冷的量化观察

回测 `bias` 常数 **1.01 在干燥晴天只覆盖一半缺口**。七模式集合均值 vs 结算站（尖沙咀）实测：

| 日期 | 模式集合均值 | 结算站实测 | 偏差 |
|---|---:|---:|---:|
| 2026-09-09 | 30.06 | 32.2 | **+2.14** |
| 2026-09-10 | 30.13 | 31.8 | **+1.67** |

**样本仅 2 天，明确不作为定论**，只作「上档情景」的交叉验证：干燥晴天取
「模式集合均值 + 1.7」为 mu 上沿、「+ 1.0」为下沿。

同时记下一条更稳的判据：**模式对绝对水平不可信，对日际变化通常可信** ——
9/11 模式说比 9/10 热 0.88 °C，HKO 官方说冷 1 °C，两者打架时取中值并压仓位。
条目内附了可直接复现的七模式 API URL。

---

## v0.13.2 — 2026-09-10 · 干掉那个黑框（PATCH）

**现象**：每跑一次脚本，屏幕上冒出一个命令窗口。

**这不是脚本 bug，是 Windows 的进程模型**：`python.exe` / `cmd.exe` 都是**控制台子系统**程序，
Windows 规定它们必须挂在控制台上。当启动它的宿主自己没有控制台时（桌面版应用、资源管理器
双击、计划任务、部分 GUI 启动器），系统会**新建一个控制台窗口**给子进程——那就是黑框。
`--watch --loop` 跑到 17:00 才退出，所以框也一直不关。

两条触发路径，分别处理：

1. **`watch.bat` 必然开窗**：`.bat` 只能由 cmd.exe 解析，而 cmd.exe 是控制台程序，
   双击它一定出框。新增 `watch_silent.vbs`——用 `WScript.Shell.Run(..., 0, False)`
   把窗口样式设成隐藏，输出照旧写 `watch.log`，屏幕上什么都不出现。
   配套 `stop_watch.vbs`（窗口藏了就没有 Ctrl-C 可按，按命令行精确匹配
   `hk_edge.py --loop` 结束进程）。`watch.bat` 保留但加了醒目注释指向 vbs。

2. **宿主新建控制台**：`hk_edge.py` 启动时自查——若该控制台**只挂着自己一个进程**
   （`GetConsoleProcessList` 返回 1）且 stdout 不在终端上，就 `ShowWindow(hwnd, SW_HIDE)`
   自我隐藏。用户在 cmd / Windows Terminal 里手动跑时，控制台上还挂着 cmd.exe，
   列表长度 > 1，**不动**——不会把用户自己的窗口弄没。

实测对拍（`CREATE_NO_WINDOW` vs `CREATE_NEW_CONSOLE` 启动同一个探针）：

| 启动方式 | 控制台 | 窗口可见 | 结果 |
|---|---|---|---|
| `CREATE_NO_WINDOW`（当前宿主） | 无 | — | 不需要处理 |
| `CREATE_NEW_CONSOLE`（GUI 启动器） | 有，仅 1 个进程 | 可见 → **隐藏** | 自我隐藏生效 |

**中途踩的坑**：第一版写成了 `kernel32.ShowWindow`——`ShowWindow` 住在 **user32**，
`kernel32.ShowWindow` 直接 `AttributeError`；而它被 `try/except` 吞掉，表面无事、
实际根本没隐藏。同时 HWND 是 64 位指针，必须 `restype = c_void_p` +
`argtypes = [c_void_p, c_int]`，否则按默认 int 传会被截断。两者都已在代码里注明。

---

## v0.13.1 — 2026-09-10 · 修 v0.13.0 里让「长进闭环」空转的三处（PATCH）

v0.13.0 引入的 `--score` 与站点序列归档，实际全都没能按设计工作。都是今天盘中发现的。

1. **`--watch --loop` 在 Windows 下重定向输出就崩（P0，静默丢弃整个外推块）**
   stdout 被 `>` 重定向时 Python 用 GBK，而输出里有 ⚠ / 📈，第一处就抛
   `UnicodeEncodeError`。因为它发生在 `try` 内部，被就地吞成一行
   `[日内外推跳过: ...]` —— **不报错、不退出，只是概率全没了**。
   实测（09-10 13:50，`watch.bat >> watch.log`）：日志里就是
   `[loop] 本次采集失败: UnicodeEncodeError`，当次采集完全作废。
   现在 `main()` 在任何 print 之前把 stdout/stderr reconfigure 到 UTF-8 + `errors=replace`。
   对照验证：`PYTHONIOENCODING=gbk` 下，v0.13.0 → `[日内外推跳过: 'gbk' codec...]`，
   v0.13.1 → 完整输出 23 行。

2. **站点序列归档只挂在「跨天」分支上，今天的数据永远进不了档**
   `_archive_1min()` 只在发现日志日期 ≠ 今天时被调用，也就是说必须等明天跑一次
   才把今天归档；明天若忘了跑，今天就永久丢失。改为每次采样后都调用
   （函数自带长度比较，幂等）。`data/obs_1min_archive.json` 已开始累积
   —— 这是把分位表从 ERA5 网格口径换成结算站口径的唯一途径。

3. **`--score` 会被重复样本污染**：`forecast_log.jsonl` 只追加不判重，同一分钟内
   手工跑两次 `--watch` 就写两条相同记录，样本量翻倍、Brier 被稀释。
   现在同一 `(date, t)` 覆盖而非追加。

顺带：本地 `model_calib.json` 已重校准（bias 1.008→1.007，n 86→83，窗口到 09-10），
该文件在 `.gitignore` 内，不入库。

---

## v0.13.0 — 2026-09-10 · 日内外推改为 one-touch 口径（**MAJOR：午后数字全变**）

结算量是**日内路径的最大值**，这是连续监控的向上触碰（one-touch），不是数字期权。
据此重写 `--watch` 的日内外推，并修掉两个由旧结构直接导致的系统性偏差。

**1. 剩余升温改建模 `R = M − 已观测最高`，用历史经验分位表，不用正态**

旧式 `peak = 当前读数 + 气候表 E[M−T_h] + 站点加成 k` 的三个错误（ERA5 + NOAA ISD 机场站实测）：

| 问题 | 实测 |
|---|---|
| 斜率：旧式 `net = E[M−T_h] − g`（g=回落）隐含 β=−1.0 | 回归真实 β = −0.222(h12) / −0.132(h13) / −0.088(h14) / −0.035(h15) / −0.007(h16)。回落幅度 g 基本是时刻的确定性函数，几乎不含未来信息 |
| 离散：`rise_sd` 用跨日无条件 sd（h=16 时 0.871） | 条件 sd 实测仅 **0.082**，尾部放大约 10 倍 |
| 形状：假设正态 | 剩余升温右偏且 0 处有原子（h=13 时 35.5% 的日子 13 点就是全天最高）。正态近端高估 41%、远端低估 3.4 倍 |

**误差在 13 时前后反号**：13 时前偏冷 0.28–0.4°C，14 时后偏暖 0.3–1.0°C。
同时解释 2026-09-08 16:00 的 P(33)=22~68%（真实基准 ≈0.7%，市场报 0.6~1.3% 是对的）
和 09-10 上午的系统性偏冷。

**2. 站点偏差不再回归气候均值 / 不再封顶**

`k = 气候偏差 − 当下偏差` 与 `fill_bias = min(cur_bias, 气候偏差)` 都会在干燥晴热天
制造持续性冷偏差（当天偏差长期 +2.4 而不回到 +1.01，等于硬砍 1.4°C）。
现在水位 L 直接取**今日已采样时段的中位偏差**（抗 10 分钟级抖动），只留 ±1.5°C sane 带。

**3. 今天模式自己的日变化曲线只以「偏离 δ」进入**

`δ = ρ̂ − ρ̄_h`，限幅 ±1.0°C。气候升水已隐含在经验分布里，NWP 只贡献增量，
避免把点估计当确定性用（晚峰日如 09-08 仍能拿到真实剩余升温）。
δ 取代了原来手调的"已过峰值守卫"——午后剩余升水自然收敛到 0，不再需要 `s_hour/s_gap`。

**验证**（`scripts/build_remaining_rise.py --validate`，ERA5 4266 天，奇数年建表/偶数年检验，δ=0）：
Brier 平均 **−25.3%**；h=14 −51%，h=15 −85%，h=16 **−94%**；仅 h=11 的 Δ=0.5 略差（+4.1%）。

**4. 新增：让它能自己变好**

- `--score` — 给 `data/forecast_log.jsonl` 里的历史预测打 Brier + 可靠性分。
  每次 `--watch` 都会把概率向量落地。改完分位表/校准参数可以直接看分数变化，
  不用再靠盘中人工复盘（09-08~09-10 连续三轮偏差都是事后才抓到的）。
- 0.1°C 站点序列**按日归档**到 `data/obs_1min_archive.json`。
- `scripts/build_remaining_rise.py` — 可重建分位表（`--validate` 附新旧对照）。

**已知未解（输出里也打了提示）**：分位表基于 ERA5 网格，尾部比真实站点薄约
**4.7 倍**（9月 h=13 的 P(剩余升温≥0.5)：网格 7.0% vs 机场站 32.8%）→
给出的尾档概率是**下界**，站点口径约为其 2–5 倍。
彻底解决要等 `obs_1min_archive.json` 攒够结算站自己的逐时序列后重建表。

---

## v0.12.5 — 2026-09-10

文档补丁（**不改变任何数字**，三处版本号已同步）。「已知局限」新增最高优先级一条：
**nowcast 的站点偏差回归长期均值是持续性冷偏差来源**。干燥晴天偏差不回归
（9/10 12:20 实测仍 +2.40，脚本仍按 −1.39 折减），导致峰值估计被下限顶回、等于不预测升温。
实测后果：P32 估计三小时被市场连续上修打脸 22%→25%→32%→58%，市场 55%→63%→74.5% 全程更准。
修正：此类日站点加成衰减到 0 ~ −0.5；并加一条通用纪律 ——
**自己的估计若连续 3 次朝同方向被市场修正，视为系统性偏差，向市场收敛一半并按保守情景 Kelly 下注。**

---

## v0.12.4 — 2026-09-10

文档补丁（**不改变任何数字**，三处版本号已同步）。交易纪律两条：
1. **深度不足 ≠ 不报仓位**（用户明确要求）。只要有 edge 就必须给 Kelly 三件套
   （金额 / 份数 / 占本金%）；深度只影响执行方式（吃单 vs 挂限价），不影响该买多少。
   禁止再出现「可选、很小、深度只有 N 份」这种不给数字的表述。
2. **Kelly 是独立头寸口径**：与现有持仓同档重叠打 7–8 折，反向对冲腿不打折。

---

## v0.12.3 — 2026-09-10

文档补丁（**不改变任何数字**，三处版本号已同步）。交易纪律新增「**止盈对比用总财富口径**」：
判断卖不卖必须用 `卖出所得现金 + 剩余头寸期望派息` vs `原头寸期望派息`。
实测踩坑：把已卖部分成本从分母剔除却漏加卖出所得，会得出「留点敞口更好」的反向结论
（错误口径 −1% vs −9%；正确口径 −3.9%/+4.9%/+23.7% 全卖都占优）。
**卖出价 > 公允价时，多卖一份就多赚一份，不要为留敞口少卖。**

---

## v0.12.2 — 2026-09-10

文档补丁（**不改变任何数字**，三处版本号已同步）。交易纪律新增「**单档概率硬上限**」：
1 °C 档的概率上限 = `2Φ(0.5/sd) − 1`（sd 0.89 → **43%**）。
**某档 YES bid 超过该上限 = 无条件卖出，不需要判断 mu。**
实测 9/10：32 YES bid 0.46–0.48 > 43% 硬顶，而模型/官方/市场三锚点分歧 0.9 °C 吵不出结论，
硬上限直接给答案。适用于锚点打架、无法定中枢的场景。

---

## v0.12.1 — 2026-09-09

文档补丁（**不改变任何数字**，三处版本号已同步）。交易纪律新增一条：
**报浮盈必须同时报可成交量**。实测 9/10 的 32 YES：bid 0.260 但两侧深度都只有 20 份，
150 份的仓位浮盈 +20% 大部分无法兑现。浮盈是可报价，可成交量才是可兑现，两者须成对给出。

---

## v0.12.0 — 2026-09-09 · 结论末尾强制输出「下次刷新时间」

**动机**：「算完就忘」是本工具最大的实操漏洞。09-09 的教训是 13:00→14:00 之间漏掉 +1.2 °C 的峰值，
把 31 档算成 66% 而它其实已归零 —— 不是概率算错，是**结论过期了还在用**。
用户明确要求：每次结论最后都要提醒下一次刷新时间。

**改动**（新增输出行，**不改变任何概率 / EV / Kelly 数字**）：

1. 新增常量与两个函数：`MARKET_CLOSE_HKT=20`、`NWP_LAND_HKT=(2,8,14,20)`、
   `FND_UPDATE_HKT`（每 2h :30）、`PEAK_WINDOW=(12,17)`；
   `next_refresh(now, target, is_today)` 取最近的一个未到锚点，
   `print_next_refresh()` 输出 `■ 下次刷新：MM-DD HH:MM HKT（Xh YYm 后）—— 触发原因`。
2. 三条路径全部接上：`hk_edge.py --date …`（含 7 天扫描）、`--watch`、`--watch --loop` 的单次输出。
3. 已过目标日 20:00 HKT（= 12:00 UTC 收盘）→ 输出「已收盘，不再重跑」，改为提醒回填 `decision_log.csv`。
4. `SKILL.md` 新增「输出纪律」整节：**聊天里的口头结论也要给刷新时间，不只是脚本输出**，
   并附锚点表（NWP 落地 / fnd / rhrread / 10 分钟 CSV / CLOB 实时）。

三处版本号已同步（`--version` 输出 0.12.0）。三条路径已实测通过。

---

## v0.11.1 — 2026-09-09

文档补丁（**不改变任何输出数字**，三处版本号已同步）。新增两条用 9/9、9/10 实盘换来的方法论：

1. **NO 边 EV 会系统性高估，必须手工复算**（写进 `A0`）。
   脚本内部把 NO 价算成 `1 − m`，而 `m` 是 `--market` 传入的 YES 价；若传的是 YES **ask**，
   则 `1 − YES ask = NO 的 bid`，那是卖出 NO 才拿得到的价格。
   实测 9/10 的 31 档：脚本 +8.2%（过门槛）vs NO ask 0.570 复算 **+6.3%**（不过门槛）。
   修法：凡脚本在 NO 边报动作，一律用 `market_prices.py` 的 NO ask 重算 `P(NO)/ask − 1`。
2. **「连晴干燥日」模型系统性偏冷**（写进「已知局限」）。
   9/9 模型 mu 30.65 vs 实测 32.2，误差 −1.55 °C ≈ 2 sd。措辞含
   「Dry during the day / Sunny periods」时必须叠加 regime 修正，bias 常数不够用。
3. 附带记录：**P(N 档) 是 mu 的帐篷函数，档位中点见顶后随 mu 上升而下降** ——
   NO 头寸「越暖越安全」，与直觉相反（9/10 的 NO 31 公允 57.4%→62.6%）。


---

## v0.11.0 — 2026-09-09 · 加速：`--watch --loop` 常驻轮询 + `--sigma` 时跳过集合

**先说诊断结论（这一版所有改动都基于它）**：单次运行的耗时**几乎全在 TCP/TLS 握手上，
跟数据量无关**。实测同一主机：首次请求 1.02s，连接热了之后 0.20s；
7 模型 × 10 天（1251 B）与 1 模型 × 2 天（318 B）在热连接上都是 0.20s。
所以**砍参数、砍模型、砍天数几乎不省时间**，省时间只能靠"少握手 / 不重新握手"。

1. **新增 `--watch --loop [--loop-min N] [--until HH:MM]`**：一个进程内反复采集，
   复用同一条 TLS 连接。实测同一进程内连续 4 次 watch()：1.85s → **0.36 / 1.24 / 0.73s**，
   即首次之后平均约 0.6~0.8s，而每 10 分钟起一个新进程每次都要付 ~1.7s。
   **约 2.5~3 倍**。走的还是同一个 `watch()`，数字一模一样。
2. **`--sigma` 给出时跳过 ECMWF 集合请求**：那时概率直接用 `N(mu, sigma)`，
   集合成员根本不进入计算（`draws=[mu]`），而集合是三个请求里最慢的一个。
   冷启动 `--date --mu --sigma --no-cache` 从 1.80s → 1.52s。
   「集合离散度」如实显示 `n/a(--sigma)`，不假装有数。
3. 实测记录（本机）：`--watch` 1.56s / `--date` 缓存命中 **0.39s** / `--date --no-cache` 1.65s /
   `market_prices` 1.38s。

配套：Windows 计划任务改为 11:00 启动 `--watch --loop --until 17:00`，
重复间隔放宽到 30 分钟且 `MultipleInstancesPolicy=IgnoreNew` —— 重复触发只是
**进程挂掉时的兜底**，正常情况下不会重复启动，也不会漏采。

**数字不变**：概率 / EV / Kelly 的计算方式一律未触碰，只改"什么时候发请求、发几次"。
验证方式：`--date 2026-09-09 --mu 32.2 --sigma 0.35` 改动前后各档公允 P 逐位一致
（32 档 97.8% / 33 档 2.2%），唯一差异是「集合离散度」显示为 n/a。

---

## v0.10.0 — 2026-09-09 · 接入 0.1 °C / 10 分钟实况源（结算精度的关键修复）

`--watch` 与 `load_observed_max()` 的实况从 `rhrread`（**整数位 + 每小时**）换成
**分区气温 CSV**（**0.1 °C + 每 10 分钟**，39 站，含结算站）。这是 09-09 用一次实亏换来的。

**改了什么**
- 新增常量 `HKO_OBS_1MIN`（`.../regional-weather/latest_1min_temperature.csv`）
  与 `OBS_STATION_NAMES`（结算站在该 CSV 里叫 **`HK Observatory`**）。
- 新增 `_http_raw()`（非 JSON 接口取原文）、`fetch_hko_1min()`、`load_1min_max()`、
  `_load_1min_log()`，running max 落在 `data/obs_1min_log.json`，跨天自动作废。
- `watch()`：三路并行（0.1 °C 源 / rhrread / 网格），**优先 0.1 °C 源**，
  取不到才回落到 rhrread 并打印醒目提示；输出新增「实况源」一行。
- `load_observed_max()`：0.1 °C running max 与整点日志**取大者**，`hk_edge.py --date 今日`
  无需再手动传 `--observed-max`。
- 回填未采样小时用的 `sampled_h` 现在并入 10 分钟序列覆盖到的小时。

**为什么值这一版**
`rhrread` 在结算值刚跨过整数边界时双重失真：① 漏掉整点之间的升温
（09-09 13:00 报 31，实际 13:40 已 **32.2**，差 1.2 °C）；② 整数舍入撑开 ±0.5 °C 未知。
后果是 13:49 算出「31 档 66%」，而**31 档当时已归零——错 66 个概率点**。
接入后同一时点自动得到「已观测最高 32.2 → 众数档 32」，并正确触发已过峰值守卫
（回落 0.9 °C）。

**数字会变**：只影响当日（`--date == today`）的实况输入与由此而来的下限截断、
站点偏差、过峰守卫；**历史回测、提前 N≥1 天的概率、Kelly 公式一律未触碰**。
验证方式：`--watch` 前后逐行比对，唯一变化是实况源与 `今日已观测最高`
（31 → 32.2），其余结构不变；`--date 2026-09-10` 输出与改动前逐位一致。

**必须配套**：CSV 只给「最新一笔」，不自带当日最高 —— **要每 10 分钟跑一次 `--watch`**
才能累积 running max（cron `*/10 0-12 * * *` UTC）。轮询越密，漏掉的峰值越小。

---

## v0.9.3 — 2026-09-09 · 新增 A3：实况源必须用 0.1 °C / 10 分钟分区气温

**这是一次真实亏损换来的（09-09 当天）**，优先级高于其它条目。

`rhrread` 的 `temperature` 只有**整数位**且**每小时**更新，在结算值刚跨过整数边界时会双重失真：
① 漏掉整点之间的升温（13:00 仍报 31，实际 13:40 已达 **32.2**）；
② 整数舍入带来 ±0.5 °C 未知，直接决定 30/31/32 三档归属。
后果：按 31 外推得出「31 档还有 66%」，而**31 档当时已归零**。

- 新增 A3 小节，给出正确数据源
  `https://data.weather.gov.hk/weatherAPI/hko_data/regional-weather/latest_1min_temperature.csv`
  （0.1 °C / 10 分钟 / 39 站；结算站名是 **`HK Observatory`**）。
- 给出判断规则：当日且实测离整数边界 <0.5 °C 时，必须以该 CSV 为准。
- **TODO**：同目录「午夜至現時最高最低氣溫」的 CSV 文件名未猜到（`latest_max_min_temperature.csv`
  等 6 个候选均 404），未确认前仍由 `--watch` 按 10 分钟轮询自建 running max。
- **数字不变**：本版只改文档，脚本尚未接入该源（接入属 MINOR，留待下一版）。

---

## v0.9.2 — 2026-09-09 · 按实测更新频率重定 `--watch` 的盯盘节奏

原来建议「08:00–20:00 每 10 分钟跑一次 `--watch`」，但**跑得比数据还快**：实测（2026-09-09）
`rhrread` 是**每小时**更新（整点后约 2 分钟，08:02 → 10:02）、HKO 9 天预报约每 2 小时
（07:50 → 09:50）、NWP 多模式 daily tmax 一天 4 次（08:43 vs 10:05，7 个模式仅 UKMO +0.3）。
10 分钟一次只是在重复读同一个数。

- 改为：**概率重算 = 每小时一次**（整点后 5 分）；**13:00–17:00 峰值窗口加密到每 30 分钟**。
- 明确区分：需要高频盯的是**盘口价**（实时，用 `market_prices.py`），不是 `--watch`。
  实测 08:45→09:47 一小时内 31 档 YES 从 0.420 走到 0.505，EV 从 +23% 推到 +43%。
- 附实测更新频率表，后续再调节奏有据可依。
- **数字不变**：只改文档，脚本计算路径未触碰。

---

## v0.9.1 — 2026-09-08 · 修「已观测下限」提示对非当日目标的误报

查未来日期（如 `--date 2026-09-09`）时，`bucket_probs` 并不会用当日已实测最高温截断
（`floor` 只在 `t == today` 时传入），但提示行无条件打印
「已观测下限 … → 当日分布截断重命名」，读者会误以为目标日分布已被截断。

- 现在区分两种情况：目标日包含当日时照原样打印；否则打印
  「注: …已实测最高 X°C 已知，但目标日不是当日 → 不做截断」。
- **数字不变**：只改打印文案，概率 / EV / Kelly 的计算路径未触碰。
  验证方式：同一条命令 `--date 2026-09-09 --market "…"` 修改前后各档公允 P 逐位一致
  （29:0.1968 / 30:0.4254 / 31:0.2851 / 32:0.0607 / 33:0.0040）。

---

## v0.9.0 — 2026-09-08 · 修正日内外推的三处系统性偏差

`--watch` 的日内外推以前会**凭空高估午后还能升多少**，三个来源叠加，实测在 2026-09-08 把
P(≥33) 推到 25%（当时市场只给 3%）。三处都修了：

1. **观测不对时**：rhrread 整点更新、实际滞后可达 1 小时，却拿 `now.hour` 去取网格值，
   等于用 15 时的实测比 16 时的网格（当日多算出 +0.4 °C 偏差）。
   现在所有小时内推算一律以**观测时间戳**为基准，并在滞后 ≥1 小时时打印提示。
2. **「今日已观测最高」只是自身轮询采样的最大值**：漏点即系统性低估当日真实最高
   （09-08 漏掉 12、13 时整点）。它是概率下限、也是「已过峰值守卫」的触发条件——
   低估等于让模型以为"还能再升"。现在用模式逐时值 + 站点偏差回填**没有实测样本的小时**
   （`past_hours` 拉到当天 0 时），有实测的一律以实测为准。
   回填偏差**封顶在气候值**（`min(当下偏差, calib.bias)`）：当下偏差今天能在 1 小时内
   摆 ±1.3 °C（09-08：15 时 +0.3 → 16 时 +1.7），直接拿它外推未采样小时会凭空宣称
   "中午已到过 33.1 °C"。宁可低估，不可高估。
3. **过峰守卫与概率起算点不一致（双重惩罚）**：守卫里的 `net = gross − g` 本来就是
   "在已观测最高之上还能再升多少"，但概率循环却从**已回落的当前读数**起算，
   把同一段回落罚了两次。现在起算点统一为 `max(当前读数, 已观测最高)`。

同快照对照（09-08 16:00，站点 32 °C）：v0.8.0 峰值 32.34 °C / P(≥33)=22%；
v0.9.0 峰值 32.40 °C / P(≥33)=24% —— 概率几乎没变，但**下限与守卫站到了完整的数据上**：
已观测最高按未采样的中午时段修到 32.4 °C，过峰守卫随之正确触发（回落 0.4 °C）。

⚠️ 已知未解：整套估计对"站点偏差"这个输入**高度敏感**——同一快照，偏差取单点 +1.70 得
P(≥33)=22%，取全天稳健均值 +0.73 得 68%。本案不对此下结论（那等于给 P(≥33) 凭空调档），
只做保守封顶；要定标它需要逐时站点历史，见 `SKILL.md`「已知弱点」。

- 批量订单簿 `POST /books` 已验证：与逐个并行拉取的输出**逐字一致**（19 行全等），
  耗时 0.79s vs 回退路径 1.72s；批量失败仍自动回退，功能不依赖该接口。

## v0.8.0 — 2026-09-08 · Kelly 比例 1/4(25%) → 35%

- 仓位缩放从 **1/4 Kelly（25%）改为 35% Kelly**：建议在原来的基础上放大 1.4 倍
- 抽成常量 `KELLY_FRAC = 0.35`（`scripts/hk_edge.py`），金额、份数、占本金%、表头、
  参数说明全部由它派生——以后再调只改一处，不会再散落
- 表头由 `1/4Kelly(金额/份数)` 变为 `35%Kelly(金额/份数)`
- SKILL.md 交易纪律与 README 同步改为 35% Kelly
- ⚠️ 提醒：这会让**单笔下注额放大 40%**。容量约束没变（近月档深度常常只有几十到几百份），
   Kelly 算出来的金额超过可成交量时，**仍以可成交量为准**（见 SKILL.md A0）

## v0.7.0 — 2026-09-08 · 反向 edge = 止盈提醒 + 持仓检查

- 每档新增**止盈警告**：某方向「现在买入」的 EV ≤ `--exit-ev`（默认 **−15%**）时打
  `⚠持YES止盈-XX%` / `⚠持NO止盈-XX%`。含义不是反向开仓，而是
  **市价已明显高于公允值，持有人应平仓而不是持有到期**
- 新增 `--hold "档位:成本价:份数[:Y|N]"`：把已用头寸喂进去，直接输出
  **份数 / 成本 / 现价 / 浮盈% / 公允值 / 现在买入EV / 建议（止盈·持有观察·仍低估可加仓）**
- 新增 `--exit-ev` 调整止盈阈值；SKILL.md 纪律要求每次复跑必须在结论里点名提醒这类信号
- 只新增展示与提醒，**概率、EV、Kelly 的数值计算均未改动**

## v0.6.0 — 2026-09-08 · 建议输出增加「份数」

- 1/4 Kelly 列从「只给金额」改为 **金额 + 建议份数 + 占本金百分比**：
  下单界面填的是份数，只给金额等于没给
- 份数 = 金额 ÷ 该档单价（买 YES 用市场价 `m`，买 NO 用 `1−m`）；
  输出末尾固定提示「按订单簿可执行价复核」——单价差 1 分钱份数就差不少
- SKILL.md 交易纪律改为**三件套**（金额 / 份数 / 占本金%），并写进 A 节输出说明
- 只改展示，**不改变任何概率、EV 或 Kelly 数值**

## v0.5.0 — 2026-09-07 · 性能：全流程 ~9s → ~1.5–3s

只动传输层，**任何一个数字的计算方式都没变**（已逐位比对：优化前后概率表最大差 `0.00e+00`）。

- `requests.Session` 保活 + 互不依赖的接口并行 → 主脚本 3.2s → 冷 1.3s / **热 0.2s**
- `market_prices.py` 改用 CLOB **批量接口 `/books`**（22 个盘口 1 次请求）：5.6–7.3s → **1.2–1.6s**；
  批量失败自动回退逐个并行拉取
- 模式 / 官方预报类接口加 **15 分钟磁盘缓存**；**实况观测与订单簿价格永不缓存**
- 命中缓存时输出必打印数据年龄；新增 `--no-cache`、`--ttl <秒>`（默认 900）
- 延迟导入 `requests`；历史 CSV 只在真需要重校准时加载
- `--watch` 的观测与网格实况并行：1.7s → 1.2s
- 文档：SKILL.md / README 新增「性能与缓存」章节

## v0.4.0 — 2026-09-07 · 已过峰值守卫（修午后系统性高估）

- `--watch` 日内外推新增**已过峰值守卫**：实测已从今日最高回落 ≥0.3 °C 且时点 ≥14 时 →
  剩余升温期望与离散度大幅收窄，只留少量"晚间反弹创出新高"的可能
- 修掉一个荒谬输出：09-07 16:00（29 °C vs 已观测最高 30 °C）P(31) 此前给 **56%**，修复后 **~2%**
- 与「外推低于已实测最高」是同一族问题的两面：早上高估是网格还没到过那么高，
  午后高估是**已经到过又回落了**

## v0.3.0 — 2026-09-07 · 订单簿取价（A0）

- 新增 `scripts/market_prices.py`：从 **CLOB 订单簿**取可成交价（YES/NO 各自 bid/ask + 深度）
- 明确「最后成交价 vs 订单簿」的差异（实测同一时刻差 15–17 分钱），
  算 EV 必须用**可执行价**；买卖方向对应 ask/bid 的规则写进 SKILL.md

## v0.2.0 — 2026-09-07 · 日内分布以实况为条件 + Kelly 常驻

- 新增 `--observed-max`：以「今日已实测最高」截断并重命名分布（省略时自动读 `--watch` 日志）
- 新增 `--mu`：手动覆盖点估计（午后雷暴时模式的日最高值不会实时下修）
- 新增 `--sigma`：给出**剩余总不确定性**，不再叠加集合距平
- 1/4 Kelly 列改为常驻，同时输出金额与占本金百分比；未给 `--bankroll` 时按 1000 USDC 并标注假设
- `decision_log.csv` 记录「公允 P vs 市场价 vs edge」，供结算后复盘

## v0.1.0 — 2026-09-07 · 首个可用版本

- 多模型（7 家）确定性融合 + ECMWF 集合，dressed ensemble 输出整数档公允概率
- 92 天回测自动校准 `bias` / `resid_sd`
- `--watch` 日内实况追踪，写 `intraday_log.json`
- `backtest.py` / `hitrate.py` / `analyze.py` / `diurnal.py` 校准与分析脚本

---

**v1.0.0 的门槛**：累积 30 笔带实际结算结果的复盘（`decision_log.csv` 填回实际档位），
并用真实命中率校准过自己的概率之后再发。
